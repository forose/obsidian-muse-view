/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/main.ts
var main_exports = {};
__export(main_exports, {
  default: () => MuseViewPlugin
});
module.exports = __toCommonJS(main_exports);
var import_obsidian7 = require("obsidian");

// src/settings.ts
var import_obsidian = require("obsidian");

// src/i18n.ts
var ZH = {
  "plugin.name": "MuseView",
  "view.title": "\u788E\u7247\u8BB0\u5FC6",
  "input.placeholder": "\u8BB0\u5F55\u6B64\u523B\u7684\u60F3\u6CD5\u2026 \u7528 #\u6807\u7B7E \u5F52\u7C7B\uFF0CEnter \u53D1\u9001",
  "input.placeholder.mod": "\u8BB0\u5F55\u6B64\u523B\u7684\u60F3\u6CD5\u2026 \u7528 #\u6807\u7B7E \u5F52\u7C7B\uFF0CCmd/Ctrl+Enter \u53D1\u9001",
  "input.placeholderWithTag": "\u8BB0\u5F55\u6B64\u523B\u7684\u60F3\u6CD5\u2026 \u5C06\u5F52\u5165 #{tag}",
  "input.editPlaceholder": "\u7F16\u8F91 {date} {time} \u7684\u8BB0\u5FC6",
  "input.editTimeTitle": "\u4FEE\u6539\u521B\u5EFA\u65F6\u95F4",
  "input.hint": "Enter \u53D1\u9001 \xB7 Shift+Enter \u6362\u884C",
  "input.hint.mod": "Cmd/Ctrl+Enter \u53D1\u9001 \xB7 Shift+Enter \u6362\u884C",
  "input.submit": "\u53D1\u9001",
  "input.cancel": "\u53D6\u6D88",
  "input.edit.submit": "\u4FDD\u5B58",
  "input.edit.cancel": "\u53D6\u6D88",
  "search.placeholder": "\u641C\u7D22",
  "sidebar.tags": "\u6807\u7B7E",
  "sidebar.all": "\u5168\u90E8",
  "sidebar.pinned": "\u7F6E\u9876",
  "sidebar.starred": "\u661F\u6807",
  "sidebar.today": "\u4ECA\u5929",
  "sidebar.week": "\u672C\u5468",
  "sidebar.review": "\u56DE\u987E",
  "sidebar.todo": "\u5F85\u529E",
  "sidebar.noTag": "\u65E0\u6807\u7B7E",
  "sidebar.withImage": "\u6709\u56FE\u7247",
  "sidebar.withLink": "\u6709\u94FE\u63A5",
  "sidebar.random": "\u968F\u673A",
  "sidebar.section.views": "\u89C6\u56FE",
  "sidebar.section.search": "\u7B5B\u9009",
  "sidebar.section.years": "\u5E74\u4EFD",
  "sidebar.section.tags": "\u6807\u7B7E",
  "stat.total": "\u6761\u6570",
  "stat.tags": "\u6807\u7B7E",
  "stat.days": "\u5929\u6570",
  "stats.memos": "\u6761\u6570",
  "stats.tags": "\u6807\u7B7E",
  "stats.days": "\u5929\u6570",
  "empty.title": "\u8FD8\u6CA1\u6709\u8BB0\u5F55",
  "empty.text": "\u5728\u4E0A\u65B9\u8F93\u5165\u6846\u5199\u4E0B\u7B2C\u4E00\u6761\u788E\u7247\u8BB0\u5FC6\u5427",
  "empty.sub": "\u652F\u6301 #\u6807\u7B7E\u3001![[\u56FE\u7247]] \u4E0E\u5B8C\u6574 Markdown",
  "empty.default": "\u8FD9\u91CC\u7A7A\u7A7A\u5982\u4E5F",
  "empty.defaultSub": "\u5207\u6362\u7B5B\u9009\u6761\u4EF6\uFF0C\u6216\u5199\u4E0B\u65B0\u7684\u8BB0\u5FC6",
  "empty.todo": "\u6CA1\u6709\u5F85\u529E\u4E8B\u9879",
  "empty.todoSub": "\u52FE\u9009\u5361\u7247\u91CC\u7684\u590D\u9009\u6846\u5373\u53EF\u51FA\u73B0\u5728\u8FD9\u91CC",
  "empty.onThisDay": "\u8FD9\u4E00\u5929\u8FD8\u6CA1\u6709\u8BB0\u5F55",
  "empty.onThisDaySub": "\u770B\u770B\u5F80\u5E74\u4ECA\u5929\uFF0C\u6216\u968F\u673A\u7FFB\u7FFB\u65E7\u8BB0\u5FC6",
  "empty.onThisDayBtn": "\u968F\u673A\u4E00\u6761",
  "card.pin": "\u7F6E\u9876",
  "card.unpin": "\u53D6\u6D88\u7F6E\u9876",
  "card.star": "\u661F\u6807",
  "card.unstar": "\u53D6\u6D88\u661F\u6807",
  "card.edit": "\u7F16\u8F91",
  "card.openSource": "\u6253\u5F00\u6E90\u6587\u4EF6",
  "card.copySource": "\u590D\u5236\u6E90\u6587\u672C",
  "card.exportImage": "\u5BFC\u51FA\u4E3A\u56FE\u7247",
  "card.copyImage": "\u590D\u5236",
  "card.exportPng": "\u4FDD\u5B58",
  "card.delete": "\u5220\u9664",
  "card.pinnedMark": "\u5DF2\u7F6E\u9876",
  "card.starredMark": "\u5DF2\u661F\u6807",
  "card.collapseFull": "\u5C55\u5F00\u5168\u6587",
  "card.collapseLess": "\u6536\u8D77",
  "notice.confirmDelete": "\u786E\u5B9A\u5220\u9664\u8FD9\u6761\u7B14\u8BB0\u5417\uFF1F",
  "notice.confirmDeleteOk": "\u786E\u8BA4\u5220\u9664",
  "notice.saved": "\u5DF2\u4FDD\u5B58",
  "notice.deleted": "\u5DF2\u5220\u9664",
  "notice.pinned": "\u5DF2\u7F6E\u9876",
  "notice.unpinned": "\u5DF2\u53D6\u6D88\u7F6E\u9876",
  "notice.starred": "\u5DF2\u661F\u6807",
  "notice.unstarred": "\u5DF2\u53D6\u6D88\u661F\u6807",
  "notice.copied": "\u5DF2\u590D\u5236",
  "notice.exportDone": "\u5DF2\u4FDD\u5B58\u56FE\u7247\uFF1A{name}",
  "notice.exportFailed": "\u4FDD\u5B58\u56FE\u7247\u5931\u8D25\uFF1A{msg}",
  "notice.copiedImage": "\u56FE\u7247\u5DF2\u590D\u5236\u5230\u526A\u8D34\u677F",
  "notice.copyFailed": "\u590D\u5236\u56FE\u7247\u5931\u8D25\uFF1A{msg}",
  "modal.shareTitle": "\u5206\u4EAB\u56FE\u9884\u89C8",
  "notice.error": "\u51FA\u9519\u4E86\uFF1A",
  "notice.saveFailed": "\u4FDD\u5B58\u5931\u8D25\uFF1A{msg}",
  "notice.invalidTime": "\u65F6\u95F4\u683C\u5F0F\u65E0\u6548\uFF0C\u8BF7\u91CD\u65B0\u9009\u62E9",
  "notice.dailyGoalDone": "\u4ECA\u65E5\u76EE\u6807\u5DF2\u8FBE\u6210 \u{1F389}",
  "notice.checkFailed": "\u4EFB\u52A1\u52FE\u9009\u5931\u8D25\uFF1A{msg}",
  "settings.header": "MuseView \u8BBE\u7F6E",
  "settings.folder": "\u7B14\u8BB0\u6587\u4EF6\u5939",
  "settings.folder.desc": "MuseView \u5728\u6B64\u6587\u4EF6\u5939\u4E0B\u8BFB\u5199 YYYY.md \u6587\u4EF6\uFF08\u76F8\u5BF9 vault \u6839\u76EE\u5F55\uFF09",
  "settings.attachment": "\u56FE\u7247\u9644\u4EF6\u6587\u4EF6\u5939",
  "settings.attachment.desc": "\u7C98\u8D34/\u62D6\u62FD/\u9009\u62E9\u7684\u56FE\u7247\u4F1A\u4FDD\u5B58\u5230\u6B64\u76EE\u5F55\uFF08\u76F8\u5BF9 vault \u6839\u76EE\u5F55\uFF09",
  "settings.clear": "\u53D1\u9001\u540E\u6E05\u7A7A\u8F93\u5165\u6846",
  "settings.pageSize": "\u6BCF\u6B21\u52A0\u8F7D\u6761\u6570",
  "settings.pageSize.desc": "\u7011\u5E03\u6D41\u6BCF\u6B21\u5C55\u793A\u591A\u5C11\u6761\uFF0C\u6EDA\u52A8\u5230\u5E95\u81EA\u52A8\u52A0\u8F7D\u66F4\u591A",
  "settings.sidebarTags": "\u4FA7\u680F\u6807\u7B7E\u5C55\u793A",
  "settings.sidebarTags.desc": "\u9009\u62E9\u4FA7\u8FB9\u680F\u6807\u7B7E\u533A\u5982\u4F55\u5C55\u793A\uFF1A\u5173\u95ED / \u5C42\u7EA7\u6811 / \u6807\u7B7E\u4E91\u3002\u5173\u95ED\u540E\u53EF\u5728\u5361\u7247\u5E95\u90E8\u70B9\u51FB\u6807\u7B7E\u80F6\u56CA\u7B5B\u9009\uFF0C\u6216\u5728\u641C\u7D22\u6846\u8F93\u5165\u300C#\u6807\u7B7E\u540D\u300D\u7B5B\u9009\u3002",
  "settings.sidebarTags.off": "\u5173\u95ED",
  "settings.sidebarTags.tree": "\u5C42\u7EA7\u6811",
  "settings.sidebarTags.cloud": "\u6807\u7B7E\u4E91",
  "settings.useTrash": "\u5220\u9664\u65F6\u4FDD\u7559\u5230\u56DE\u6536\u7AD9",
  "settings.useTrash.desc": "\u5F00\u542F\u540E\uFF0C\u5220\u9664\u7684\u7B14\u8BB0\u4F1A\u8FFD\u52A0\u5230 <\u7B14\u8BB0\u6587\u4EF6\u5939>/_trash.md\uFF08\u800C\u4E0D\u662F\u5F7B\u5E95\u6D88\u5931\uFF09\uFF0C\u4FBF\u4E8E\u8BEF\u5220\u540E\u624B\u52A8\u6062\u590D\u3002\u5173\u95ED = \u5F7B\u5E95\u5220\u9664\u3002",
  "settings.trashMax.name": "\u56DE\u6536\u7AD9\u6700\u5927\u6761\u6570",
  "settings.trashMax.desc": "_trash.md \u4FDD\u7559\u7684\u6700\u5927\u7B14\u8BB0\u6570\uFF0C\u8D85\u51FA\u540E\u81EA\u52A8\u4E22\u5F03\u6700\u65E7\u7684\u3002\u9632\u6B62\u957F\u671F\u4F7F\u7528\u540E\u56DE\u6536\u7AD9\u6587\u4EF6\u53D8\u5F97\u8FC7\u5927\u5F71\u54CD\u6027\u80FD\u3002",
  "settings.trash.0": "\u4E0D\u9650\u5236\uFF08\u4E0D\u63A8\u8350\uFF09",
  "settings.trash.100": "100 \u6761",
  "settings.trash.300": "300 \u6761\uFF08\u63A8\u8350\uFF09",
  "settings.trash.500": "500 \u6761",
  "settings.trash.1000": "1000 \u6761",
  "settings.trash.3000": "3000 \u6761",
  "settings.density": "\u89C6\u56FE\u5BC6\u5EA6",
  "settings.density.compact": "\u7D27\u51D1",
  "settings.density.comfortable": "\u5BBD\u677E",
  "settings.density.desc": "\u7D27\u51D1\u6A21\u5F0F\u6BCF\u5F20\u5361\u53EA\u663E\u793A\u524D\u51E0\u884C\uFF0C\u9002\u5408\u5FEB\u901F\u6D4F\u89C8 1000+ \u6761\u7B14\u8BB0\uFF1B\u5BBD\u677E\u6A21\u5F0F\u662F\u9ED8\u8BA4",
  "settings.collapse.name": "\u957F\u7B14\u8BB0\u81EA\u52A8\u6298\u53E0",
  "settings.collapse.desc": "\u8D85\u8FC7\u8BBE\u5B9A\u884C\u6570\u7684\u7B14\u8BB0\u4F1A\u81EA\u52A8\u6298\u53E0\uFF0C\u5E95\u90E8\u663E\u793A\u300C\u7EE7\u7EED\u8BFB\u300D\u6309\u94AE\u3002\u56FE\u7247\u59CB\u7EC8\u5B8C\u6574\u663E\u793A\uFF0C\u53EA\u6298\u6587\u5B57\u90E8\u5206\u3002",
  "settings.collapse.0": "\u6C38\u4E0D\u6298\u53E0",
  "settings.collapse.4": "4 \u884C",
  "settings.collapse.6": "6 \u884C",
  "settings.collapse.8": "8 \u884C\uFF08\u63A8\u8350\uFF09",
  "settings.collapse.12": "12 \u884C",
  "settings.collapse.20": "20 \u884C",
  "settings.exportTheme.name": "\u5BFC\u51FA\u56FE\u7247 \xB7 \u80CC\u666F\u4E3B\u9898",
  "settings.exportTheme.desc": "\u4FDD\u5B58\u5361\u7247\u56FE\u7247\u65F6\u7684\u80CC\u666F\u6837\u5F0F\u30028 \u79CD\u7CBE\u9009\u4E3B\u9898 + \u8DDF\u968F Obsidian \u660E\u6697\u8272 + \u968F\u673A\u3002",
  "settings.exportTheme.auto": "\u{1F3AD} \u8DDF\u968F Obsidian \u660E\u6697",
  "settings.exportTheme.kraft": "\u{1F7EB} \u725B\u76AE\u7EB8",
  "settings.exportTheme.paper": "\u{1F4C4} \u7EB8\u5F20\u767D",
  "settings.exportTheme.charcoal": "\u26AB \u6728\u70AD\u9ED1",
  "settings.exportTheme.midnight": "\u{1F319} \u5348\u591C\u84DD",
  "settings.exportTheme.mint": "\u{1F37F} \u8584\u8377\u7EFF",
  "settings.exportTheme.lavender": "\u{1F49C} \u85B0\u8863\u8349",
  "settings.exportTheme.peach": "\u{1F351} \u871C\u6843\u7C89",
  "settings.exportTheme.sky": "\u2601\uFE0F \u6674\u7A7A\u84DD",
  "settings.exportTheme.random": "\u{1F3B2} \u6BCF\u6B21\u968F\u673A",
  "settings.defaultOverview.name": "\u4FA7\u680F\u9ED8\u8BA4\u89C6\u56FE",
  "settings.defaultOverview.desc": "\u6253\u5F00 MuseView \u9762\u677F\u65F6\u4FA7\u680F\u9876\u90E8\u9ED8\u8BA4\u663E\u793A\u54EA\u4E2A\u89C6\u56FE\u3002\u4ECD\u7136\u53EF\u4EE5\u968F\u65F6\u70B9\u5207\u6362\u6309\u94AE\u4E34\u65F6\u5207\u6362\u5230\u53E6\u4E00\u4E2A\uFF08\u4E34\u65F6\u5207\u6362\u4E0D\u4F1A\u6539\u8FD9\u91CC\u7684\u9ED8\u8BA4\u503C\uFF09\u3002",
  "settings.defaultOverview.heatmap": "\u{1F525} \u70ED\u529B\u56FE\uFF08\u9ED8\u8BA4\uFF09",
  "settings.defaultOverview.calendar": "\u{1F4C5} \u6708\u5386",
  "settings.heatmap.name": "\u663E\u793A\u8D21\u732E\u7F51\u683C",
  "settings.heatmap.desc": "\u5728\u4E3B\u5217\u8868\u9876\u90E8\u663E\u793A\u4E00\u5E74\u8D21\u732E\u7F51\u683C\uFF08\u6BCF\u65E5\u6D3B\u8DC3\u65B9\u5757\uFF09\u3002\u7EDF\u8BA1\u884C\u7684\u300C\u6700\u8FD1\u4E00\u5E74\u8D21\u732E / \u6700\u957F\u8FDE\u7EED / \u6700\u8FD1\u8FDE\u7EED\u300D\u59CB\u7EC8\u663E\u793A\uFF0C\u4E0D\u53D7\u6B64\u5F00\u5173\u5F71\u54CD\u3002",
  "settings.mood.name": "\u542F\u7528\u60C5\u611F\u8272\u5F69\u53EF\u89C6\u5316",
  "settings.mood.desc": "\u6839\u636E\u5185\u5BB9\u5173\u952E\u8BCD\uFF0C\u5728\u5361\u7247\u5DE6\u8FB9\u663E\u793A 3px \u8272\u6761\u30027 \u79CD\u7EF4\u5EA6\uFF1A\u5F00\u5FC3(\u91D1)\u3001\u611F\u52A8(\u7C89)\u3001\u9F13\u52B1(\u6A59)\u3001\u4F4E\u843D(\u84DD\u7070)\u3001\u70E6\u8E81(\u7EA2\u8910)\u3001\u5BB3\u6015(\u6697\u7D2B)\u3001\u75B2\u60EB(\u7070\u68D5)\u3002\u57FA\u4E8E\u5173\u952E\u8BCD\u8BCD\u5178\uFF0C\u4F1A\u6709\u8BEF\u5224\u3002",
  "settings.smartReview.name": "\u542F\u7528\u667A\u80FD\u56DE\u987E",
  "settings.smartReview.desc": "\u300C\u968F\u673A 5 \u6761\u300D\u6539\u7528\u52A0\u6743\u7B97\u6CD5\u6311\u9009\uFF1A\u8D8A\u4E45\u6CA1\u7FFB\u8FC7\u7684\u8D8A\u4F18\u5148\u3001\u548C\u4ECA\u5929\u6807\u7B7E/\u60C5\u7EEA\u547C\u5E94\u7684\u52A0\u5206",
  "settings.livePreview.name": "\u7F16\u8F91\u6846\u5B9E\u65F6\u9884\u89C8",
  "settings.livePreview.desc": "\u5355\u680F Live Preview\uFF1A\u5149\u6807\u6240\u5728\u884C\u663E\u793A markdown \u6E90\u7801\uFF0C\u5176\u4F59\u884C\u5B9E\u65F6\u6E32\u67D3\u4E3A\u5BCC\u6587\u672C\uFF08\u4EFF Obsidian \u7F16\u8F91\u89C6\u56FE\uFF09\u3002\u5173\u95ED\u5219\u56DE\u9000\u4E3A\u7EAF\u6587\u672C\u8F93\u5165\u6846\u3002\u5207\u6362\u540E\u91CD\u65B0\u6253\u5F00 MuseView \u89C6\u56FE\u751F\u6548\u3002",
  "settings.heading.newFeatures": "\u529F\u80FD\u5F00\u5173",
  "settings.heading.about": "\u5173\u4E8E",
  "settings.about.p1": "MuseView \u2014 \u788E\u7247\u7B14\u8BB0\u63D2\u4EF6\u3002\u6240\u6709\u7B14\u8BB0\u4EE5\u7EAF Markdown \u683C\u5F0F\u5B58\u50A8\uFF08",
  "settings.about.p2": "\uFF09\uFF0C\u505C\u7528\u63D2\u4EF6\u540E\u4F60\u7684\u7B14\u8BB0\u4F9D\u7136\u5B8C\u6574\u53EF\u8BFB\u3002",
  "settings.repo.name": "GitHub \u4ED3\u5E93",
  "settings.repo.desc": "\u67E5\u770B\u6E90\u7801\u3001\u53CD\u9988\u95EE\u9898\u3001\u63D0\u51FA\u5EFA\u8BAE\uFF0C\u90FD\u5728\u8FD9\u91CC\u89C1",
  "settings.repo.btn": "\u6253\u5F00\u4ED3\u5E93",
  "settings.version": "\u5F53\u524D\u7248\u672C\uFF1Av{ver}",
  "settings.bindFile.name": "\u7ED1\u5B9A\u6587\u4EF6",
  "settings.bindFile.desc": "\u70B9\u51FB\u6307\u5B9A\u7684 Markdown \u6587\u4EF6\u65F6\u81EA\u52A8\u6253\u5F00 MuseView \u4E3B\u754C\u9762\uFF08\u7559\u7A7A\u8868\u793A\u7981\u7528\uFF09",
  "settings.bindFile.btn": "\u9009\u62E9\u6587\u4EF6",
  "settings.bindFile.clear": "\u6E05\u9664",
  "settings.bindFile.placeholder": "\u9009\u62E9\u8981\u7ED1\u5B9A\u7684\u6587\u4EF6",
  "settings.language": "\u8BED\u8A00",
  "settings.language.desc": "auto \u4F1A\u8DDF\u968F Obsidian \u8BED\u8A00\uFF1B\u624B\u52A8\u5207\u6362\u5373\u65F6\u751F\u6548\uFF08\u9700\u91CD\u5F00 MuseView \u89C6\u56FE\uFF09",
  "settings.language.auto": "\u81EA\u52A8\uFF08\u8DDF\u968F Obsidian\uFF09",
  "settings.language.zh": "\u7B80\u4F53\u4E2D\u6587",
  "settings.language.en": "English",
  "settings.sendHotkey": "\u53D1\u9001\u5FEB\u6377\u952E",
  "settings.sendHotkey.desc": "\u9ED8\u8BA4 Ctrl/Cmd+Enter \u53D1\u9001\u3002\u5982\u679C\u6309\u4E0B\u6CA1\u53CD\u5E94\uFF0C\u901A\u5E38\u662F Obsidian \u628A Ctrl+Enter \u7ED1\u5B9A\u7ED9\u4E86\u5185\u7F6E\u547D\u4EE4\u300C\u5728\u65B0\u6807\u7B7E\u9875\u4E2D\u6253\u5F00\u5149\u6807\u5904\u94FE\u63A5\u300D\uFF08\u6309\u952E\u641C\u7D22\u641C\u4E0D\u5230\uFF0C\u9700\u8981\u5728 \u8BBE\u7F6E \u2192 \u5FEB\u6377\u952E \u5217\u8868\u91CC\u624B\u52A8\u6ED1\u5230\u8BE5\u547D\u4EE4\u4F4D\u7F6E\uFF0C\u70B9\u51FB\u53F3\u4FA7 \xD7 \u89E3\u9664\u7ED1\u5B9A\uFF09\u3002\u89E3\u9664\u540E\u5373\u53EF\u6062\u590D\uFF0C\u6216\u5207\u6362\u4E3A Enter \u53D1\u9001\u3002",
  "settings.sendHotkey.enter": "Enter \u53D1\u9001\uFF08Shift+Enter \u6362\u884C\uFF09",
  "settings.sendHotkey.mod": "Ctrl/Cmd+Enter \u53D1\u9001\uFF08Enter \u6362\u884C\uFF09\u2B50 \u9ED8\u8BA4",
  "settings.dailyGoal": "\u6BCF\u65E5\u76EE\u6807\u7B14\u8BB0\u6570",
  "settings.dailyGoal.desc": "\u5DE6\u4FA7\u680F\u70ED\u529B\u56FE\u4E0B\u65B9\u7684\u8FDB\u5EA6\u6761\u6EE1\u503C\u3002\u8BB0\u5F55\u8D8A\u7B80\u5355\u8D8A\u5BB9\u6613\u575A\u6301\uFF0C\u5EFA\u8BAE 3-7 \u6761\u3002",
  "toolbar.insertTag": "\u63D2\u5165\u6807\u7B7E",
  "toolbar.insertImage": "\u63D2\u5165\u56FE\u7247",
  "toolbar.insertUL": "\u65E0\u5E8F\u5217\u8868",
  "toolbar.insertOL": "\u6709\u5E8F\u5217\u8868",
  "toolbar.insertTask": "\u4EFB\u52A1\u6E05\u5355",
  "toolbar.insertTable": "\u63D2\u5165\u8868\u683C",
  "toolbar.toggleSidebar": "\u5207\u6362\u4FA7\u680F",
  "toolbar.quote": "\u5F15\u7528",
  "toolbar.more": "\u66F4\u591A",
  "list.dailyGoalExceed": "\u4ECA\u65E5 {done} \u6761\uFF0C\u8D85\u51FA\u76EE\u6807 {extra} \u6761",
  "list.dailyGoalDone": "\u4ECA\u65E5\u76EE\u6807 {goal} \u6761\uFF0C\u5DF2\u5B8C\u6210 {done} \u6761",
  "list.presetRandom": "\u968F\u673A\u56DE\u987E",
  "list.presetOnThisDay": "\u5386\u53F2\u4E0A\u7684\u4ECA\u5929",
  "list.presetPinned": "\u7F6E\u9876",
  "list.presetStarred": "\u661F\u6807",
  "list.totalCount": "\u5171 {n} \u6761",
  "list.loadMore": "\u52A0\u8F7D\u66F4\u591A\uFF08\u5269\u4F59 {n} \u6761\uFF09",
  "list.pinnedHead": "\u7F6E\u9876 \xB7 {n}",
  "list.imageHolder": "\u56FE\u7247",
  "list.noText": "\uFF08\u65E0\u6587\u5B57\uFF09",
  "list.heatmapMore": "\u7B49 {n} \u6761",
  "date.today": "\u4ECA\u5929",
  "date.yesterday": "\u6628\u5929",
  "month.0": "\u4E00\u6708",
  "month.1": "\u4E8C\u6708",
  "month.2": "\u4E09\u6708",
  "month.3": "\u56DB\u6708",
  "month.4": "\u4E94\u6708",
  "month.5": "\u516D\u6708",
  "month.6": "\u4E03\u6708",
  "month.7": "\u516B\u6708",
  "month.8": "\u4E5D\u6708",
  "month.9": "\u5341\u6708",
  "month.10": "\u5341\u4E00\u6708",
  "month.11": "\u5341\u4E8C\u6708",
  "meta.reroll": "\u6362\u4E00\u6279",
  "meta.backToOnThisDay": "\u56DE\u5230\u5386\u53F2\u4E0A\u7684\u4ECA\u5929",
  "meta.heatmapTitle": "\u8FD1\u671F\u6D3B\u8DC3",
  "meta.heatmapLess": "\u5C11",
  "meta.heatmapMore": "\u591A",
  "meta.heatmapTotalYear": "\u6700\u8FD1\u4E00\u5E74\u8D21\u732E\uFF1A<b>{n}</b> \u6B21",
  "meta.heatmapLongest": "\u6700\u957F\u8FDE\u7EED\u8D21\u732E\uFF1A<b>{n}</b> \u65E5",
  "meta.heatmapCurrent": "\u6700\u8FD1\u8FDE\u7EED\u8D21\u732E\uFF1A<b>{n}</b> \u65E5",
  "lightbox.close": "\u5173\u95ED",
  "lightbox.prev": "\u4E0A\u4E00\u5F20",
  "lightbox.next": "\u4E0B\u4E00\u5F20",
  "weekday.0": "\u5468\u65E5",
  "weekday.1": "\u5468\u4E00",
  "weekday.2": "\u5468\u4E8C",
  "weekday.3": "\u5468\u4E09",
  "weekday.4": "\u5468\u56DB",
  "weekday.5": "\u5468\u4E94",
  "weekday.6": "\u5468\u516D",
  "command.openMuseView": "\u6253\u5F00\u788E\u7247\u8BB0\u5FC6",
  "command.quickCapture": "\u5FEB\u901F\u8BB0\u5F55",
  "command.normalizeAll": "\u5F52\u4E00\u5316\u5168\u90E8\u7B14\u8BB0",
  "quickCapture.title": "\u{1F4AC} \u7A81\u7136\u60F3\u5230\u4E86\u4EC0\u4E48\uFF1F",
  "quickCapture.cancel": "\u53D6\u6D88",
  "quickCapture.send": "\u53D1\u9001",
  "notice.normalizeConfirm": "\u786E\u5B9A\u8981\u91CD\u65B0\u683C\u5F0F\u5316\u6240\u6709\u7B14\u8BB0\u6587\u4EF6\u5417\uFF1F\u6B64\u64CD\u4F5C\u4F1A\u89C4\u8303\u5316\u5168\u90E8 memo \u7684\u683C\u5F0F\u3002",
  "notice.normalizing": "\u6B63\u5728\u5F52\u4E00\u5316\u2026",
  "notice.normalized": "\u5DF2\u5F52\u4E00\u5316 {n} \u6761\u7B14\u8BB0",
  "notice.normalizeFailed": "\u5F52\u4E00\u5316\u5931\u8D25\uFF1A{msg}"
};
var EN = {
  "plugin.name": "MuseView",
  "view.title": "Memos",
  "input.placeholder": "Capture a thought\u2026 use #tag, Enter to send",
  "input.placeholder.mod": "Capture a thought\u2026 use #tag, Cmd/Ctrl+Enter to send",
  "input.placeholderWithTag": "Capture a thought\u2026 will be tagged #{tag}",
  "input.editPlaceholder": "Edit memo from {date} {time}",
  "input.editTimeTitle": "Change creation time",
  "input.hint": "Enter to send \xB7 Shift+Enter for newline",
  "input.hint.mod": "Cmd/Ctrl+Enter to send \xB7 Shift+Enter for newline",
  "input.submit": "Send",
  "input.edit.submit": "Save",
  "input.edit.cancel": "Cancel",
  "input.cancel": "Cancel",
  "search.placeholder": "Search",
  "sidebar.tags": "Tags",
  "sidebar.all": "All",
  "sidebar.pinned": "Pinned",
  "sidebar.starred": "Starred",
  "sidebar.today": "Today",
  "sidebar.week": "This week",
  "sidebar.review": "Review",
  "sidebar.todo": "To-do",
  "sidebar.noTag": "No tag",
  "sidebar.withImage": "With image",
  "sidebar.withLink": "With link",
  "sidebar.random": "Random",
  "sidebar.section.views": "Views",
  "sidebar.section.search": "Filters",
  "sidebar.section.years": "Years",
  "sidebar.section.tags": "Tags",
  "stat.total": "Memos",
  "stat.tags": "Tags",
  "stat.days": "Days",
  "stats.memos": "Memos",
  "stats.tags": "Tags",
  "stats.days": "Days",
  "empty.title": "No memos yet",
  "empty.text": "Write your first fleeting thought above",
  "empty.sub": "Supports #tags, ![[images]] and full Markdown",
  "empty.default": "Nothing here",
  "empty.defaultSub": "Switch filters, or write a new memo",
  "empty.todo": "No to-dos",
  "empty.todoSub": "Check a checkbox in a card to see it here",
  "empty.onThisDay": "Nothing on this day",
  "empty.onThisDaySub": "Look back at past years, or shuffle a memory",
  "empty.onThisDayBtn": "Shuffle one",
  "card.pin": "Pin",
  "card.unpin": "Unpin",
  "card.star": "Star",
  "card.unstar": "Unstar",
  "card.edit": "Edit",
  "card.openSource": "Open source file",
  "card.copySource": "Copy source text",
  "card.exportImage": "Export as image",
  "card.copyImage": "Copy",
  "card.exportPng": "Save",
  "card.delete": "Delete",
  "card.pinnedMark": "Pinned",
  "card.starredMark": "Starred",
  "card.collapseFull": "Expand",
  "card.collapseLess": "Collapse",
  "notice.confirmDelete": "Delete this memo?",
  "notice.confirmDeleteOk": "Delete",
  "notice.saved": "Saved",
  "notice.deleted": "Deleted",
  "notice.pinned": "Pinned",
  "notice.unpinned": "Unpinned",
  "notice.starred": "Starred",
  "notice.unstarred": "Unstarred",
  "notice.copied": "Copied",
  "notice.exportDone": "Image saved: {name}",
  "notice.exportFailed": "Save failed: {msg}",
  "notice.copiedImage": "Image copied to clipboard",
  "notice.copyFailed": "Copy failed: {msg}",
  "modal.shareTitle": "Share preview",
  "notice.error": "Error: ",
  "notice.saveFailed": "Save failed: {msg}",
  "notice.invalidTime": "Invalid datetime, please re-select",
  "notice.dailyGoalDone": "Daily goal reached \u{1F389}",
  "notice.checkFailed": "Toggle failed: {msg}",
  "settings.header": "MuseView Settings",
  "settings.folder": "Note folder",
  "settings.folder.desc": "MuseView reads/writes YYYY.md files under this folder (relative to vault root)",
  "settings.attachment": "Image attachment folder",
  "settings.attachment.desc": "Pasted/dropped/selected images are saved here (relative to vault root)",
  "settings.clear": "Clear input after sending",
  "settings.pageSize": "Memos loaded per page",
  "settings.pageSize.desc": "How many memos the feed shows at once; auto-loads more on scroll",
  "settings.sidebarTags": "Sidebar tags",
  "settings.sidebarTags.desc": 'How the sidebar tag area is displayed: off / hierarchical tree / tag cloud. When off, click a tag pill at the bottom of a card to filter, or type "#tag" in search.',
  "settings.sidebarTags.off": "Off",
  "settings.sidebarTags.tree": "Tree",
  "settings.sidebarTags.cloud": "Cloud",
  "settings.useTrash": "Keep deleted notes in trash",
  "settings.useTrash.desc": "When on, deleted notes are appended to <folder>/_trash.md instead of vanishing, so you can recover them manually. Off = permanent delete.",
  "settings.trashMax.name": "Trash max items",
  "settings.trashMax.desc": "Max notes kept in _trash.md; older ones are dropped automatically beyond this. Prevents the trash file from growing too large over time.",
  "settings.trash.0": "No limit (not recommended)",
  "settings.trash.100": "100 items",
  "settings.trash.300": "300 items (recommended)",
  "settings.trash.500": "500 items",
  "settings.trash.1000": "1000 items",
  "settings.trash.3000": "3000 items",
  "settings.density": "View density",
  "settings.density.compact": "Compact",
  "settings.density.comfortable": "Comfortable",
  "settings.density.desc": "Compact mode shows only the first few lines per card, good for browsing 1000+ notes; comfortable is the default.",
  "settings.collapse.name": "Auto-collapse long notes",
  "settings.collapse.desc": 'Notes longer than the set line count collapse automatically, with a "keep reading" button at the bottom. Images always show in full; only text folds.',
  "settings.collapse.0": "Never",
  "settings.collapse.4": "4 lines",
  "settings.collapse.6": "6 lines",
  "settings.collapse.8": "8 lines (recommended)",
  "settings.collapse.12": "12 lines",
  "settings.collapse.20": "20 lines",
  "settings.exportTheme.name": "Export image \xB7 background theme",
  "settings.exportTheme.desc": "Background style when saving a card as an image. 8 curated themes + follow Obsidian light/dark + random.",
  "settings.exportTheme.auto": "\u{1F3AD} Follow Obsidian",
  "settings.exportTheme.kraft": "\u{1F7EB} Kraft",
  "settings.exportTheme.paper": "\u{1F4C4} Paper white",
  "settings.exportTheme.charcoal": "\u26AB Charcoal",
  "settings.exportTheme.midnight": "\u{1F319} Midnight blue",
  "settings.exportTheme.mint": "\u{1F37F} Mint",
  "settings.exportTheme.lavender": "\u{1F49C} Lavender",
  "settings.exportTheme.peach": "\u{1F351} Peach",
  "settings.exportTheme.sky": "\u2601\uFE0F Sky blue",
  "settings.exportTheme.random": "\u{1F3B2} Random each time",
  "settings.defaultOverview.name": "Sidebar default view",
  "settings.defaultOverview.desc": "Which view shows at the top of the sidebar when you open MuseView. You can still switch temporarily with the toggle button (temporary switch does not change this default).",
  "settings.defaultOverview.heatmap": "\u{1F525} Heatmap (default)",
  "settings.defaultOverview.calendar": "\u{1F4C5} Calendar",
  "settings.heatmap.name": "Show contribution grid",
  "settings.heatmap.desc": 'Show a one-year contribution grid (daily-activity cells) at the top of the main list. The stats row "last year / longest streak / current streak" always shows regardless of this toggle.',
  "settings.mood.name": "Enable mood coloring",
  "settings.mood.desc": "Show a 3px color bar on the left of each card based on content keywords. 7 dimensions: happy(gold), moved(pink), encouraged(orange), low(blue-grey), annoyed(red-brown), afraid(dark purple), tired(grey-brown). Keyword-dictionary based, may misjudge.",
  "settings.smartReview.name": "Enable smart review",
  "settings.smartReview.desc": `"Random 5" uses a weighted algorithm: notes you haven't seen in a while rank higher, and ones whose tags/mood echo today get a bonus.`,
  "settings.livePreview.name": "Live preview in editor",
  "settings.livePreview.desc": "Single-pane Live Preview: the line with the cursor shows raw markdown, other lines render to rich text in real time (like Obsidian's editing view). Turn off to fall back to a plain textarea. Reopen the MuseView view after toggling.",
  "settings.heading.newFeatures": "Feature toggles",
  "settings.heading.about": "About",
  "settings.about.p1": "MuseView \u2014 a fleeting-note plugin. All notes are stored as plain Markdown (",
  "settings.about.p2": "), so your notes stay fully readable even after disabling the plugin.",
  "settings.repo.name": "GitHub repository",
  "settings.repo.desc": "Source code, issue reports and suggestions all live here.",
  "settings.repo.btn": "Open repository",
  "settings.version": "Current version: v{ver}",
  "settings.bindFile.name": "Bind file",
  "settings.bindFile.desc": "Auto-open the MuseView main view when you click the specified Markdown file (leave empty to disable).",
  "settings.bindFile.btn": "Choose file",
  "settings.bindFile.clear": "Clear",
  "settings.bindFile.placeholder": "Choose a file to bind",
  "settings.language": "Language",
  "settings.language.desc": "auto follows the Obsidian language; manual switch applies instantly (reopen the MuseView view).",
  "settings.language.auto": "Auto (follow Obsidian)",
  "settings.language.zh": "Simplified Chinese",
  "settings.language.en": "English",
  "settings.sendHotkey": "Send shortcut",
  "settings.sendHotkey.desc": `Default Ctrl/Cmd+Enter to send. If it doesn't respond, Obsidian usually bound Ctrl+Enter to the built-in command "Open link under cursor in new tab" \u2014 search can't find it; go to Settings \u2192 Hotkeys, scroll to that command, click the \xD7 on the right to unbind. Then it works, or switch to Enter to send.`,
  "settings.sendHotkey.enter": "Enter to send (Shift+Enter newline)",
  "settings.sendHotkey.mod": "Ctrl/Cmd+Enter to send (Enter newline) \u2B50 default",
  "settings.dailyGoal": "Daily goal (notes)",
  "settings.dailyGoal.desc": "Full value of the progress bar under the heatmap in the left sidebar. Simpler is easier to keep up; 3-7 suggested.",
  "toolbar.insertTag": "Insert tag",
  "toolbar.insertImage": "Insert image",
  "toolbar.insertUL": "Bullet list",
  "toolbar.insertOL": "Numbered list",
  "toolbar.insertTask": "Task list",
  "toolbar.insertTable": "Insert table",
  "toolbar.toggleSidebar": "Toggle sidebar",
  "toolbar.quote": "Quote",
  "toolbar.more": "More",
  "list.dailyGoalExceed": "Today {done}, over goal by {extra}",
  "list.dailyGoalDone": "Goal {goal}, done {done} today",
  "list.presetRandom": "Random review",
  "list.presetOnThisDay": "On this day",
  "list.presetPinned": "Pinned",
  "list.presetStarred": "Starred",
  "list.totalCount": "{n} memos",
  "list.loadMore": "Load more ({n} left)",
  "list.pinnedHead": "Pinned \xB7 {n}",
  "list.imageHolder": "image",
  "list.noText": "(no text)",
  "list.heatmapMore": "and {n} more",
  "date.today": "Today",
  "date.yesterday": "Yesterday",
  "month.0": "Jan",
  "month.1": "Feb",
  "month.2": "Mar",
  "month.3": "Apr",
  "month.4": "May",
  "month.5": "Jun",
  "month.6": "Jul",
  "month.7": "Aug",
  "month.8": "Sep",
  "month.9": "Oct",
  "month.10": "Nov",
  "month.11": "Dec",
  "meta.reroll": "Shuffle",
  "meta.backToOnThisDay": "Back to On this day",
  "meta.heatmapTitle": "Recent activity",
  "meta.heatmapLess": "Less",
  "meta.heatmapMore": "More",
  "meta.heatmapTotalYear": "Last year: <b>{n}</b> contributions",
  "meta.heatmapLongest": "Longest streak: <b>{n}</b> days",
  "meta.heatmapCurrent": "Current streak: <b>{n}</b> days",
  "lightbox.close": "Close",
  "lightbox.prev": "Previous",
  "lightbox.next": "Next",
  "weekday.0": "Sun",
  "weekday.1": "Mon",
  "weekday.2": "Tue",
  "weekday.3": "Wed",
  "weekday.4": "Thu",
  "weekday.5": "Fri",
  "weekday.6": "Sat",
  "command.openMuseView": "Open MuseView",
  "command.quickCapture": "Quick capture",
  "command.normalizeAll": "Normalize all notes",
  "quickCapture.title": "\u{1F4AD} What's on your mind?",
  "quickCapture.cancel": "Cancel",
  "quickCapture.send": "Send",
  "notice.normalizeConfirm": "Reformat all note files? This normalizes the format of every memo.",
  "notice.normalizing": "Normalizing\u2026",
  "notice.normalized": "Normalized {n} notes",
  "notice.normalizeFailed": "Normalize failed: {msg}"
};
var DICTS = { zh: ZH, en: EN };
function resolveLang(language, appLang) {
  if (language === "zh" || language === "en")
    return language;
  return (appLang == null ? void 0 : appLang.toLowerCase().startsWith("zh")) ? "zh" : "en";
}
function getAppLocale(app) {
  const fn = app.getLocale;
  return typeof fn === "function" ? fn.call(app) : "en";
}
function createT(lang) {
  const dict = DICTS[lang];
  return (key, vars) => {
    var _a, _b;
    let s = (_b = (_a = dict[key]) != null ? _a : ZH[key]) != null ? _b : key;
    if (vars) {
      for (const [k, v] of Object.entries(vars)) {
        s = s.replace(new RegExp(`\\{${k}\\}`, "g"), String(v));
      }
    }
    return s;
  };
}

// src/settings.ts
var DEFAULT_SETTINGS = {
  folder: "0- \u788E\u7247\u8BB0\u5FC6",
  attachmentFolder: "9- \u672C\u5730\u9644\u4EF6",
  clearAfterSave: true,
  pageSize: 50,
  sidebarTagsMode: "cloud",
  useTrash: false,
  exportTheme: "kraft",
  collapseLineLimit: 8,
  dailyGoal: 5,
  trashMaxItems: 300,
  density: "compact",
  enableMoodColoring: true,
  enableSmartReview: true,
  livePreview: true,
  showHeatmap: true,
  language: "zh",
  sendHotkey: "enter",
  defaultOverviewMode: "heatmap",
  bindFile: null
};
var REPO_URL = "https://github.com/forose/obsidian-muse-view";
var FileSuggestModal = class extends import_obsidian.SuggestModal {
  constructor(app, placeholder, onPick) {
    super(app);
    this.onPick = onPick;
    this.setPlaceholder(placeholder);
  }
  getSuggestions(query) {
    const q = query.toLowerCase();
    return this.app.vault.getMarkdownFiles().filter((f) => f.path.toLowerCase().includes(q));
  }
  renderSuggestion(file, el) {
    el.textContent = file.path;
  }
  onChooseSuggestion(file) {
    this.onPick(file);
  }
};
var MuseViewSettingTab = class extends import_obsidian.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
    this.t = createT(
      resolveLang(plugin.settings.language, getAppLocale(app))
    );
  }
  display() {
    const { containerEl } = this;
    containerEl.empty();
    containerEl.createEl("h2", { text: this.t("settings.header") });
    new import_obsidian.Setting(containerEl).setName(this.t("settings.folder")).setDesc(this.t("settings.folder.desc")).addText(
      (text) => text.setPlaceholder("0- \u788E\u7247\u8BB0\u5FC6").setValue(this.plugin.settings.folder).onChange(async (value) => {
        var _a;
        this.plugin.settings.folder = value.trim() || "0- \u788E\u7247\u8BB0\u5FC6";
        await this.plugin.saveSettings();
        (_a = this.plugin.getView()) == null ? void 0 : _a.refresh();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.t("settings.attachment")).setDesc(this.t("settings.attachment.desc")).addText(
      (text) => text.setPlaceholder("9- \u672C\u5730\u9644\u4EF6").setValue(this.plugin.settings.attachmentFolder).onChange(async (value) => {
        var _a;
        this.plugin.settings.attachmentFolder = value.trim() || "9- \u672C\u5730\u9644\u4EF6";
        await this.plugin.saveSettings();
        (_a = this.plugin.getView()) == null ? void 0 : _a.refresh();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.t("settings.clear")).addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.clearAfterSave).onChange(async (value) => {
        var _a;
        this.plugin.settings.clearAfterSave = value;
        await this.plugin.saveSettings();
        (_a = this.plugin.getView()) == null ? void 0 : _a.refresh();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.t("settings.sidebarTags")).setDesc(this.t("settings.sidebarTags.desc")).addDropdown(
      (dropdown) => dropdown.addOption("off", this.t("settings.sidebarTags.off")).addOption("tree", this.t("settings.sidebarTags.tree")).addOption("cloud", this.t("settings.sidebarTags.cloud")).setValue(this.plugin.settings.sidebarTagsMode).onChange(async (value) => {
        var _a;
        this.plugin.settings.sidebarTagsMode = value;
        await this.plugin.saveSettings();
        (_a = this.plugin.getView()) == null ? void 0 : _a.renderSidebar();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.t("settings.useTrash")).setDesc(this.t("settings.useTrash.desc")).addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.useTrash).onChange(async (value) => {
        var _a;
        this.plugin.settings.useTrash = value;
        await this.plugin.saveSettings();
        (_a = this.plugin.getView()) == null ? void 0 : _a.refresh();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.t("settings.trashMax.name")).setDesc(this.t("settings.trashMax.desc")).addDropdown(
      (dropdown) => dropdown.addOption("0", this.t("settings.trash.0")).addOption("100", this.t("settings.trash.100")).addOption("300", this.t("settings.trash.300")).addOption("500", this.t("settings.trash.500")).addOption("1000", this.t("settings.trash.1000")).addOption("3000", this.t("settings.trash.3000")).setValue(String(this.plugin.settings.trashMaxItems)).onChange(async (value) => {
        var _a;
        this.plugin.settings.trashMaxItems = parseInt(value, 10);
        await this.plugin.saveSettings();
        (_a = this.plugin.getView()) == null ? void 0 : _a.refresh();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.t("settings.density")).setDesc(this.t("settings.density.desc")).addDropdown(
      (dropdown) => dropdown.addOption("compact", this.t("settings.density.compact")).addOption("comfortable", this.t("settings.density.comfortable")).setValue(this.plugin.settings.density).onChange(async (value) => {
        var _a;
        this.plugin.settings.density = value;
        await this.plugin.saveSettings();
        (_a = this.plugin.getView()) == null ? void 0 : _a.applyDensity();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.t("settings.collapse.name")).setDesc(this.t("settings.collapse.desc")).addDropdown(
      (dropdown) => dropdown.addOption("0", this.t("settings.collapse.0")).addOption("4", this.t("settings.collapse.4")).addOption("6", this.t("settings.collapse.6")).addOption("8", this.t("settings.collapse.8")).addOption("12", this.t("settings.collapse.12")).addOption("20", this.t("settings.collapse.20")).setValue(String(this.plugin.settings.collapseLineLimit)).onChange(async (value) => {
        var _a;
        this.plugin.settings.collapseLineLimit = parseInt(value, 10);
        await this.plugin.saveSettings();
        (_a = this.plugin.getView()) == null ? void 0 : _a.refresh();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.t("settings.exportTheme.name")).setDesc(this.t("settings.exportTheme.desc")).addDropdown(
      (dropdown) => dropdown.addOption("auto", this.t("settings.exportTheme.auto")).addOption("kraft", this.t("settings.exportTheme.kraft")).addOption("paper", this.t("settings.exportTheme.paper")).addOption("charcoal", this.t("settings.exportTheme.charcoal")).addOption("midnight", this.t("settings.exportTheme.midnight")).addOption("mint", this.t("settings.exportTheme.mint")).addOption("lavender", this.t("settings.exportTheme.lavender")).addOption("peach", this.t("settings.exportTheme.peach")).addOption("sky", this.t("settings.exportTheme.sky")).addOption("random", this.t("settings.exportTheme.random")).setValue(this.plugin.settings.exportTheme).onChange(async (value) => {
        var _a;
        this.plugin.settings.exportTheme = value;
        await this.plugin.saveSettings();
        (_a = this.plugin.getView()) == null ? void 0 : _a.refresh();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.t("settings.defaultOverview.name")).setDesc(this.t("settings.defaultOverview.desc")).addDropdown(
      (dropdown) => dropdown.addOption("heatmap", this.t("settings.defaultOverview.heatmap")).addOption("calendar", this.t("settings.defaultOverview.calendar")).setValue(this.plugin.settings.defaultOverviewMode).onChange(async (value) => {
        var _a;
        this.plugin.settings.defaultOverviewMode = value;
        await this.plugin.saveSettings();
        (_a = this.plugin.getView()) == null ? void 0 : _a.setOverviewMode(
          this.plugin.settings.defaultOverviewMode
        );
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.t("settings.heatmap.name")).setDesc(this.t("settings.heatmap.desc")).addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.showHeatmap).onChange(async (value) => {
        var _a;
        this.plugin.settings.showHeatmap = value;
        await this.plugin.saveSettings();
        (_a = this.plugin.getView()) == null ? void 0 : _a.refresh();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.t("settings.sendHotkey")).setDesc(this.t("settings.sendHotkey.desc")).addDropdown(
      (dropdown) => dropdown.addOption("enter", this.t("settings.sendHotkey.enter")).addOption("mod+enter", this.t("settings.sendHotkey.mod")).setValue(this.plugin.settings.sendHotkey).onChange(async (value) => {
        var _a;
        this.plugin.settings.sendHotkey = value;
        await this.plugin.saveSettings();
        (_a = this.plugin.getView()) == null ? void 0 : _a.refresh();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.t("settings.dailyGoal")).setDesc(this.t("settings.dailyGoal.desc")).addText((text) => {
      text.inputEl.type = "number";
      text.inputEl.setAttribute("min", "1");
      text.setPlaceholder("5").setValue(String(this.plugin.settings.dailyGoal)).onChange(async (value) => {
        var _a;
        const n = parseInt(value, 10);
        this.plugin.settings.dailyGoal = Number.isFinite(n) && n > 0 ? n : 5;
        await this.plugin.saveSettings();
        (_a = this.plugin.getView()) == null ? void 0 : _a.refresh();
      });
    });
    new import_obsidian.Setting(containerEl).setName(this.t("settings.pageSize")).setDesc(this.t("settings.pageSize.desc")).addText((text) => {
      text.inputEl.type = "number";
      text.inputEl.setAttribute("min", "1");
      text.setPlaceholder("50").setValue(String(this.plugin.settings.pageSize)).onChange(async (value) => {
        var _a;
        const n = parseInt(value, 10);
        this.plugin.settings.pageSize = Number.isFinite(n) && n > 0 ? n : 50;
        await this.plugin.saveSettings();
        (_a = this.plugin.getView()) == null ? void 0 : _a.refresh();
      });
    });
    containerEl.createEl("h3", {
      text: this.t("settings.heading.newFeatures")
    });
    new import_obsidian.Setting(containerEl).setName(this.t("settings.smartReview.name")).setDesc(this.t("settings.smartReview.desc")).addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.enableSmartReview).onChange(async (value) => {
        var _a;
        this.plugin.settings.enableSmartReview = value;
        await this.plugin.saveSettings();
        (_a = this.plugin.getView()) == null ? void 0 : _a.refresh();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.t("settings.mood.name")).setDesc(this.t("settings.mood.desc")).addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.enableMoodColoring).onChange(async (value) => {
        var _a;
        this.plugin.settings.enableMoodColoring = value;
        await this.plugin.saveSettings();
        (_a = this.plugin.getView()) == null ? void 0 : _a.refresh();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.t("settings.livePreview.name")).setDesc(this.t("settings.livePreview.desc")).addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.livePreview).onChange(async (value) => {
        var _a;
        this.plugin.settings.livePreview = value;
        await this.plugin.saveSettings();
        new import_obsidian.Notice(this.t("notice.saved"));
        (_a = this.plugin.getView()) == null ? void 0 : _a.refresh();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.t("settings.bindFile.name")).setDesc(this.t("settings.bindFile.desc")).addText(
      (text) => {
        var _a;
        return text.setPlaceholder(this.t("settings.bindFile.placeholder")).setValue((_a = this.plugin.settings.bindFile) != null ? _a : "").onChange(async (value) => {
          var _a2;
          this.plugin.settings.bindFile = value.trim() || null;
          await this.plugin.saveSettings();
          (_a2 = this.plugin.getView()) == null ? void 0 : _a2.refresh();
        });
      }
    ).addButton(
      (btn) => btn.setButtonText(this.t("settings.bindFile.btn")).onClick(() => {
        new FileSuggestModal(
          this.app,
          this.t("settings.bindFile.placeholder"),
          (file) => {
            this.plugin.settings.bindFile = file.path;
            this.plugin.saveSettings();
            this.display();
          }
        ).open();
      })
    ).addButton(
      (btn) => btn.setButtonText(this.t("settings.bindFile.clear")).onClick(() => {
        this.plugin.settings.bindFile = null;
        this.plugin.saveSettings();
        this.display();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.t("settings.language")).setDesc(this.t("settings.language.desc")).addDropdown(
      (dropdown) => dropdown.addOption("auto", this.t("settings.language.auto")).addOption("zh", this.t("settings.language.zh")).addOption("en", this.t("settings.language.en")).setValue(this.plugin.settings.language).onChange(async (value) => {
        this.plugin.settings.language = value;
        await this.plugin.saveSettings();
        new import_obsidian.Notice(this.t("notice.saved"));
        this.display();
      })
    );
    containerEl.createEl("h3", { text: this.t("settings.heading.about") });
    new import_obsidian.Setting(containerEl).setName(this.t("settings.repo.name")).setDesc(this.t("settings.repo.desc")).addButton(
      (btn) => btn.setButtonText(this.t("settings.repo.btn")).onClick(() => window.open(REPO_URL, "_blank"))
    );
    const about = containerEl.createDiv({ cls: "muse-settings-about" });
    about.appendText(this.t("settings.about.p1"));
    about.appendText(this.t("settings.version", { ver: this.plugin.manifest.version }));
    about.appendText(this.t("settings.about.p2"));
  }
};

// src/store.ts
var import_obsidian3 = require("obsidian");

// src/parser.ts
var import_obsidian2 = require("obsidian");
var TAG_RE = /#([\p{L}\p{N}_/-]+)/gu;
var WIKILINK_IMG_RE = /!\[\[([^\]]+?)\]\]/g;
var MD_IMG_RE = /!\[[^\]]*\]\(([^)\s]+)(?:\s+"[^"]*")?\)/g;
function extractTags(text) {
  const out = /* @__PURE__ */ new Set();
  for (const m of text.matchAll(TAG_RE)) {
    const tag = m[1].trim();
    if (tag)
      out.add(tag);
  }
  return Array.from(out).sort((a, b) => a.localeCompare(b, "zh"));
}
function looksLikeImage(path) {
  const p = path.split(/[?#]/)[0];
  return /\.(png|jpe?g|gif|webp|svg|bmp|avif)(\|[^\]]*)?$/i.test(p);
}
function hasImage(text) {
  return /!\[\[[^\]]+\.(png|jpe?g|gif|webp|svg|bmp|avif)(\|[^\]]*)?\]\]/i.test(text) || /!\[[^\]]*\]\([^)]+\.(png|jpe?g|gif|webp|svg|bmp|avif)[^)]*\)/i.test(text);
}
function hasLink(text) {
  return /(^|[^!])\[\[[^\]]+\]\]/.test(text) || /(^|[^!])\[[^\]]+\]\([^)]+\)/.test(text);
}
function hasOpenTask(text) {
  return /^\s*[-*]\s+\[ \]/m.test(text);
}
function stripInlineTags(content) {
  const tags = [];
  const text = content.replace(
    /[ \t]*#([A-Za-z0-9_\u4e00-\u9fff][A-Za-z0-9_\u4e00-\u9fff/]*)/g,
    (_m, t) => {
      if (!tags.includes(t))
        tags.push(t);
      return "";
    }
  ).split("\n").map((l) => l.replace(/\s+$/, "")).join("\n").replace(/\n{3,}/g, "\n\n").trim();
  return { text, tags };
}
function extractMemoImages(app, content, filePath) {
  const images = [];
  let s = content.replace(WIKILINK_IMG_RE, (_m, inner) => {
    const link = inner.trim();
    const p = link.split(/[?#]/)[0];
    if (!looksLikeImage(p))
      return _m;
    const dest = app.metadataCache.getFirstLinkpathDest(p, filePath);
    if (!(dest instanceof import_obsidian2.TFile))
      return _m;
    images.push({ src: app.vault.getResourcePath(dest), alt: dest.basename });
    return "";
  });
  s = s.replace(MD_IMG_RE, (_m, url) => {
    const link = url.trim();
    const p = link.split(/[?#]/)[0];
    if (!looksLikeImage(p) && !link.startsWith("data:image/") && !/^https?:/i.test(link)) {
      return _m;
    }
    let src = link;
    if (!/^https?:/i.test(link) && !link.startsWith("data:")) {
      const dest = app.metadataCache.getFirstLinkpathDest(link, filePath);
      if (dest instanceof import_obsidian2.TFile)
        src = app.vault.getResourcePath(dest);
    }
    images.push({ src, alt: link || "image" });
    return "";
  });
  const text = s.split("\n").map((l) => l.replace(/\s+$/, "")).join("\n").replace(/\n{3,}/g, "\n\n").trim();
  return { text, images };
}

// src/types.ts
var VIEW_TYPE_MUSE = "muse-view";
var SYSTEM_TAGS = ["\u7F6E\u9876", "\u6536\u85CF"];
var RESERVED_TAG_PIN = "\u7F6E\u9876";
var RESERVED_TAG_STAR = "\u6536\u85CF";
var RESERVED_TAGS = /* @__PURE__ */ new Set([RESERVED_TAG_PIN, RESERVED_TAG_STAR]);

// src/store.ts
var MemoStore = class {
  constructor(app, settings) {
    this.app = app;
    this.settings = settings;
    this.memos = [];
    this.listeners = [];
  }
  get folder() {
    return (0, import_obsidian3.normalizePath)(this.settings.folder);
  }
  folderPath() {
    return this.folder;
  }
  onChange(cb) {
    this.listeners.push(cb);
    return () => {
      this.listeners = this.listeners.filter((l) => l !== cb);
    };
  }
  emit() {
    for (const l of this.listeners)
      l();
  }
  /** 同步获取内存缓存的全部 memo（已按 pinned 优先 + 时间倒序排序）。 */
  getAll() {
    return this.memos;
  }
  /** 从 vault 重新读取全部年度文件并刷新缓存。 */
  async reloadAll() {
    const files = this.collectFiles();
    const parsed = (await Promise.all(
      files.map(async (f) => {
        const raw = await this.app.vault.read(f);
        return parseMemos(f, raw);
      })
    )).flat();
    this.sortMemos(parsed);
    this.memos = parsed;
    this.emit();
  }
  /** 归一化全部年度文件：按规范格式重新序列化每条 memo（不动日期头/年份头）。返回归一化条数。 */
  async normalizeAll() {
    var _a;
    await this.reloadAll();
    const all = this.getAll();
    const byFile = /* @__PURE__ */ new Map();
    for (const m of all) {
      const arr = (_a = byFile.get(m.file.path)) != null ? _a : [];
      arr.push(m);
      byFile.set(m.file.path, arr);
    }
    let count = 0;
    for (const [path, memos] of byFile) {
      memos.sort((a, b) => b.range[0] - a.range[0]);
      const file = this.app.vault.getAbstractFileByPath(path);
      if (!(file instanceof import_obsidian3.TFile))
        continue;
      const lines = (await this.app.vault.read(file)).split(/\r?\n/);
      for (const m of memos) {
        const [start, end] = m.range;
        const replacement = serializeMemo(m.time, m.content).split("\n");
        lines.splice(start, end - start + 1, ...replacement);
        count++;
      }
      await this.app.vault.modify(file, lines.join("\n"));
    }
    await this.reloadAll();
    return count;
  }
  /** 重新读取单个年度文件（vault 事件回调用）。 */
  async reloadFile(file) {
    if (!this.isInFolder(file))
      return;
    const raw = await this.app.vault.read(file);
    const parsed = parseMemos(file, raw);
    this.memos = this.memos.filter((m) => m.file.path !== file.path);
    this.memos.push(...parsed);
    this.sortMemos(this.memos);
    this.emit();
  }
  /** 删除缓存中对应路径的 memo（vault 删除事件用）。 */
  removeFile(path) {
    const before = this.memos.length;
    this.memos = this.memos.filter((m) => m.file.path !== path);
    if (this.memos.length !== before)
      this.emit();
  }
  sortMemos(arr) {
    arr.sort((a, b) => {
      if (a.isPinned !== b.isPinned)
        return a.isPinned ? -1 : 1;
      const d = b.datetime.getTime() - a.datetime.getTime();
      if (d !== 0)
        return d;
      if (a.file.path !== b.file.path)
        return a.file.path < b.file.path ? 1 : -1;
      return b.range[0] - a.range[0];
    });
  }
  collectFiles() {
    const folder = this.folder;
    return this.app.vault.getMarkdownFiles().filter((f) => {
      if (f.name.startsWith("_"))
        return false;
      return f.path === `${folder}/${f.name}` || f.path.startsWith(`${folder}/`);
    });
  }
  isInFolder(file) {
    return !file.name.startsWith("_") && file.path.startsWith(`${this.folder}/`);
  }
  async ensureFolder(path) {
    if (!this.app.vault.getAbstractFileByPath(path)) {
      await this.app.vault.createFolder(path);
    }
  }
  /** 新增一条 memo（视图调用入口）。默认写入「当前时间」对应的年度文件。 */
  async addMemo(text, when = /* @__PURE__ */ new Date()) {
    const body = text.trim();
    if (!body)
      return;
    const year = when.getFullYear().toString();
    const dateStr = formatDate(when);
    const timeStr = formatTime(when);
    const weekday = WEEKDAYS[when.getDay()];
    const path = (0, import_obsidian3.normalizePath)(`${this.folder}/${year}.md`);
    const existing = this.app.vault.getAbstractFileByPath(path);
    if (existing instanceof import_obsidian3.TFile) {
      const raw = await this.app.vault.read(existing);
      const next = insertMemoIntoYear(raw, year, dateStr, weekday, timeStr, body);
      await this.app.vault.modify(existing, next);
    } else {
      await this.ensureFolder(this.folder);
      const created = `# ${year}

## ${dateStr} ${weekday}

${serializeMemo(timeStr, body)}
`;
      await this.app.vault.create(path, created);
    }
    const file = this.app.vault.getAbstractFileByPath(path);
    if (file instanceof import_obsidian3.TFile)
      await this.reloadFile(file);
  }
  /** 编辑 memo 正文（保留原始 date/time）。 */
  async editMemo(memo, text) {
    var _a;
    const body = text.trim();
    if (!body)
      return;
    const file = this.app.vault.getAbstractFileByPath(memo.file.path);
    if (!(file instanceof import_obsidian3.TFile))
      return;
    const raw = await this.app.vault.read(file);
    const lines = raw.split(/\r?\n/);
    const [s, e] = (_a = locateMemoRange(lines, memo)) != null ? _a : memo.range;
    const block = serializeMemo(memo.time, body).split("\n");
    lines.splice(s, e - s + 1, ...block);
    await this.app.vault.modify(file, lines.join("\n"));
    await this.reloadFile(file);
  }
  /** 编辑 memo 的正文与时间（视图「修改创建时间」入口）。 */
  async editMemoDateTime(memo, date, text) {
    var _a, _b;
    const body = text.trim();
    if (!body)
      return;
    const file = this.app.vault.getAbstractFileByPath(memo.file.path);
    if (!(file instanceof import_obsidian3.TFile))
      return;
    const srcYear = memo.file.basename;
    const dstYear = date.getFullYear().toString();
    const dateStr = formatDate(date);
    const timeStr = formatTime(date);
    const weekday = WEEKDAYS[date.getDay()];
    if (dstYear === srcYear) {
      const raw = await this.app.vault.read(file);
      const lines = raw.split(/\r?\n/);
      const [s, e] = (_a = locateMemoRange(lines, memo)) != null ? _a : memo.range;
      lines.splice(s, e - s + 1);
      removeOrphanDateHeaders(lines);
      trimTrailing(lines);
      const next = insertMemoIntoYear(
        lines.join("\n"),
        dstYear,
        dateStr,
        weekday,
        timeStr,
        body
      );
      await this.app.vault.modify(file, next);
      await this.reloadFile(file);
    } else {
      const raw = await this.app.vault.read(file);
      const lines = raw.split(/\r?\n/);
      const [s, e] = (_b = locateMemoRange(lines, memo)) != null ? _b : memo.range;
      lines.splice(s, e - s + 1);
      removeOrphanDateHeaders(lines);
      trimTrailing(lines);
      await this.app.vault.modify(file, lines.join("\n"));
      const dstPath = (0, import_obsidian3.normalizePath)(`${this.folder}/${dstYear}.md`);
      const dst = this.app.vault.getAbstractFileByPath(dstPath);
      if (dst instanceof import_obsidian3.TFile) {
        const rawDst = await this.app.vault.read(dst);
        const next = insertMemoIntoYear(
          rawDst,
          dstYear,
          dateStr,
          weekday,
          timeStr,
          body
        );
        await this.app.vault.modify(dst, next);
        await this.reloadFile(dst);
      } else {
        await this.ensureFolder(this.folder);
        const created = `# ${dstYear}

## ${dateStr} ${weekday}

${serializeMemo(timeStr, body)}
`;
        const newFile = await this.app.vault.create(dstPath, created);
        if (newFile instanceof import_obsidian3.TFile)
          await this.reloadFile(newFile);
      }
      await this.reloadFile(file);
    }
  }
  /** 删除一条 memo（先可选写入回收站，再删除正文行并清理孤儿日期标题）。 */
  async deleteMemo(memo) {
    const file = this.app.vault.getAbstractFileByPath(memo.file.path);
    if (!(file instanceof import_obsidian3.TFile))
      return;
    if (this.settings.useTrash) {
      try {
        await this.appendToTrash(memo);
      } catch (err) {
        console.error("[MuseView] \u5199\u5165\u56DE\u6536\u7AD9\u5931\u8D25\uFF08\u5C06\u7EE7\u7EED\u6267\u884C\u5220\u9664\uFF09:", err);
      }
    }
    const raw = await this.app.vault.read(file);
    const lines = raw.split(/\r?\n/);
    const [s, e] = memo.range;
    lines.splice(s, e - s + 1);
    removeOrphanDateHeaders(lines);
    trimTrailing(lines);
    await this.app.vault.modify(file, lines.join("\n"));
    await this.reloadFile(file);
  }
  /** 切换置顶（基于内联保留标签 `#置顶`）。 */
  async togglePinned(memo) {
    await this.toggleReservedTag(memo, RESERVED_TAG_PIN);
  }
  /** 切换星标（基于内联保留标签 `#收藏`）。 */
  async toggleStarred(memo) {
    await this.toggleReservedTag(memo, RESERVED_TAG_STAR);
  }
  async toggleReservedTag(memo, tag) {
    const has = memo.tags.includes(tag);
    let next;
    if (has) {
      const re = new RegExp(`\\s*#${escapeRe(tag)}(?![A-Za-z0-9_\\u4e00-\\u9fff/])`, "g");
      next = memo.content.replace(re, "").split("\n").map((l) => l.replace(/[ \t]+$/, "")).join("\n").replace(/\n{3,}/g, "\n\n").trim();
      if (next === "")
        next = `\uFF08\u5DF2\u53D6\u6D88${tag}\uFF09`;
    } else {
      const lines = memo.content.split("\n");
      if (lines.length === 0 || lines[0].trim() === "")
        lines[0] = `#${tag}`;
      else
        lines[0] = `${lines[0].replace(/\s+$/, "")} #${tag}`;
      next = lines.join("\n");
    }
    await this.editMemo(memo, next);
  }
  /** 保存图片附件到附件文件夹，返回可写入正文的链接路径。 */
  async saveImageAttachment(file, ext) {
    const folder = (0, import_obsidian3.normalizePath)(this.settings.attachmentFolder);
    await this.ensureFolder(folder);
    const d = /* @__PURE__ */ new Date();
    const pad = (n) => String(n).padStart(2, "0");
    const stamp = `${d.getFullYear()}${pad(d.getMonth() + 1)}${pad(d.getDate())}-${pad(d.getHours())}${pad(d.getMinutes())}${pad(d.getSeconds())}`;
    const rand = Math.random().toString(36).slice(2, 6);
    const extName = (ext || file.name.split(".").pop() || "png").replace(/^\./, "").toLowerCase();
    const path = `${folder}/mattach-${stamp}-${rand}.${extName}`;
    const buf = await file.arrayBuffer();
    await this.app.vault.createBinary(path, buf);
    return path;
  }
  /** 聚合全部标签及其出现次数（倒序）。 */
  async allTags() {
    var _a;
    const counts = /* @__PURE__ */ new Map();
    for (const m of this.memos) {
      for (const tag of m.tags) {
        if (RESERVED_TAGS.has(tag))
          continue;
        counts.set(tag, ((_a = counts.get(tag)) != null ? _a : 0) + 1);
      }
    }
    return Array.from(counts.entries()).map(([tag, count]) => ({ tag, count })).sort((a, b) => b.count - a.count || a.tag.localeCompare(b.tag, "zh"));
  }
  // ---------------------------------------------------------------- 回收站
  async appendToTrash(memo) {
    const folder = this.folder;
    await this.ensureFolder(folder);
    const path = (0, import_obsidian3.normalizePath)(`${folder}/_trash.md`);
    const now = /* @__PURE__ */ new Date();
    const head = `## \u5DF2\u5220\u9664 ${formatDate(now)} ${formatTime(now)}`;
    const body = memo.content.split("\n").map((l) => l === "" ? "" : `  ${l}`).join("\n");
    const entry = `
${head}

- \u6765\u6E90\uFF1A\`${memo.file.path}\` \xB7 \u539F\u65F6\u95F4 ${memo.date} ${memo.time}
${body}
`;
    const existing = this.app.vault.getAbstractFileByPath(path);
    if (existing instanceof import_obsidian3.TFile) {
      const raw = await this.app.vault.read(existing);
      const next = trimTrashToLimit(raw + entry, this.settings.trashMaxItems);
      await this.app.vault.modify(existing, next);
    } else {
      const header = `# MuseView \u56DE\u6536\u7AD9

> \u8FD9\u91CC\u4FDD\u5B58\u88AB\u5220\u9664\u7684\u7B14\u8BB0\u3002\u505C\u7528\u63D2\u4EF6\u540E\u4F9D\u7136\u53EF\u8BFB\uFF0C\u53EF\u624B\u52A8\u6062\u590D\u6216\u6E05\u7A7A\u3002
> \u8BE5\u6587\u4EF6\u4E0D\u4F1A\u88AB MuseView \u4E3B\u89C6\u56FE\u8BC6\u522B\u4E3A\u666E\u901A\u7B14\u8BB0\u3002
`;
      await this.app.vault.create(path, header + entry);
    }
  }
};
function parseMemos(file, content) {
  var _a;
  const lines = content.split(/\r?\n/);
  const out = [];
  let date = "";
  const dateRe = /^##\s+(\d{4}-\d{2}-\d{2})(?:\s+.+)?$/;
  const memoRe = /^-\s+(\d{2}:\d{2})\s?(.*)$/;
  const yearRe = /^#\s+\d{4}\s*$/;
  let i = 0;
  while (i < lines.length) {
    const line = lines[i];
    const dh = line.match(dateRe);
    if (dh) {
      date = dh[1];
      i++;
      continue;
    }
    const mp = line.match(memoRe);
    if (mp && date) {
      const time = mp[1];
      const firstRest = (_a = mp[2]) != null ? _a : "";
      const start = i;
      const contentLines = [firstRest];
      i++;
      while (i < lines.length) {
        const cur = lines[i];
        if (memoRe.test(cur) || dateRe.test(cur) || yearRe.test(cur))
          break;
        if (cur.startsWith("  ")) {
          contentLines.push(cur.slice(2));
          i++;
          continue;
        }
        if (cur.trim() === "") {
          let j = i + 1;
          while (j < lines.length && lines[j].trim() === "")
            j++;
          if (j >= lines.length)
            break;
          const nxt = lines[j];
          if (memoRe.test(nxt) || dateRe.test(nxt) || yearRe.test(nxt))
            break;
          if (nxt.startsWith("  ")) {
            for (let k = i; k < j; k++)
              contentLines.push("");
            i = j;
            continue;
          }
          break;
        }
        break;
      }
      while (contentLines.length && contentLines[0].trim() === "")
        contentLines.shift();
      while (contentLines.length && contentLines[contentLines.length - 1].trim() === "")
        contentLines.pop();
      const body = contentLines.join("\n");
      const tags = extractTags(body);
      out.push({
        file,
        range: [start, i - 1],
        date,
        time,
        datetime: makeDateTime(date, time),
        content: body,
        tags,
        hasImage: hasImage(body),
        hasLink: hasLink(body),
        hasOpenTask: hasOpenTask(body),
        isPinned: tags.includes(RESERVED_TAG_PIN),
        isStarred: tags.includes(RESERVED_TAG_STAR)
      });
      continue;
    }
    i++;
  }
  return out;
}
function locateMemoRange(lines, memo) {
  const [s, e] = memo.range;
  const timeRe = new RegExp(`^-\\s+${memo.time}(?:\\s|$)`);
  if (s >= 0 && s < lines.length && timeRe.test(lines[s]) && e < lines.length) {
    return [s, e];
  }
  const matches = parseMemos(memo.file, lines.join("\n")).filter(
    (m) => m.date === memo.date && m.time === memo.time && m.content === memo.content
  );
  if (matches.length === 0)
    return null;
  matches.sort((a, b) => Math.abs(a.range[0] - s) - Math.abs(b.range[0] - s));
  return matches[0].range;
}
function serializeMemo(time, content) {
  const t = content.replace(/\r\n/g, "\n").split("\n");
  while (t.length && t[0].trim() === "")
    t.shift();
  while (t.length && t[t.length - 1].trim() === "")
    t.pop();
  if (t.length === 0)
    return `- ${time}`;
  const s = t.map((l) => l.trim() === "" ? "" : `  ${l}`).join("\n");
  return `- ${time}
${s}`;
}
function insertMemoIntoYear(raw, year, dateStr, weekday, timeStr, body) {
  const lines = raw.split(/\r?\n/);
  const yearHeading = `# ${year}`;
  const dateHeading = `## ${dateStr} ${weekday}`;
  const block = serializeMemo(timeStr, body);
  let yIdx = lines.findIndex((l) => l.trim() === yearHeading);
  if (yIdx < 0) {
    if (lines.length && lines[0].trim() !== "")
      lines.unshift("", yearHeading, "");
    else
      lines.unshift(yearHeading, "");
    yIdx = lines.findIndex((l) => l.trim() === yearHeading);
  }
  const dateRe = new RegExp(`^##\\s+${dateStr}(?:\\s+.+)?$`);
  const dateIdx = lines.findIndex((l) => dateRe.test(l));
  if (dateIdx >= 0) {
    let sectionEnd = lines.length;
    for (let m = dateIdx + 1; m < lines.length; m++) {
      if (/^#{1,2}\s+/.test(lines[m])) {
        sectionEnd = m;
        break;
      }
    }
    const timeRe = /^-\s+(\d{2}:\d{2})(?:\s|$)/;
    let insertAt = -1;
    for (let m = dateIdx + 1; m < sectionEnd; m++) {
      const mm = lines[m].match(timeRe);
      if (mm && mm[1] > timeStr) {
        insertAt = m;
        break;
      }
    }
    if (insertAt >= 0) {
      let m = insertAt;
      while (m > dateIdx + 1 && lines[m - 1].trim() === "")
        m--;
      lines.splice(m, 0, block, "");
      return lines.join("\n");
    }
    const D = trimTrailingBlank(lines, dateIdx + 1, sectionEnd);
    lines.splice(D, 0, "", block);
    return lines.join("\n");
  }
  const dateOnlyRe = /^##\s+(\d{4}-\d{2}-\d{2})/;
  const yearOnlyRe = /^#\s+\d{4}\s*$/;
  let nextYear = lines.length;
  for (let k = yIdx + 1; k < lines.length; k++) {
    if (yearOnlyRe.test(lines[k])) {
      nextYear = k;
      break;
    }
  }
  let insertIdx = -1;
  for (let k = yIdx + 1; k < nextYear; k++) {
    const v = lines[k].match(dateOnlyRe);
    if (v && v[1] > dateStr) {
      insertIdx = k;
      break;
    }
  }
  if (insertIdx === -1) {
    if (nextYear < lines.length) {
      let k = nextYear;
      while (k > yIdx + 1 && lines[k - 1].trim() === "")
        k--;
      const block22 = [dateHeading, "", block, ""];
      lines.splice(k, 0, "", ...block22);
      return lines.join("\n");
    }
    while (lines.length && lines[lines.length - 1].trim() === "")
      lines.pop();
    lines.push("", dateHeading, "", block, "");
    return lines.join("\n");
  }
  const block2 = ["", dateHeading, "", block, ""];
  lines.splice(insertIdx, 0, ...block2);
  return lines.join("\n");
}
function removeOrphanDateHeaders(lines) {
  const dateRe = /^##\s+\d{4}-\d{2}-\d{2}(?:\s+.+)?$/;
  const memoRe = /^- \d{2}:\d{2}/;
  const headingRe = /^#{1,2}\s+/;
  const toRemove = [];
  for (let i = 0; i < lines.length; i++) {
    if (!dateRe.test(lines[i]))
      continue;
    let has = false;
    for (let l = i + 1; l < lines.length && !headingRe.test(lines[l]); l++) {
      if (memoRe.test(lines[l])) {
        has = true;
        break;
      }
    }
    if (!has)
      toRemove.push(i);
  }
  for (let i = toRemove.length - 1; i >= 0; i--)
    lines.splice(toRemove[i], 1);
}
function trimTrailing(lines) {
  while (lines.length > 1 && lines[lines.length - 1].trim() === "")
    lines.pop();
}
function trimTrailingBlank(lines, start, end) {
  let s = start;
  for (let n = start; n < end; n++)
    if (lines[n].trim() !== "")
      s = n + 1;
  return s;
}
function trimTrashToLimit(content, limit) {
  if (!limit || limit <= 0)
    return content;
  const lines = content.split(/\r?\n/);
  const headRe = /^##\s+已删除\s+/;
  const heads = [];
  for (let i = 0; i < lines.length; i++)
    if (headRe.test(lines[i]))
      heads.push(i);
  if (heads.length <= limit)
    return content;
  const keepFrom = heads[heads.length - limit];
  const head = lines.slice(0, heads[0]);
  const body = lines.slice(keepFrom);
  while (head.length && head[head.length - 1].trim() === "")
    head.pop();
  return head.join("\n") + "\n\n" + body.join("\n");
}
var WEEKDAYS = ["\u5468\u65E5", "\u5468\u4E00", "\u5468\u4E8C", "\u5468\u4E09", "\u5468\u56DB", "\u5468\u4E94", "\u5468\u516D"];
function formatDate(d) {
  const y = d.getFullYear();
  const m = String(d.getMonth() + 1).padStart(2, "0");
  const day = String(d.getDate()).padStart(2, "0");
  return `${y}-${m}-${day}`;
}
function formatTime(d) {
  const h = String(d.getHours()).padStart(2, "0");
  const m = String(d.getMinutes()).padStart(2, "0");
  return `${h}:${m}`;
}
function makeDateTime(date, time) {
  const [Y, M, D] = date.split("-").map((n) => parseInt(n, 10));
  const [h, m] = time.split(":").map((n) => parseInt(n, 10));
  return new Date(Y, M - 1, D, h, m, 0, 0);
}
function escapeRe(s) {
  return s.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}

// src/view/MuseView.ts
var import_obsidian6 = require("obsidian");

// src/shareImage.ts
var import_obsidian4 = require("obsidian");
var WIDTH = 640;
var PAD = 48;
var CONTENT_W = WIDTH - PAD * 2;
var BODY_TOP = 110;
var FONT = "-apple-system,BlinkMacSystemFont,'Segoe UI','PingFang SC','Hiragino Sans GB','Microsoft YaHei','\u5FAE\u8F6F\u96C5\u9ED1',sans-serif";
var FEATHER = "M13.5 0.5 C11 2.5, 8 5, 5.5 8 C3.5 10.5, 2 12.5, 1.2 14 L3 14.5 L13.5 4 Z M5 6.5 L3.5 9 L5.5 9.2 Z M8 3.5 L6.5 6 L8.5 6.2 Z M10.5 1.2 L9 3.6 L11 3.8 Z M1 14.2 L0 15.5 L1 15.5 Z";
var PALETTES = {
  paper: { bg: "#fdfdfd", fg: "#1a1a1c", muted: "#8a8a8e", accent1: "#7c3aed", accent2: "#3b82f6", tagBg: "rgba(124,58,237,0.08)", tagFg: "#6d28d9", border: "#c8c8cc" },
  kraft: { bg: "#f5ebd8", fg: "#3d2f1e", muted: "#8a6f4a", accent1: "#b45309", accent2: "#d97706", tagBg: "rgba(180,83,9,0.12)", tagFg: "#92400e", border: "#c8a876" },
  mint: { bg: "#e8f5ec", fg: "#1a3a28", muted: "#5a8368", accent1: "#059669", accent2: "#10b981", tagBg: "rgba(5,150,105,0.12)", tagFg: "#047857", border: "#95c8a5" },
  peach: { bg: "#fde8e1", fg: "#3d1f18", muted: "#a77363", accent1: "#ea580c", accent2: "#f97316", tagBg: "rgba(234,88,12,0.12)", tagFg: "#c2410c", border: "#ecab93" },
  sky: { bg: "#e0f2fe", fg: "#0c2a3e", muted: "#5a7a95", accent1: "#0284c7", accent2: "#0ea5e9", tagBg: "rgba(2,132,199,0.12)", tagFg: "#0369a1", border: "#84bcd8" },
  lavender: { bg: "#eee7fa", fg: "#2a1a3e", muted: "#7a6a95", accent1: "#7c3aed", accent2: "#a78bfa", tagBg: "rgba(124,58,237,0.12)", tagFg: "#6d28d9", border: "#bba9de" },
  midnight: { bg: "#1a2238", fg: "#e8e8ea", muted: "#8a95b0", accent1: "#60a5fa", accent2: "#a78bfa", tagBg: "rgba(167,139,250,0.18)", tagFg: "#c4b5fd", border: "#3a4568" },
  charcoal: { bg: "#1a1b1e", fg: "#e8e8ea", muted: "#8a8a90", accent1: "#a78bfa", accent2: "#60a5fa", tagBg: "rgba(167,139,250,0.14)", tagFg: "#c4b5fd", border: "#3a3a40" }
};
function resolvePalette(theme, isDark) {
  let d = theme || "auto";
  if (d === "auto")
    d = isDark ? "charcoal" : "paper";
  else if (d === "random") {
    const keys = Object.keys(PALETTES);
    d = keys[Math.floor(Math.random() * keys.length)];
  }
  return PALETTES[d] || PALETTES.paper;
}
function isCJK(ch) {
  var _a;
  const c = (_a = ch.codePointAt(0)) != null ? _a : 0;
  return c >= 12288 && c <= 40959 || c >= 65280 && c <= 65519 || c >= 12352 && c <= 12543;
}
function charWidth(ch, fontSize, bold) {
  let w;
  if (isCJK(ch))
    w = fontSize;
  else if (/\s/.test(ch))
    w = fontSize * 0.3;
  else if (/[.,;:!?'"()\[\]{}<>\/\\|@#$%^&*\-+=~]/.test(ch))
    w = fontSize * 0.5;
  else
    w = fontSize * 0.56;
  return w * (bold ? 1.05 : 1);
}
function measure(str, fontSize, bold) {
  let w = 0;
  for (const ch of str)
    w += charWidth(ch, fontSize, bold);
  return w;
}
function esc(s) {
  return s.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&apos;");
}
function hexToRgba(hex, a) {
  const m = /^#?([0-9a-f]{6})$/i.exec(hex.trim());
  if (!m)
    return hex;
  const n = parseInt(m[1], 16);
  return `rgba(${n >> 16 & 255}, ${n >> 8 & 255}, ${n & 255}, ${a})`;
}
function parseInline(text) {
  const spans = [];
  const push = (t, bold = false, italic = false, code = false, link = false, strike = false) => {
    if (t)
      spans.push({ text: t, bold, italic, code, link, strike });
  };
  let i = 0;
  while (i < text.length) {
    const c = text[i];
    if (c === "`") {
      const m = text.slice(i).match(/^`([^`]+)`/);
      if (m) {
        push(m[1], false, false, true);
        i += m[0].length;
        continue;
      }
    }
    if (c === "*" && text[i + 1] === "*") {
      const m = text.slice(i).match(/^\*\*([^*]+)\*\*/);
      if (m) {
        push(m[1], true);
        i += m[0].length;
        continue;
      }
    }
    if (c === "~" && text[i + 1] === "~") {
      const m = text.slice(i).match(/^~~([^~]+)~~/);
      if (m) {
        push(m[1], false, false, false, false, true);
        i += m[0].length;
        continue;
      }
    }
    if (c === "[") {
      const link = text.slice(i).match(/^\[([^\]]*)\]\(([^)]+)\)/);
      if (link) {
        push(link[1] || link[2], false, false, false, true);
        i += link[0].length;
        continue;
      }
      const wiki = text.slice(i).match(/^\[\[([^\]]+)\]\]/);
      if (wiki) {
        push(wiki[1], false, false, false, true);
        i += wiki[0].length;
        continue;
      }
    }
    if (c === "*") {
      const m = text.slice(i).match(/^\*([^*\n]+)\*(?!\*)/);
      if (m) {
        push(m[1], false, true);
        i += m[0].length;
        continue;
      }
    }
    let j = i + 1;
    while (j < text.length) {
      const d = text[j];
      if (d === "`" || d === "~" || d === "[" || d === "*")
        break;
      j++;
    }
    push(text.slice(i, j));
    i = j;
  }
  return spans;
}
function parseBlocks(src) {
  const lines = src.split("\n");
  const blocks = [];
  let i = 0;
  while (i < lines.length) {
    const line = lines[i];
    if (/^\s*$/.test(line)) {
      i++;
      continue;
    }
    if (/^\s*```/.test(line)) {
      const buf2 = [];
      i++;
      while (i < lines.length && !/^\s*```/.test(lines[i])) {
        buf2.push(lines[i]);
        i++;
      }
      i++;
      blocks.push({ kind: "code", text: buf2.join("\n") });
      continue;
    }
    const h = line.match(/^(#{1,6})\s+(.*)$/);
    if (h) {
      blocks.push({ kind: "h", level: h[1].length, text: h[2] });
      i++;
      continue;
    }
    const q = line.match(/^>\s?(.*)$/);
    if (q) {
      const buf2 = [];
      while (i < lines.length && /^>\s?/.test(lines[i])) {
        buf2.push(lines[i].replace(/^>\s?/, ""));
        i++;
      }
      blocks.push({ kind: "quote", text: buf2.join("\n") });
      continue;
    }
    const task = line.match(/^(\s*)[-*]\s+\[([ xX])\]\s+(.*)$/);
    if (task) {
      blocks.push({
        kind: "task",
        indent: task[1].length,
        checked: task[2].toLowerCase() === "x",
        text: task[3]
      });
      i++;
      continue;
    }
    const ul = line.match(/^(\s*)[-*+]\s+(.*)$/);
    if (ul) {
      blocks.push({ kind: "ul", indent: ul[1].length, text: ul[2] });
      i++;
      continue;
    }
    const ol = line.match(/^(\s*)\d+\.\s+(.*)$/);
    if (ol) {
      const m = line.match(/^(\s*)(\d+)\.\s+(.*)$/);
      blocks.push({
        kind: "ol",
        indent: m[1].length,
        num: parseInt(m[2], 10),
        text: m[3]
      });
      i++;
      continue;
    }
    if (/^\s*(---|\*\*\*|___)\s*$/.test(line)) {
      blocks.push({ kind: "hr" });
      i++;
      continue;
    }
    const buf = [];
    while (i < lines.length && !/^\s*$/.test(lines[i]) && !/^\s*```/.test(lines[i]) && !/^(#{1,6})\s+/.test(lines[i]) && !/^>\s?/.test(lines[i]) && !/^(\s*)[-*+]\s+/.test(lines[i]) && !/^(\s*)\d+\.\s+/.test(lines[i]) && !/^\s*(---|\*\*\*|___)\s*$/.test(lines[i])) {
      buf.push(lines[i]);
      i++;
    }
    blocks.push({ kind: "p", text: buf.join(" ") });
  }
  return blocks;
}
function wrapSpans(spans, fontSize, maxWidth, x0) {
  const lines = [];
  let cur = [];
  let x = x0;
  for (const sp of spans) {
    let rem = sp.text;
    while (rem.length) {
      let len = 0;
      let w = 0;
      while (len < rem.length) {
        const cw = charWidth(rem[len], fontSize, !!sp.bold);
        if (x + w + cw > x0 + maxWidth && len > 0)
          break;
        w += cw;
        len++;
      }
      const piece = rem.slice(0, len);
      cur.push({
        text: piece,
        x,
        bold: !!sp.bold,
        italic: !!sp.italic,
        code: !!sp.code,
        link: !!sp.link,
        strike: !!sp.strike
      });
      x += w;
      rem = rem.slice(len);
      if (rem.length) {
        lines.push(cur);
        cur = [];
        x = x0;
      }
    }
  }
  if (cur.length)
    lines.push(cur);
  else if (lines.length === 0)
    lines.push([]);
  return lines;
}
function headingSize(level) {
  var _a;
  return (_a = [0, 28, 24, 20, 17, 16, 15][level]) != null ? _a : 15;
}
function layoutBody(body, pal) {
  const blocks = parseBlocks(body);
  let y = BODY_TOP;
  const deco = [];
  const text = [];
  const emitLine = (line, ly, fs, blockBold) => {
    const tspans = line.map((s) => {
      const attrs = [];
      if (s.bold || blockBold)
        attrs.push('font-weight="700"');
      if (s.italic)
        attrs.push('font-style="italic"');
      if (s.code)
        attrs.push('font-family="monospace"');
      if (s.link) {
        attrs.push(`fill="${pal.accent1}"`);
        attrs.push('text-decoration="underline"');
      }
      if (s.strike) {
        attrs.push('text-decoration="line-through"');
        attrs.push(`fill="${pal.muted}"`);
      }
      return `<tspan x="${s.x}" y="${ly}" ${attrs.join(" ")}>${esc(s.text)}</tspan>`;
    }).join("");
    text.push(`<text font-size="${fs}" fill="${pal.fg}">${tspans}</text>`);
  };
  for (const b of blocks) {
    if (b.kind === "hr") {
      const yy = y + 8;
      deco.push(
        `<line x1="${PAD}" y1="${yy}" x2="${WIDTH - PAD}" y2="${yy}" stroke="${pal.border}" stroke-width="1.5" stroke-dasharray="4 4"/>`
      );
      y += 28;
      continue;
    }
    if (b.kind === "code") {
      const fs2 = 14;
      const lh2 = fs2 * 1.5;
      const innerW = CONTENT_W - 24;
      const lines2 = [];
      for (const raw of b.text.split("\n")) {
        for (const ln of wrapSpans(parseInline(raw), fs2, innerW, PAD + 12))
          lines2.push(ln);
      }
      const padV = 10;
      const boxTop = y - fs2 * 0.8;
      const boxH = lines2.length * lh2 + padV * 2;
      deco.push(
        `<rect x="${PAD}" y="${boxTop}" width="${CONTENT_W}" height="${boxH}" rx="8" fill="${hexToRgba(pal.fg, 0.05)}" stroke="${pal.border}" stroke-width="1"/>`
      );
      let ly2 = y;
      for (const line of lines2) {
        const tspans = line.map(
          (s) => `<tspan x="${s.x}" y="${ly2}"${s.bold ? ' font-weight="700"' : ""}>${esc(s.text)}</tspan>`
        ).join("");
        text.push(`<text font-size="${fs2}" fill="${pal.fg}" font-family="monospace">${tspans}</text>`);
        ly2 += lh2;
      }
      y = ly2 + 12;
      continue;
    }
    const isHeading = b.kind === "h";
    const fs = isHeading ? headingSize(b.level) : 16;
    const lh = fs * 1.6;
    const blockBold = isHeading;
    let spans = parseInline(b.text || "");
    let x0 = PAD;
    let maxW = CONTENT_W;
    let prefixSvg = "";
    if (b.kind === "quote") {
      x0 = PAD + 16;
      maxW = CONTENT_W - 16;
      const barTop = y - fs * 0.8;
      deco.push(
        `<rect x="${PAD}" y="${barTop}" width="3" height="${lh}" rx="1.5" fill="${pal.accent1}" opacity="0.5"/>`
      );
    } else if (b.kind === "ul") {
      x0 = PAD + 22;
      maxW = CONTENT_W - 22;
      prefixSvg = `<text x="${PAD + 4}" y="${y}" font-size="${fs}" fill="${pal.accent1}">\u2022</text>`;
    } else if (b.kind === "ol") {
      x0 = PAD + 30;
      maxW = CONTENT_W - 30;
      prefixSvg = `<text x="${PAD + 4}" y="${y}" font-size="${fs}" fill="${pal.accent1}" font-weight="600">${b.num}.</text>`;
    } else if (b.kind === "task") {
      x0 = PAD + 26;
      maxW = CONTENT_W - 26;
      const tb = b;
      const boxTop = y - fs * 0.8;
      if (tb.checked) {
        deco.push(
          `<rect x="${PAD + 2}" y="${boxTop}" width="14" height="14" rx="3" fill="${pal.accent1}"/>`
        );
        deco.push(
          `<polyline points="${PAD + 6},${boxTop + 7} ${PAD + 8.5},${boxTop + 9.5} ${PAD + 13},${boxTop + 4}" fill="none" stroke="${pal.bg}" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>`
        );
        spans = spans.map((s) => ({ ...s, strike: true }));
      } else {
        deco.push(
          `<rect x="${PAD + 2}" y="${boxTop}" width="14" height="14" rx="3" fill="transparent" stroke="${pal.muted}" stroke-width="1.5"/>`
        );
      }
    }
    const lines = wrapSpans(spans, fs, maxW, x0);
    let ly = y;
    let first = true;
    for (const line of lines) {
      if (first && prefixSvg)
        text.push(prefixSvg);
      emitLine(line, ly, fs, blockBold);
      ly += lh;
      first = false;
    }
    y = ly + (isHeading ? 10 : 6);
  }
  return { svg: deco.concat(text).join("\n  "), height: Math.max(0, y - BODY_TOP) };
}
function layoutTags(tags, pal) {
  if (!tags.length)
    return { svg: "", height: 0 };
  const fs = 14;
  const padX = 10;
  const h = 24;
  const gap = 8;
  const items = [];
  let x = PAD;
  let rowY = 0;
  let bottom = 0;
  for (const t of tags) {
    const label = "#" + t;
    const tw = measure(label, fs, false) + padX * 2;
    if (x + tw > WIDTH - PAD && x > PAD) {
      x = PAD;
      rowY += h + gap;
    }
    items.push(
      `<rect x="${x}" y="${rowY}" width="${tw}" height="${h}" rx="12" fill="${pal.tagBg}"/>`
    );
    items.push(
      `<text x="${x + tw / 2}" y="${rowY + h / 2 + fs * 0.35}" font-size="${fs}" fill="${pal.tagFg}" text-anchor="middle">${esc(label)}</text>`
    );
    x += tw + gap;
    bottom = Math.max(bottom, rowY + h);
  }
  return { svg: items.join("\n  "), height: bottom > 0 ? bottom + gap : 0 };
}
function buildSVG(memo, pal, bodySvg, bodyH, tagsSvg, tagsH) {
  const afterBody = BODY_TOP + bodyH;
  const tagsTop = tagsH > 0 ? afterBody + 18 : afterBody;
  const tagsBottom = tagsH > 0 ? tagsTop + tagsH : afterBody;
  const dividerY = tagsBottom + 22;
  const dateY = dividerY + 30;
  const timeY = dateY + 20;
  const TOTAL = timeY + 36;
  const ds = memo.date.replace(/-/g, ".");
  return `<svg xmlns="http://www.w3.org/2000/svg" width="${WIDTH}" height="${TOTAL}" viewBox="0 0 ${WIDTH} ${TOTAL}" font-family="${FONT}">
  <defs>
    <linearGradient id="topBar" x1="0" y1="0" x2="1" y2="0">
      <stop offset="0%" stop-color="${pal.accent1}"/>
      <stop offset="100%" stop-color="${pal.accent2}"/>
    </linearGradient>
  </defs>

  <!-- \u80CC\u666F -->
  <rect width="100%" height="100%" fill="${pal.bg}"/>

  <!-- \u9876\u90E8\u6E10\u53D8\u88C5\u9970\u6761 -->
  <rect x="${PAD}" y="0" width="${CONTENT_W}" height="6" fill="url(#topBar)"/>

  <!-- \u88C5\u9970\u5F15\u53F7 -->
  <text x="${PAD}" y="72" font-size="54" font-family="Georgia, 'Times New Roman', serif" fill="${pal.accent1}" opacity="0.22" font-weight="700">&#8220;</text>

  <!-- \u6B63\u6587 -->
  ${bodySvg}

  ${tagsH > 0 ? `<!-- \u6807\u7B7E\u80F6\u56CA -->
  <g transform="translate(0, ${tagsTop})">
  ${tagsSvg}
  </g>` : ""}

  <!-- \u865A\u7EBF\u5206\u5272 -->
  <line x1="${PAD}" y1="${dividerY}" x2="${WIDTH - PAD}" y2="${dividerY}" stroke="${pal.border}" stroke-width="1.6" stroke-linecap="round" stroke-dasharray="5 4"/>

  <!-- \u65E5\u671F / \u65F6\u95F4 -->
  <text x="${PAD}" y="${dateY}" font-size="18" fill="${pal.fg}" font-weight="600" letter-spacing="1">${esc(ds)}</text>
  <text x="${PAD}" y="${timeY}" font-size="13" fill="${pal.muted}">${esc(memo.time)}</text>

  <!-- \u53F3\u4E0B\u89D2\uFF1A\u7FBD\u6BDB\u7B14 + Muse View \u6C34\u5370 -->
  <g transform="translate(${WIDTH - PAD - 110}, ${dateY - 15}) scale(1.3)" fill="${pal.muted}" opacity="0.85">
    <path d="${FEATHER}"/>
  </g>
  <text x="${WIDTH - PAD}" y="${dateY}" font-size="12" fill="${pal.muted}" text-anchor="end" letter-spacing="1.5" font-weight="600">Muse View</text>
</svg>`;
}
function svgToPngBlob(svg, w, h, scale) {
  return new Promise((resolve, reject) => {
    const blob = new Blob([svg], { type: "image/svg+xml;charset=utf-8" });
    const url = URL.createObjectURL(blob);
    const img = new Image();
    img.width = w;
    img.height = h;
    img.onload = () => {
      try {
        const canvas = document.createElement("canvas");
        canvas.width = w * scale;
        canvas.height = h * scale;
        const ctx = canvas.getContext("2d");
        if (!ctx) {
          URL.revokeObjectURL(url);
          reject(new Error("canvas \u4E0D\u53EF\u7528"));
          return;
        }
        ctx.scale(scale, scale);
        ctx.drawImage(img, 0, 0, w, h);
        canvas.toBlob((out) => {
          URL.revokeObjectURL(url);
          if (out)
            resolve(out);
          else
            reject(new Error("canvas.toBlob \u8FD4\u56DE null\uFF08\u901A\u5E38\u662F tainted canvas\uFF09"));
        }, "image/png");
      } catch (e) {
        URL.revokeObjectURL(url);
        reject(e);
      }
    };
    img.onerror = () => {
      URL.revokeObjectURL(url);
      reject(new Error("SVG \u6E32\u67D3\u5931\u8D25"));
    };
    img.src = url;
  });
}
function downloadBlob(blob, filename) {
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url;
  a.download = filename;
  document.body.appendChild(a);
  a.click();
  a.remove();
  setTimeout(() => URL.revokeObjectURL(url), 1e3);
}
function buildShareSvg(plugin, memo) {
  const isDark = document.body.classList.contains("theme-dark") || document.documentElement.classList.contains("theme-dark");
  const pal = resolvePalette(plugin.settings.exportTheme, isDark);
  const { text: stripped, tags } = stripInlineTags(memo.content);
  const body = stripped.replace(/!\[\[[^\]]+\]\]/g, "").replace(/!\[[^\]]*\]\([^)]+\)/g, "").trim();
  const visibleTags = tags.filter((t) => !SYSTEM_TAGS.includes(t));
  const bodyLayout = layoutBody(body, pal);
  const tagLayout = layoutTags(visibleTags, pal);
  const svg = buildSVG(
    memo,
    pal,
    bodyLayout.svg,
    bodyLayout.height,
    tagLayout.svg,
    tagLayout.height
  );
  const filename = `muse-${memo.date}-${memo.time.replace(/:/g, "")}`;
  return { svg, width: WIDTH, height: measureTotal(svg), filename };
}
async function copyPngToClipboard(blob) {
  const Ctor = window.ClipboardItem;
  if (!navigator.clipboard || !Ctor) {
    throw new Error("\u5F53\u524D\u73AF\u5883\u4E0D\u652F\u6301\u590D\u5236\u56FE\u7247\u5230\u526A\u8D34\u677F");
  }
  await navigator.clipboard.write([new Ctor({ "image/png": blob })]);
}
var ShareImageModal = class extends import_obsidian4.Modal {
  constructor(plugin, svg, width, height, filename) {
    super(plugin.app);
    this.busy = false;
    this.plugin = plugin;
    this.svg = svg;
    this.width = width;
    this.height = height;
    this.filename = filename;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.addClass("muse-share-modal");
    this.modalEl.addClass("muse-share-modal-wrap");
    this.titleEl.setText(this.plugin.t("modal.shareTitle"));
    const preview = contentEl.createDiv({ cls: "muse-share-preview" });
    preview.innerHTML = this.svg;
    const bar = contentEl.createDiv({ cls: "muse-share-bar" });
    const copyBtn = bar.createEl("button", {
      cls: "mod-cta muse-share-btn",
      text: this.plugin.t("card.copyImage")
    });
    copyBtn.addEventListener("click", () => void this.copy());
    const exportBtn = bar.createEl("button", {
      cls: "muse-share-btn",
      text: this.plugin.t("card.exportPng")
    });
    exportBtn.addEventListener("click", () => void this.exportPng());
  }
  async toPng() {
    return svgToPngBlob(this.svg, this.width, this.height, 2);
  }
  async copy() {
    if (this.busy)
      return;
    this.busy = true;
    try {
      const blob = await this.toPng();
      await copyPngToClipboard(blob);
      new import_obsidian4.Notice(this.plugin.t("notice.copiedImage"));
    } catch (e) {
      console.error("[MuseView] \u590D\u5236\u56FE\u7247\u5931\u8D25\uFF1A", e);
      new import_obsidian4.Notice(this.plugin.t("notice.copyFailed", { msg: e.message }));
    } finally {
      this.busy = false;
    }
  }
  async exportPng() {
    if (this.busy)
      return;
    this.busy = true;
    try {
      const blob = await this.toPng();
      downloadBlob(blob, `${this.filename}.png`);
      new import_obsidian4.Notice(this.plugin.t("notice.exportDone", { name: this.filename }));
    } catch (e) {
      console.warn("[MuseView] PNG \u5BFC\u51FA\u5931\u8D25\uFF0C\u964D\u7EA7\u4E3A SVG\uFF1A", e);
      const svgBlob = new Blob([this.svg], {
        type: "image/svg+xml;charset=utf-8"
      });
      downloadBlob(svgBlob, `${this.filename}.svg`);
      new import_obsidian4.Notice(this.plugin.t("notice.exportFailed", { msg: e.message }));
    } finally {
      this.busy = false;
    }
  }
  onClose() {
    this.contentEl.empty();
  }
};
async function openShareImage(plugin, memo) {
  try {
    const { svg, width, height, filename } = buildShareSvg(plugin, memo);
    new ShareImageModal(plugin, svg, width, height, filename).open();
  } catch (e) {
    console.error("[MuseView] \u751F\u6210\u5206\u4EAB\u56FE\u5931\u8D25\uFF1A", e);
    new import_obsidian4.Notice(plugin.t("notice.exportFailed", { msg: e.message }));
  }
}
function measureTotal(svg) {
  const m = /height="(\d+(?:\.\d+)?)"/.exec(svg);
  return m ? parseFloat(m[1]) : 400;
}

// src/editor/MuseEditor.ts
var import_obsidian5 = require("obsidian");
var HEADING_CLASSES = [
  "lp-h1",
  "lp-h2",
  "lp-h3",
  "lp-h4",
  "lp-h5",
  "lp-h6"
];
function escapeHtml(s) {
  return s.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
}
var TextareaEditor = class {
  constructor(el) {
    this.el = el;
    el.addEventListener("input", () => {
      var _a;
      return (_a = this.changeCb) == null ? void 0 : _a.call(this);
    });
    el.addEventListener("keydown", (e) => {
      var _a;
      return (_a = this.keyCb) == null ? void 0 : _a.call(this, e);
    });
    el.addEventListener("focus", () => {
      var _a;
      return (_a = this.focusCb) == null ? void 0 : _a.call(this);
    });
    el.addEventListener("blur", () => {
      var _a;
      return (_a = this.blurCb) == null ? void 0 : _a.call(this);
    });
  }
  getValue() {
    return this.el.value;
  }
  setValue(v) {
    this.el.value = v;
  }
  focus() {
    this.el.focus();
  }
  isEmpty() {
    return this.el.value.length === 0;
  }
  setPlaceholder(t) {
    this.el.setAttr("placeholder", t);
  }
  getActiveText() {
    return this.el.value;
  }
  getCaret() {
    var _a;
    return (_a = this.el.selectionStart) != null ? _a : this.el.value.length;
  }
  getCaretEnd() {
    var _a;
    return (_a = this.el.selectionEnd) != null ? _a : this.el.value.length;
  }
  setActiveText(t, caret) {
    this.el.value = t;
    if (caret != null)
      this.el.setSelectionRange(caret, caret);
  }
  replaceRange(start, end, text) {
    this.el.value = this.el.value.slice(0, start) + text + this.el.value.slice(end);
    const pos = start + text.length;
    this.el.setSelectionRange(pos, pos);
  }
  autoResize() {
    const el = this.el;
    el.classList.add("muse-no-transition");
    if (el.value.length === 0) {
      el.style.height = "";
      requestAnimationFrame(
        () => el.classList.remove("muse-no-transition")
      );
      return;
    }
    el.style.height = "auto";
    const h = el.scrollHeight + 2;
    const max = import_obsidian5.Platform.isMobile ? 56 : 96;
    el.style.height = h <= max ? "" : `${h}px`;
    requestAnimationFrame(
      () => el.classList.remove("muse-no-transition")
    );
  }
  setOnChange(cb) {
    this.changeCb = cb;
  }
  setOnKeydown(cb) {
    this.keyCb = cb;
  }
  setOnFocus(cb) {
    this.focusCb = cb;
  }
  setOnBlur(cb) {
    this.blurCb = cb;
  }
  destroy() {
  }
};
var LivePreviewEditor = class {
  constructor(parent, app, sourcePath = "") {
    this.source = [""];
    this.cursorRow = 0;
    this.component = new import_obsidian5.Component();
    this.composing = false;
    this.placeholder = "";
    this.lastCol = 0;
    this.navArrow = false;
    this.renderScheduled = false;
    /** 全选模式：Ctrl/Cmd+A 时置 true，所有行都以「源码 + 样式」渲染（类似 Obsidian
     * Live Preview 全选时整体切到源码视图），便于整体查看/复制原始 markdown。 */
    this.allSource = false;
    this.suggest = null;
    /** vault 内全部 Markdown 笔记的 basename（已去重），搜索时复用。 */
    this.cachedFiles = [];
    this.onWinScroll = () => this.hideSuggest();
    this.onSelectionChange = () => {
      var _a, _b;
      if (this.composing)
        return;
      const sel = window.getSelection();
      if (sel && !sel.isCollapsed)
        return;
      const nav = this.navArrow;
      this.navArrow = false;
      const newRow = this.getCurrentCaretRow();
      if (this.allSource) {
        this.allSource = false;
        this.cursorRow = newRow;
        this.renderLines();
        this.lastCol = this.getCaretCol();
        return;
      }
      if (newRow === this.cursorRow) {
        this.lastCol = this.getCaretCol();
        return;
      }
      if (nav) {
        this.cursorRow = newRow;
        this.renderLines();
        this.setCaretCol(this.lastCol);
        this.lastCol = this.getCaretCol();
        return;
      }
      const oldRowEl = this.lineEls()[newRow];
      const visOff = oldRowEl ? this.getCaretColInRow(oldRowEl) : 0;
      const visLen = oldRowEl ? ((_a = oldRowEl.textContent) != null ? _a : "").length : 0;
      const srcLen = ((_b = this.source[newRow]) != null ? _b : "").length;
      const srcOff = Math.max(0, Math.min(visOff + (srcLen - visLen), srcLen));
      this.cursorRow = newRow;
      this.renderLines();
      this.setCaretCol(srcOff);
      this.lastCol = srcOff;
    };
    /**
     * 拦截复制：原生 copy 读的是「渲染后 DOM 文本」，非光标行 markdown 语法已被吞
     *（如 `## 标题` 渲染成 `<h2>标题</h2>`，复制出来丢掉 `## `）。
     * 这里按选区重建「源码」文本——整行被选中的行直接用 `dataset.src`/受控 source，
     * 局部选中的活跃行按源码切片（渲染文本==源码），局部选中的渲染行退回渲染文本切片。
     */
    this.onCopy = (e) => {
      var _a;
      const sel = window.getSelection();
      if (!sel || sel.rangeCount === 0 || sel.isCollapsed)
        return;
      const text = this.getSelectionSource();
      if (text == null)
        return;
      e.preventDefault();
      (_a = e.clipboardData) == null ? void 0 : _a.setData("text/plain", text);
    };
    this.app = app;
    this.sourcePath = sourcePath;
    this.el = parent.createDiv({ cls: "muse-lp muse-input" });
    this.el.setAttribute("contenteditable", "true");
    this.el.setAttribute("role", "textbox");
    this.el.setAttribute("aria-multiline", "true");
    this.component.load();
    this.el.addEventListener("input", () => this.onInput());
    this.el.addEventListener("compositionstart", () => {
      this.composing = true;
    });
    this.el.addEventListener("compositionend", () => {
      var _a;
      this.composing = false;
      this.syncFromDom();
      this.scheduleRender();
      (_a = this.changeCb) == null ? void 0 : _a.call(this);
    });
    this.el.addEventListener("keydown", (e) => this.onKeydown(e));
    this.el.addEventListener("paste", (e) => this.onPaste(e));
    this.el.addEventListener("copy", this.onCopy);
    this.el.addEventListener("focus", () => {
      var _a;
      return (_a = this.focusCb) == null ? void 0 : _a.call(this);
    });
    this.el.addEventListener("blur", () => {
      var _a;
      this.hideSuggest();
      (_a = this.blurCb) == null ? void 0 : _a.call(this);
    });
    this.el.addEventListener("scroll", () => this.hideSuggest());
    document.addEventListener("selectionchange", this.onSelectionChange);
    window.addEventListener("scroll", this.onWinScroll, true);
    this.ensureFirstLine();
    this.buildSuggestEl();
    this.updateEmpty();
  }
  /* ----------------------------- 接口实现 ----------------------------- */
  getValue() {
    const rows = this.lineEls();
    const parts = [];
    rows.forEach((row, i) => {
      var _a, _b, _c, _d, _e;
      if (row.dataset.codeHidden)
        return;
      if (row.classList.contains("is-code-block")) {
        parts.push((_a = row.dataset.src) != null ? _a : "");
        return;
      }
      if (row.classList.contains("is-rendered")) {
        const ds = row.dataset.src;
        if (!ds) {
          const dom = ((_b = row.textContent) != null ? _b : "").trim();
          if (dom) {
            parts.push(dom);
            return;
          }
        }
        parts.push((_c = ds != null ? ds : this.source[i]) != null ? _c : "");
      } else {
        const dom = (_d = row.textContent) != null ? _d : "";
        if (dom.trim())
          parts.push(dom);
        else
          parts.push((_e = this.source[i]) != null ? _e : "");
      }
    });
    return parts.join("\n");
  }
  setValue(v) {
    this.component.unload();
    this.component = new import_obsidian5.Component();
    this.component.load();
    this.allSource = false;
    this.composing = false;
    this.el.empty();
    this.source = v.length ? v.split("\n") : [""];
    this.source.forEach((s) => {
      const d = this.el.createDiv({ cls: "muse-lp-line" });
      d.textContent = s;
    });
    this.cursorRow = this.source.length - 1;
    this.updateEmpty();
    this.scheduleRender();
    requestAnimationFrame(
      () => {
        var _a;
        return this.setCaretCol(((_a = this.source[this.cursorRow]) != null ? _a : "").length);
      }
    );
  }
  focus() {
    this.el.focus();
  }
  isEmpty() {
    return this.getValue().trim().length === 0;
  }
  setPlaceholder(t) {
    this.placeholder = t;
    this.el.setAttr("data-placeholder", t);
    this.updateEmpty();
  }
  getActiveText() {
    var _a;
    return (_a = this.source[this.cursorRow]) != null ? _a : "";
  }
  getCaret() {
    return this.getCaretCol();
  }
  getCaretEnd() {
    return this.getCaretCol();
  }
  setActiveText(t, caret) {
    this.source[this.cursorRow] = t;
    const row = this.lineEls()[this.cursorRow];
    if (row)
      row.textContent = t;
    if (caret != null)
      this.setCaretCol(caret);
    else
      this.setCaretCol(t.length);
    this.updateEmpty();
    this.scheduleRender();
  }
  replaceRange(start, end, text) {
    var _a;
    const row = this.cursorRow;
    const cur = (_a = this.source[row]) != null ? _a : "";
    const next = cur.slice(0, start) + text + cur.slice(end);
    this.source[row] = next;
    const lineEl = this.lineEls()[row];
    if (lineEl)
      lineEl.textContent = next;
    const pos = start + text.length;
    this.setCaretCol(pos);
    this.scheduleRender();
  }
  autoResize() {
  }
  setOnChange(cb) {
    this.changeCb = cb;
  }
  setOnKeydown(cb) {
    this.keyCb = cb;
  }
  setOnFocus(cb) {
    this.focusCb = cb;
  }
  setOnBlur(cb) {
    this.blurCb = cb;
  }
  destroy() {
    var _a;
    document.removeEventListener("selectionchange", this.onSelectionChange);
    window.removeEventListener("scroll", this.onWinScroll, true);
    (_a = this.suggestEl) == null ? void 0 : _a.remove();
    this.el.removeEventListener("copy", this.onCopy);
    this.component.unload();
  }
  /* ----------------------------- 内部逻辑 ----------------------------- */
  lineEls() {
    const out = [];
    for (const child of Array.from(this.el.children)) {
      if (child instanceof HTMLElement && child.classList.contains("muse-lp-line")) {
        out.push(child);
      } else if (child instanceof HTMLElement) {
        child.addClass("muse-lp-line");
        out.push(child);
      }
    }
    return out;
  }
  ensureFirstLine() {
    var _a, _b, _c;
    const rows = this.lineEls();
    const freeTexts = [];
    for (const node of Array.from(this.el.childNodes)) {
      if (node.nodeType === Node.TEXT_NODE && ((_a = node.textContent) != null ? _a : "").length > 0) {
        freeTexts.push((_b = node.textContent) != null ? _b : "");
      }
    }
    if (rows.length === 0 || freeTexts.length > 0) {
      const texts = rows.map(
        (r) => {
          var _a2;
          return ((_a2 = r.textContent) != null ? _a2 : "").replace(/\n$/, "");
        }
      );
      freeTexts.forEach((t) => texts.push(t));
      const merged = texts.length ? texts : [""];
      this.el.empty();
      merged.forEach((s) => {
        const d = this.el.createDiv({ cls: "muse-lp-line" });
        d.textContent = s;
      });
      this.cursorRow = Math.max(0, merged.length - 1);
      this.setCaretCol(((_c = merged[this.cursorRow]) != null ? _c : "").length);
    }
  }
  /** 以 this.source 为唯一真值，重建所有行的 DOM（用于 setValue / 多行粘贴后）。 */
  rebuildDom() {
    this.el.empty();
    if (this.source.length === 0)
      this.source = [""];
    this.source.forEach((s) => {
      const d = this.el.createDiv({ cls: "muse-lp-line" });
      d.textContent = s;
    });
  }
  /**
   * 仅从「光标行」回读源码到 this.source。
   * 已渲染（非光标）行的 DOM 里 markdown 语法已被消费（如 `# 标题` 渲染后
   * textContent 变成 `标题`），绝不能从它们反读，否则 source 会丢失语法、
   * 表现为"前面的样式行输入后消失"。光标行永远是纯文本节点，可安全回读。
   * 用受控的 this.cursorRow（由所有操作维护）而非实时选区，避免失焦瞬间读错。
   */
  /**
   * 从 DOM 完整重建 source 数组：行数严格镜像当前 DOM，每行取「源码真值」。
   * - 光标行 / 活跃态行（is-active，含全选源码模式 allSource 下的所有行）：
   *   textContent 即为源码（renderActiveLine 渲染标题/行内时保留 # ** 等标记），直接读。
   * - 非光标且已渲染行（is-rendered）：textContent 已被 MarkdownRenderer 消费
   *   （# 标题 → 标题），必须用渲染时存的 dataset.src。
   * 旧实现只同步 cursorRow 一行，块操作（全选删除 / 跨行删 / 跨行粘贴）后 source 与 DOM
   * 错位，残留旧行被 renderActiveLine 回填或 onPaste 的 splice 漏改 →
   * 「全选删不掉 / 删除后粘贴残留旧内容」。
   */
  syncFromDom() {
    this.ensureFirstLine();
    const rows = this.lineEls();
    const liveRow = this.getCurrentCaretRow();
    const live = liveRow >= 0 ? liveRow : Math.max(0, Math.min(this.cursorRow, rows.length - 1));
    if (rows[live]) {
      rows[live].classList.remove("is-rendered");
      rows[live].classList.add("is-active");
      delete rows[live].dataset.src;
    }
    this.source = [];
    rows.forEach((row, i) => {
      var _a, _b, _c;
      if (row.dataset.codeHidden)
        return;
      if (row.classList.contains("is-code-block") || row.dataset.codeBlock === "start") {
        const joined = (_a = row.dataset.src) != null ? _a : "";
        joined.split("\n").forEach((l) => this.source.push(l));
        return;
      }
      if (i === live || row.classList.contains("is-active")) {
        this.source.push(((_b = row.textContent) != null ? _b : "").replace(/\n$/, ""));
      } else {
        this.source.push((_c = row.dataset.src) != null ? _c : "");
      }
    });
    this.cursorRow = live;
    if (!rows.some((r) => r.classList.contains("is-code-block")) && this.source.length > 1 && this.source.every((s) => !s.trim())) {
      this.source = [""];
      for (let k = rows.length - 1; k >= 1; k--)
        rows[k].remove();
      const first = rows[0];
      if (first) {
        first.classList.remove("is-rendered");
        first.classList.add("is-active");
        delete first.dataset.src;
        delete first.dataset.activeSrc;
        first.textContent = "";
      }
      this.cursorRow = 0;
      this.setCaretCol(0);
    }
    this.updateEmpty();
  }
  getCurrentCaretRow() {
    const sel = window.getSelection();
    if (!sel || sel.rangeCount === 0)
      return this.cursorRow;
    let node = sel.anchorNode;
    while (node && node !== this.el) {
      if (node instanceof HTMLElement && node.classList.contains("muse-lp-line")) {
        const idx = this.lineEls().indexOf(node);
        if (idx >= 0)
          return idx;
      }
      node = node.parentNode;
    }
    return this.cursorRow;
  }
  getCaretCol() {
    const row = this.lineEls()[this.cursorRow];
    if (!row)
      return 0;
    const sel = window.getSelection();
    if (!sel || sel.rangeCount === 0)
      return 0;
    const range = sel.getRangeAt(0);
    if (!row.contains(range.endContainer))
      return 0;
    const pre = range.cloneRange();
    pre.selectNodeContents(row);
    pre.setEnd(range.endContainer, range.endOffset);
    return pre.toString().length;
  }
  /** 读取光标（折叠选区）在「指定行」textContent 中的字符偏移（点击跨行时用）。 */
  getCaretColInRow(row) {
    const sel = window.getSelection();
    if (!sel || sel.rangeCount === 0)
      return 0;
    const range = sel.getRangeAt(0);
    if (!row.contains(range.endContainer))
      return 0;
    const pre = range.cloneRange();
    pre.selectNodeContents(row);
    pre.setEnd(range.endContainer, range.endOffset);
    return pre.toString().length;
  }
  /** 把光标放到光标行内「第 col 个字符」处。兼容行内样式 span（走 TreeWalker）。 */
  setCaretCol(col) {
    var _a, _b, _c, _d, _e;
    const row = this.lineEls()[this.cursorRow];
    if (!row)
      return;
    const total = ((_a = row.textContent) != null ? _a : "").length;
    const c = Math.max(0, Math.min(col, total));
    const sel = window.getSelection();
    if (!sel)
      return;
    const walker = document.createTreeWalker(
      row,
      NodeFilter.SHOW_TEXT,
      null
    );
    let remaining = c;
    let node;
    let lastText = null;
    let placed = false;
    while (node = walker.nextNode()) {
      const t = node;
      const len = (_c = (_b = t.textContent) == null ? void 0 : _b.length) != null ? _c : 0;
      if (remaining <= len) {
        const range = document.createRange();
        range.setStart(t, remaining);
        range.collapse(true);
        sel.removeAllRanges();
        sel.addRange(range);
        placed = true;
        break;
      }
      remaining -= len;
      lastText = t;
    }
    if (!placed) {
      const range = document.createRange();
      if (lastText)
        range.setStart(lastText, (_e = (_d = lastText.textContent) == null ? void 0 : _d.length) != null ? _e : 0);
      else {
        range.selectNodeContents(row);
        range.collapse(false);
      }
      range.collapse(true);
      sel.removeAllRanges();
      sel.addRange(range);
    }
    this.el.focus();
  }
  updateEmpty() {
    const empty = this.getValue().trim().length === 0;
    this.el.toggleClass("is-empty", empty);
  }
  /** 统一渲染：光标行做「源码 + 样式」，其余行做完整 markdown 渲染。
   *  围栏代码块（``` / ~~~）跨多行，逐行渲染会把上下两个围栏各自变成带复制按钮的
   *  空代码块。因此检测代码块区间：光标在块外时整段渲染为单个代码块（内侧行隐藏）；
   *  光标在块内时整段以源码 / 字面文本显示，便于编辑且不出现复制按钮。 */
  renderLines() {
    var _a, _b, _c, _d;
    const rows = this.lineEls();
    const cur = Math.max(0, Math.min(this.cursorRow, rows.length - 1));
    const spans = this.getFenceSpans(this.source);
    const spanOf = this.source.map(() => -1);
    spans.forEach((sp) => {
      for (let i2 = sp.start; i2 <= sp.end; i2++)
        spanOf[i2] = spans.indexOf(sp);
    });
    const cursorInSpan = spanOf[cur] >= 0;
    const prevScroll = this.el.scrollTop;
    const forceFull = spans.length > 0;
    let needRender = false;
    if (!forceFull) {
      for (let i2 = 0; i2 < rows.length; i2++) {
        const row = rows[i2];
        const src = (_a = this.source[i2]) != null ? _a : "";
        if (this.allSource && row.classList.contains("is-rendered")) {
          needRender = true;
          break;
        }
        if (i2 === cur) {
          if (row.classList.contains("is-rendered"))
            needRender = true;
          else if (row.dataset.activeSrc !== src)
            needRender = true;
        } else if (!row.classList.contains("is-rendered") || row.dataset.src !== src) {
          needRender = true;
        }
        if (needRender)
          break;
      }
    }
    if (forceFull || needRender) {
      this.component.unload();
      this.component = new import_obsidian5.Component();
      this.component.load();
    }
    let i = 0;
    while (i < rows.length) {
      const row = rows[i];
      const src = (_b = this.source[i]) != null ? _b : "";
      if (spanOf[i] >= 0) {
        const sp = spans[spanOf[i]];
        if (cursorInSpan) {
          for (let k = sp.start; k <= sp.end; k++) {
            const r = rows[k];
            const s = (_c = this.source[k]) != null ? _c : "";
            if (k === cur) {
              this.clearCodeFlags(r);
              r.classList.remove("is-rendered");
              r.classList.add("is-active", "is-code-line");
              if (r.dataset.activeSrc !== s) {
                r.textContent = s;
                r.dataset.activeSrc = s;
              }
            } else {
              this.renderLiteralLine(r, s);
            }
          }
          i = sp.end + 1;
          continue;
        }
        const joined = this.source.slice(sp.start, sp.end + 1).join("\n");
        this.renderCodeBlock(rows[sp.start], joined);
        for (let k = sp.start + 1; k <= sp.end; k++) {
          const r = rows[k];
          r.classList.remove("is-rendered", "is-active", "is-code-block", "is-code-line");
          r.classList.add("is-code-hidden");
          r.dataset.codeHidden = "1";
          r.dataset.src = (_d = this.source[k]) != null ? _d : "";
          r.innerHTML = "";
        }
        i = sp.end + 1;
        continue;
      }
      if (this.allSource || i === cur)
        this.renderActiveLine(row, src);
      else
        this.renderInactiveLine(row, src);
      i++;
    }
    this.updateEmpty();
    this.el.scrollTop = prevScroll;
    requestAnimationFrame(() => {
      if (this.el.scrollTop < prevScroll)
        this.el.scrollTop = prevScroll;
    });
  }
  /** 检测 source 中的围栏代码块区间（``` 或 ~~~ 起，到同字符围栏止；未闭合则延到末尾）。 */
  getFenceSpans(src) {
    const spans = [];
    let i = 0;
    while (i < src.length) {
      const open = src[i].match(/^(```|~~~)/);
      if (open) {
        const marker = open[1][0];
        const closing = new RegExp("^" + marker + "+\\s*$");
        let end = -1;
        for (let j = i + 1; j < src.length; j++) {
          if (closing.test(src[j])) {
            end = j;
            break;
          }
        }
        if (end === -1)
          end = src.length - 1;
        spans.push({ start: i, end });
        i = end + 1;
      } else {
        i++;
      }
    }
    return spans;
  }
  clearCodeFlags(row) {
    row.classList.remove("is-code-block", "is-code-hidden", "is-code-line");
    delete row.dataset.codeBlock;
    delete row.dataset.codeHidden;
  }
  /** 把整段围栏代码块渲染为单个代码块（MarkdownRenderer 产出完整 <pre><code>）。 */
  renderCodeBlock(row, joinedSrc) {
    this.clearCodeFlags(row);
    row.classList.add("is-code-block");
    row.dataset.codeBlock = "start";
    row.dataset.src = joinedSrc;
    row.innerHTML = "";
    import_obsidian5.MarkdownRenderer.render(this.app, joinedSrc, row, this.sourcePath, this.component);
  }
  /** 代码块内的非激活行：以字面文本显示（保留 ``` 围栏与代码内容，不触发独立代码块/复制按钮）。 */
  renderLiteralLine(row, src) {
    this.clearCodeFlags(row);
    row.classList.add("is-code-line");
    delete row.dataset.activeSrc;
    if (row.dataset.src === src && row.textContent === src)
      return;
    row.textContent = src;
    row.dataset.src = src;
  }
  /** 非光标行：用 Obsidian 自带 MarkdownRenderer 完整渲染富文本。 */
  renderInactiveLine(row, src) {
    this.clearCodeFlags(row);
    row.classList.remove("is-active");
    HEADING_CLASSES.concat(["lp-quote", "lp-bullet", "lp-ordered"]).forEach(
      (c) => row.classList.remove(c)
    );
    delete row.dataset.activeSrc;
    if (row.classList.contains("is-rendered") && row.dataset.src === src)
      return;
    row.innerHTML = "";
    row.classList.add("is-rendered");
    row.dataset.src = src;
    import_obsidian5.MarkdownRenderer.render(
      this.app,
      src,
      row,
      this.sourcePath,
      this.component
    );
  }
  /**
   * 光标行：仍显示源码（含 markdown 标记），但叠加排版样式。
   * 例如 `# 标题` 用 .lp-h1 放大、加粗用 span 高亮——文本字符不变，
   * 因而光标偏移映射完全不受影响。
   */
  renderActiveLine(row, src) {
    this.clearCodeFlags(row);
    if (row.classList.contains("is-rendered")) {
      row.classList.remove("is-rendered");
      delete row.dataset.src;
      row.textContent = src;
    }
    row.classList.add("is-active");
    const blockCls = this.blockClass(src);
    HEADING_CLASSES.concat(["lp-quote", "lp-bullet", "lp-ordered"]).forEach(
      (c) => row.classList.remove(c)
    );
    if (blockCls)
      row.classList.add(blockCls);
    if (blockCls && blockCls.startsWith("lp-h")) {
      if (row.dataset.activeSrc === src)
        return;
      const offset = this.getCaretCol();
      row.innerHTML = this.highlightHeading(src);
      row.dataset.activeSrc = src;
      this.setCaretCol(offset);
    } else if (this.inlineToken(src)) {
      if (row.dataset.activeSrc === src)
        return;
      const offset = this.getCaretCol();
      row.innerHTML = this.highlightInline(src);
      row.dataset.activeSrc = src;
      this.setCaretCol(offset);
    } else if (row.dataset.activeSrc !== src) {
      row.dataset.activeSrc = src;
    }
  }
  scheduleRender() {
    if (this.renderScheduled)
      return;
    this.renderScheduled = true;
    requestAnimationFrame(() => {
      this.renderScheduled = false;
      if (!this.composing)
        this.renderLines();
    });
  }
  /* --------------------- 源码高亮（光标行用） --------------------- */
  blockClass(src) {
    const m = src.match(/^(#{1,6})\s/);
    if (m)
      return "lp-h" + m[1].length;
    if (/^>\s?/.test(src))
      return "lp-quote";
    if (/^[-*+]\s/.test(src))
      return "lp-bullet";
    if (/^\d+\.\s/.test(src))
      return "lp-ordered";
    return "";
  }
  inlineToken(src) {
    return /(\*\*|__|\*|_|~~|`|\[[^\]]+\]\([^)]+\))/.test(src);
  }
  highlightInline(src) {
    let s = escapeHtml(src);
    s = s.replace(/`([^`]+)`/g, '<code class="lp-code">`$1`</code>');
    s = s.replace(/\*\*([^*]+?)\*\*/g, '<strong class="lp-strong">**$1**</strong>');
    s = s.replace(/__([^_]+?)__/g, '<strong class="lp-strong">__$1__</strong>');
    s = s.replace(/(^|[^*])\*([^*\n]+?)\*(?!\*)/g, '$1<em class="lp-em">*$2*</em>');
    s = s.replace(/(^|[^_])_([^_\n]+?)_(?!_)/g, '$1<em class="lp-em">_$2_</em>');
    s = s.replace(/~~([^~]+?)~~/g, '<del class="lp-del">~~$1~~</del>');
    s = s.replace(
      /\[([^\]]+)\]\(([^)]+)\)/g,
      '<span class="lp-link">[$1]($2)</span>'
    );
    return s;
  }
  /**
   * 标题行高亮：用真正的 <h1>~<h6> 元素包住文本（与「非光标行」MarkdownRenderer
   * 产出的 <hN> 完全一致），光标进出标题行时行高严格相等、不闪烁。行首 # 标记单独
   * 包成 .lp-hash（彩色、缩小、右移），文本部分仍走 highlightInline 支持标题内行内标记。
   */
  highlightHeading(src) {
    const m = src.match(/^(#{1,6})(\s?)([\s\S]*)$/);
    if (!m)
      return escapeHtml(src);
    const level = m[1].length;
    const inner = this.highlightInline(m[2] + m[3]);
    return `<h${level} class="muse-lp-heading"><span class="lp-hash">${escapeHtml(
      m[1]
    )}</span>${inner}</h${level}>`;
  }
  onInput() {
    var _a;
    this.syncFromDom();
    if (this.composing) {
      this.updateSuggest();
      return;
    }
    this.scheduleRender();
    this.updateSuggest();
    (_a = this.changeCb) == null ? void 0 : _a.call(this);
  }
  /** 多行粘贴：按纯文本插入并按换行拆成多行，避免 contenteditable 自作主张产生错位 DOM。 */
  onPaste(e) {
    var _a, _b, _c, _d, _e;
    e.preventDefault();
    const text = (_b = (_a = e.clipboardData) == null ? void 0 : _a.getData("text/plain")) != null ? _b : "";
    if (!text)
      return;
    if (this.allSource)
      this.allSource = false;
    this.syncFromDom();
    const row = this.cursorRow;
    const col = this.getCaretCol();
    const cur = (_c = this.source[row]) != null ? _c : "";
    const before = cur.slice(0, col);
    const after = cur.slice(col);
    const pasted = text.split("\n");
    const newLines = [];
    newLines.push(before + pasted[0]);
    for (let k = 1; k < pasted.length; k++)
      newLines.push(pasted[k]);
    const lastIdx = newLines.length - 1;
    newLines[lastIdx] = newLines[lastIdx] + after;
    this.source.splice(row, 1, ...newLines);
    this.cursorRow = row + pasted.length - 1;
    this.rebuildDom();
    this.renderLines();
    this.setCaretCol(((_d = newLines[lastIdx]) != null ? _d : "").length - after.length);
    (_e = this.changeCb) == null ? void 0 : _e.call(this);
  }
  onKeydown(e) {
    var _a, _b, _c;
    if (((_a = this.suggest) == null ? void 0 : _a.active) && this.handleSuggestKey(e)) {
      e.stopPropagation();
      return;
    }
    const sel0 = window.getSelection();
    if ((this.allSource || sel0 && !sel0.isCollapsed) && (e.key === "Backspace" || e.key === "Delete" || e.key === "Enter")) {
      this.allSource = false;
      return;
    }
    (_b = this.keyCb) == null ? void 0 : _b.call(this, e);
    if (e.defaultPrevented)
      return;
    if (e.isComposing || e.keyCode === 229)
      return;
    if ((e.ctrlKey || e.metaKey) && e.key.toLowerCase() === "a") {
      e.preventDefault();
      if (!this.allSource) {
        this.allSource = true;
        this.renderLines();
      }
      const sel = window.getSelection();
      if (sel) {
        const range = document.createRange();
        range.selectNodeContents(this.el);
        sel.removeAllRanges();
        sel.addRange(range);
      }
      return;
    }
    if (e.key === "ArrowUp" || e.key === "ArrowDown") {
      this.navArrow = true;
    }
    if (e.key === "Enter") {
      e.preventDefault();
      this.splitLineAtCaret();
    } else if (e.key === "Backspace") {
      if (this.getCaretCol() === 0 && this.cursorRow > 0) {
        e.preventDefault();
        this.mergeWithPrev();
      }
    } else if (e.key === "Delete") {
      const len = ((_c = this.source[this.cursorRow]) != null ? _c : "").length;
      if (this.getCaretCol() === len && this.cursorRow < this.source.length - 1) {
        e.preventDefault();
        this.mergeWithNext();
      }
    }
  }
  continuePrefix(before) {
    const lines = before.split("\n");
    const last = lines[lines.length - 1];
    let m = last.match(/^(\s*(?:[-*+]|\d+\.)\s+)/);
    if (m)
      return m[1];
    m = last.match(/^(\s*>+\s?)/);
    if (m)
      return m[1];
    return "";
  }
  splitLineAtCaret() {
    var _a, _b;
    const row = this.cursorRow;
    const col = this.getCaretCol();
    const text = (_a = this.source[row]) != null ? _a : "";
    const before = text.slice(0, col);
    const after = text.slice(col);
    const cont = this.continuePrefix(before);
    this.source[row] = before;
    this.source.splice(row + 1, 0, cont + after);
    const curEl = this.lineEls()[row];
    if (curEl)
      curEl.textContent = before;
    const newEl = this.el.createDiv({ cls: "muse-lp-line" });
    newEl.textContent = cont + after;
    if (curEl && curEl.nextSibling)
      this.el.insertBefore(newEl, curEl.nextSibling);
    else
      this.el.appendChild(newEl);
    this.cursorRow = row + 1;
    this.renderLines();
    this.setCaretCol(cont.length);
    (_b = this.changeCb) == null ? void 0 : _b.call(this);
  }
  mergeWithPrev() {
    var _a, _b, _c, _d;
    const row = this.cursorRow;
    const prevLen = ((_a = this.source[row - 1]) != null ? _a : "").length;
    this.source[row - 1] = ((_b = this.source[row - 1]) != null ? _b : "") + ((_c = this.source[row]) != null ? _c : "");
    this.source.splice(row, 1);
    const rows = this.lineEls();
    const curEl = rows[row];
    const prevEl = rows[row - 1];
    if (prevEl)
      prevEl.textContent = this.source[row - 1];
    if (curEl)
      curEl.remove();
    this.cursorRow = row - 1;
    this.renderLines();
    this.setCaretCol(prevLen);
    (_d = this.changeCb) == null ? void 0 : _d.call(this);
  }
  mergeWithNext() {
    var _a, _b, _c, _d;
    const row = this.cursorRow;
    const curLen = ((_a = this.source[row]) != null ? _a : "").length;
    this.source[row] = ((_b = this.source[row]) != null ? _b : "") + ((_c = this.source[row + 1]) != null ? _c : "");
    this.source.splice(row + 1, 1);
    const rows = this.lineEls();
    const curEl = rows[row];
    const nextEl = rows[row + 1];
    if (curEl)
      curEl.textContent = this.source[row];
    if (nextEl)
      nextEl.remove();
    this.renderLines();
    this.setCaretCol(curLen);
    (_d = this.changeCb) == null ? void 0 : _d.call(this);
  }
  /** 读取当前光标（折叠选区）的 viewport 水平坐标；取不到返回 -1。 */
  getCaretX() {
    var _a;
    const sel = window.getSelection();
    if (!sel || sel.rangeCount === 0)
      return -1;
    const range = sel.getRangeAt(0);
    let rect = range.getBoundingClientRect();
    if (rect.height === 0 && rect.width === 0) {
      const rects = range.getClientRects();
      if (rects.length)
        rect = rects[0];
    }
    if (rect.height === 0 && rect.width === 0) {
      const node = range.endContainer;
      const off = range.endOffset;
      if (node.nodeType === Node.TEXT_NODE) {
        const len = ((_a = node.textContent) != null ? _a : "").length;
        const s = Math.max(0, off - 1);
        const e = Math.min(off, len);
        if (e > s) {
          const r2 = document.createRange();
          try {
            r2.setStart(node, s);
            r2.setEnd(node, e);
            const rr = r2.getBoundingClientRect();
            if (rr.height > 0 || rr.width > 0)
              rect = rr;
          } catch (e2) {
          }
        }
      }
    }
    if (rect.height === 0 && rect.width === 0)
      return -1;
    return rect.left;
  }
  /** 把光标放到光标行内「与给定 viewport X 最接近」的字符处，从而还原跨行前的横向列位。 */
  setCaretAtX(x) {
    var _a, _b;
    const row = this.lineEls()[this.cursorRow];
    if (!row)
      return;
    const sel = window.getSelection();
    if (!sel)
      return;
    let markerW = 0;
    row.querySelectorAll(".lp-hash").forEach((m) => {
      markerW += m.getBoundingClientRect().width;
    });
    const targetX = x + markerW;
    const walker = document.createTreeWalker(row, NodeFilter.SHOW_TEXT, null);
    let node;
    while (node = walker.nextNode()) {
      const t = node;
      const len = (_b = (_a = t.textContent) == null ? void 0 : _a.length) != null ? _b : 0;
      if (len === 0)
        continue;
      const nodeRect = this.rangeRect(t, 0, len);
      if (!nodeRect)
        continue;
      if (targetX <= nodeRect.left) {
        this.placeCaret(t, 0);
        return;
      }
      if (targetX >= nodeRect.right)
        continue;
      let best = 0;
      let bestDist = Infinity;
      for (let o = 0; o <= len; o++) {
        const r = this.rangeRect(t, o, Math.min(o + 1, len));
        if (!r)
          continue;
        const cx = (r.left + r.right) / 2;
        const dist = Math.abs(cx - targetX);
        if (dist < bestDist) {
          bestDist = dist;
          best = o;
        }
      }
      this.placeCaret(t, best);
      return;
    }
    const range = document.createRange();
    range.selectNodeContents(row);
    range.collapse(false);
    sel.removeAllRanges();
    sel.addRange(range);
    this.el.focus();
  }
  rangeRect(node, start, end) {
    const r = document.createRange();
    try {
      r.setStart(node, start);
      r.setEnd(node, end);
      return r.getBoundingClientRect();
    } catch (e) {
      return null;
    }
  }
  placeCaret(node, offset) {
    const sel = window.getSelection();
    if (!sel)
      return;
    const range = document.createRange();
    range.setStart(node, offset);
    range.collapse(true);
    sel.removeAllRanges();
    sel.addRange(range);
    this.el.focus();
  }
  /** 把当前选区还原成 markdown 源码文本（按行拼接）。 */
  getSelectionSource() {
    var _a, _b, _c, _d;
    const sel = window.getSelection();
    if (!sel || sel.rangeCount === 0)
      return null;
    const range = sel.getRangeAt(0);
    const rows = this.lineEls();
    if (rows.length === 0)
      return null;
    const startRow = this.rowOfNode(range.startContainer, rows);
    const endRow = this.rowOfNode(range.endContainer, rows);
    if (startRow < 0 || endRow < 0)
      return null;
    const parts = [];
    for (let i = startRow; i <= endRow; i++) {
      const row = rows[i];
      const rendered = ((_a = row.textContent) != null ? _a : "").replace(/\n$/, "");
      const isRendered = row.classList.contains("is-rendered");
      const src = isRendered ? (_c = (_b = row.dataset.src) != null ? _b : this.source[i]) != null ? _c : "" : (_d = this.source[i]) != null ? _d : "";
      let aLine;
      let bLine;
      if (i === startRow && i === endRow) {
        aLine = this.clampOffset(row, range.startContainer, range.startOffset, rendered.length);
        bLine = this.clampOffset(row, range.endContainer, range.endOffset, rendered.length);
      } else if (i === startRow) {
        aLine = this.clampOffset(row, range.startContainer, range.startOffset, rendered.length);
        bLine = rendered.length;
      } else if (i === endRow) {
        aLine = 0;
        bLine = this.clampOffset(row, range.endContainer, range.endOffset, rendered.length);
      } else {
        aLine = 0;
        bLine = rendered.length;
      }
      const a = Math.min(aLine, bLine);
      const b = Math.max(aLine, bLine);
      const full = a <= 0 && b >= rendered.length;
      if (full)
        parts.push(src);
      else if (!isRendered)
        parts.push(src.slice(a, b));
      else
        parts.push(rendered.slice(a, b));
    }
    return parts.join("\n");
  }
  rowOfNode(node, rows) {
    let n = node;
    while (n && n !== this.el) {
      if (n instanceof HTMLElement && n.classList.contains("muse-lp-line")) {
        return rows.indexOf(n);
      }
      n = n.parentNode;
    }
    return -1;
  }
  /** 计算 container/offset 在 row「渲染文本」中的字符偏移；落在 row 之外则夹紧到 0/max。 */
  clampOffset(row, container, offset, max) {
    if (!row.contains(container)) {
      const pre2 = document.createRange();
      pre2.selectNodeContents(row);
      let cmp = 1;
      try {
        cmp = pre2.comparePoint(container, offset);
      } catch (e) {
        cmp = 1;
      }
      return cmp < 0 ? 0 : max;
    }
    const pre = document.createRange();
    pre.selectNodeContents(row);
    pre.setEnd(container, offset);
    return Math.max(0, Math.min(pre.toString().length, max));
  }
  /* ----------------------- 内部链接建议（[[ 自动补全） ----------------------- */
  /** 构建浮层容器（挂在编辑器父节点上，用 fixed 定位，避免被 contenteditable 影响）。 */
  buildSuggestEl() {
    this.suggestEl = document.body.createDiv({ cls: "muse-suggest" });
    this.suggestEl.style.display = "none";
    this.suggestEl.addEventListener("mousedown", (e) => e.preventDefault());
  }
  /** 输入后检测光标行内是否处于未闭合的 [[ 触发态，刷新/隐藏建议。
   *  合成（IME）期间也会调用：中文输入时 [[ 后的候选要随拼音实时刷新。 */
  updateSuggest() {
    var _a, _b, _c, _d;
    const activeText = (_a = this.source[this.cursorRow]) != null ? _a : "";
    const col = this.getCaretCol();
    const before = activeText.slice(0, col);
    const m = before.match(/\[\[([^\[\]\n]*)$/);
    if (!m) {
      if ((_b = this.suggest) == null ? void 0 : _b.active)
        this.hideSuggest();
      return;
    }
    const start = m.index + 2;
    const query = m[1];
    if (!this.cachedFiles.length)
      this.refreshFiles();
    let items;
    if (!query) {
      items = this.cachedFiles.slice(0, 50);
    } else {
      const q = query.toLowerCase();
      const fuzzy = (0, import_obsidian5.prepareFuzzySearch)(query);
      items = this.cachedFiles.map((f) => {
        const idx = f.toLowerCase().indexOf(q);
        const fr = fuzzy(f);
        return { f, idx, fr };
      }).filter((x) => x.idx >= 0 || x.fr).sort((a, b) => {
        const aInc = a.idx >= 0;
        const bInc = b.idx >= 0;
        if (aInc !== bInc)
          return aInc ? -1 : 1;
        if (aInc && bInc)
          return a.idx - b.idx;
        if (a.fr && b.fr)
          return a.fr.score - b.fr.score;
        return 0;
      }).slice(0, 50).map((x) => x.f);
    }
    if (items.length === 0) {
      if ((_c = this.suggest) == null ? void 0 : _c.active)
        this.hideSuggest();
      return;
    }
    if (!((_d = this.suggest) == null ? void 0 : _d.active)) {
      this.suggest = { active: true, start, query, items, index: 0 };
    } else {
      this.suggest.start = start;
      this.suggest.query = query;
      this.suggest.items = items;
      this.suggest.index = Math.min(this.suggest.index, items.length - 1);
    }
    this.renderSuggest();
  }
  /** 渲染建议列表并定位到光标下方。 */
  renderSuggest() {
    var _a;
    if (!this.suggest)
      return;
    const host = this.suggestEl;
    host.empty();
    this.suggest.items.forEach((name, i) => {
      const item = host.createDiv({
        cls: "muse-suggest-item" + (i === this.suggest.index ? " is-active" : "")
      });
      item.createSpan({ text: name });
      item.addEventListener("click", () => this.applySuggest(name));
    });
    const rowEl = this.lineEls()[this.cursorRow];
    const caretX = this.getCaretX();
    const rect = rowEl == null ? void 0 : rowEl.getBoundingClientRect();
    const left = caretX >= 0 ? caretX : (_a = rect == null ? void 0 : rect.left) != null ? _a : 0;
    const top = rect ? rect.bottom : 0;
    host.style.left = `${left}px`;
    host.style.top = `${top + 4}px`;
    host.style.display = "block";
    const active = host.querySelector(
      ".muse-suggest-item.is-active"
    );
    if (active)
      active.scrollIntoView({ block: "nearest" });
  }
  /** 选择某项：把 [[query 替换为 [[name]]，光标落在 ]] 之后。 */
  applySuggest(name) {
    if (!this.suggest)
      return;
    const row = this.suggest.start;
    const col = this.getCaretCol();
    this.replaceRange(row, col, name + "]]");
    this.hideSuggest();
  }
  /** 隐藏建议浮层并清空状态。 */
  hideSuggest() {
    this.suggest = null;
    if (this.suggestEl)
      this.suggestEl.style.display = "none";
  }
  /** 重新读取 vault 内全部 Markdown 笔记的 basename（去重），供搜索复用。 */
  refreshFiles() {
    const set = /* @__PURE__ */ new Set();
    for (const f of this.app.vault.getMarkdownFiles())
      set.add(f.basename);
    this.cachedFiles = Array.from(set);
  }
  /** 建议激活时处理导航键，返回 true 表示已消费该事件。 */
  handleSuggestKey(e) {
    if (!this.suggest)
      return false;
    if (e.isComposing)
      return false;
    switch (e.key) {
      case "ArrowDown":
        e.preventDefault();
        this.suggest.index = (this.suggest.index + 1) % this.suggest.items.length;
        this.renderSuggest();
        return true;
      case "ArrowUp":
        e.preventDefault();
        this.suggest.index = (this.suggest.index - 1 + this.suggest.items.length) % this.suggest.items.length;
        this.renderSuggest();
        return true;
      case "Enter":
      case "Tab":
        e.preventDefault();
        if (this.suggest.items.length) {
          this.applySuggest(this.suggest.items[this.suggest.index]);
        } else {
          this.hideSuggest();
        }
        return true;
      case "Escape":
        e.preventDefault();
        this.hideSuggest();
        return true;
      default:
        return false;
    }
  }
};

// src/view/MuseView.ts
var _MuseView = class _MuseView extends import_obsidian6.ItemView {
  constructor(leaf, plugin) {
    super(leaf);
    this.filter = {
      tag: null,
      year: null,
      date: null,
      keyword: "",
      preset: "all"
    };
    this.unsubscribe = null;
    this.childComponent = new import_obsidian6.Component();
    this.overviewMode = "heatmap";
    this.overviewModeOverridden = false;
    this.calViewMonth = /* @__PURE__ */ new Date();
    this.editingMemo = null;
    this.editBannerEl = null;
    this.editDateTimeEl = null;
    this.pageLimit = 50;
    this.loadingMore = false;
    this.onListScroll = () => {
      const el = this.listEl;
      if (!el)
        return;
      if (el.scrollHeight - el.scrollTop - el.clientHeight < 60) {
        const filtered = this.getFilteredMemos();
        if (this.pageLimit < filtered.length)
          this.loadMore();
      }
    };
    this.heatmapTooltipEl = null;
    this.resizeTimer = null;
    this.handleResize = () => {
      if (this.resizeTimer !== null)
        window.clearTimeout(this.resizeTimer);
      this.resizeTimer = window.setTimeout(() => {
        this.resizeTimer = null;
        this.recomputeCollapse();
      }, 150);
    };
    this.plugin = plugin;
    this.overviewMode = this.plugin.settings.defaultOverviewMode || "heatmap";
  }
  getViewType() {
    return VIEW_TYPE_MUSE;
  }
  /** 从设置页调用：切换概览模式并立即重渲染侧栏，避免改完参数不生效。 */
  setOverviewMode(mode) {
    this.overviewMode = mode;
    this.overviewModeOverridden = false;
    this.renderSidebar();
  }
  /**
   * 设置项变化后立即生效的兜底刷新：
   * - 重新拉设置里的侧栏、列表、密度、占位符等所有与配置相关的可视部分。
   * - 不重新打开文件 / 重新加载数据，所以开销很低，仅做 DOM 重建。
   * - 任何 settings.onChange 在 saveSettings() 之后调用本方法，即可做到"改完即生效"。
   */
  refresh() {
    if (!this.overviewModeOverridden)
      this.overviewMode = this.plugin.settings.defaultOverviewMode || "heatmap";
    this.renderSidebar();
    this.renderList();
    this.updateEditBanner();
    this.applyDensity();
  }
  getDisplayText() {
    return this.plugin.t("view.title");
  }
  getIcon() {
    return "feather";
  }
  async onOpen() {
    this.contentEl.addClass("muse-root");
    this.contentEl.addClass("muse-container");
    this.buildLayout();
    this.unsubscribe = this.plugin.store.onChange(() => this.renderAll());
    this.registerDomEvent(this.contentEl, "keydown", (e) => {
      const s = document.activeElement;
      if (s instanceof HTMLElement && this.contentEl.contains(s) && this.shouldSendOnKeydown(e)) {
        e.preventDefault();
        e.stopPropagation();
        e.stopImmediatePropagation();
        this.submitMemo();
      }
    });
    this.childComponent.load();
    this.registerDomEvent(window, "resize", this.handleResize);
    try {
      await this.plugin.store.reloadAll();
    } catch (err) {
      console.error("[MuseView] reloadAll failed:", err);
    }
    const draft = this.loadDraft();
    if (draft)
      this.editor.setValue(draft);
    this.autoResizeInput();
    this.syncInputCardContentState();
    this.renderAll();
  }
  async onClose() {
    if (this.unsubscribe)
      this.unsubscribe();
    this.unsubscribe = null;
    this.hideHeatmapTooltip();
    this.childComponent.unload();
  }
  /** 供命令/设置页调用：刷新密度样式。 */
  applyDensity() {
    if (this.listEl) {
      this.listEl.toggleClass(
        "is-compact",
        this.plugin.settings.density === "compact"
      );
    }
  }
  /** 供命令调用：聚焦输入框。 */
  focusInput() {
    var _a;
    (_a = this.inputEl) == null ? void 0 : _a.focus();
  }
  /* ----------------------------- 布局骨架 ----------------------------- */
  buildLayout() {
    this.contentEl.empty();
    const shell = this.contentEl.createDiv({ cls: "muse-shell" });
    this.sidebarEl = shell.createDiv({ cls: "muse-sidebar" });
    shell.createDiv({ cls: "muse-sidebar-overlay" }).addEventListener("click", () => this.toggleSidebar(false));
    this.statsEl = this.sidebarEl.createDiv({ cls: "muse-stats" });
    const searchContainer = this.sidebarEl.createDiv({
      cls: "muse-sidebar-search"
    });
    const searchWrap = searchContainer.createDiv({ cls: "muse-search-wrap" });
    const searchIcon = searchWrap.createDiv({ cls: "muse-search-icon" });
    (0, import_obsidian6.setIcon)(searchIcon, "search");
    this.searchEl = searchWrap.createEl("input", {
      cls: "muse-search",
      attr: { placeholder: this.plugin.t("search.placeholder"), type: "text" }
    });
    const debouncedSearch = debounce(() => {
      this.filter.keyword = this.searchEl.value.trim();
      this.pageLimit = this.getInitialPageLimit();
      this.renderList();
    }, 180);
    this.searchEl.addEventListener("input", debouncedSearch);
    this.overviewWrapper = this.sidebarEl.createDiv({
      cls: "muse-overview-wrapper"
    });
    const main = shell.createDiv({ cls: "muse-main" });
    this.buildInputCard(main);
    this.metaHeatmapHeadEl = main.createDiv({
      cls: "muse-list-heatmap-head muse-list-heatmap-head-fixed"
    });
    this.listEl = main.createDiv({ cls: "muse-list" });
    this.registerDomEvent(this.listEl, "scroll", this.onListScroll);
  }
  buildInputCard(host) {
    const card = host.createDiv({ cls: "muse-input-card" });
    if (this.plugin.settings.livePreview) {
      this.editor = new LivePreviewEditor(card, this.app, "");
    } else {
      const ta = card.createEl("textarea", {
        cls: "muse-input",
        attr: {
          placeholder: this.plugin.t("input.placeholder"),
          rows: "1"
        }
      });
      this.editor = new TextareaEditor(ta);
    }
    this.inputEl = this.editor.el;
    this.editor.setOnFocus(() => card.addClass("is-focused"));
    this.editor.setOnBlur(() => card.removeClass("is-focused"));
    this.editor.setOnKeydown((e) => this.handleEditorKeydown(e));
    this.editor.setOnChange(() => this.handleEditorChange());
    this.editor.setPlaceholder(this.plugin.t("input.placeholder"));
    const toolbar = card.createDiv({ cls: "muse-input-toolbar" });
    const tools = toolbar.createDiv({ cls: "muse-input-tools" });
    const mkTool = (icon, label, onClick) => {
      const b = tools.createEl("button", {
        cls: "muse-tool-btn",
        attr: { "aria-label": label }
      });
      (0, import_obsidian6.setIcon)(b, icon);
      b.addEventListener("mousedown", (e) => e.preventDefault());
      b.addEventListener("click", onClick);
    };
    mkTool(
      "hash",
      this.plugin.t("toolbar.insertTag"),
      () => this.insertAtCursor("#")
    );
    mkTool(
      "image",
      this.plugin.t("toolbar.insertImage"),
      () => this.pickImageFromDisk()
    );
    mkTool(
      "list",
      this.plugin.t("toolbar.insertUL"),
      () => this.insertListAtCursor("- ")
    );
    mkTool(
      "list-ordered",
      this.plugin.t("toolbar.insertOL"),
      () => this.insertOrderedListAtCursor()
    );
    mkTool(
      "square-check",
      this.plugin.t("toolbar.insertTask"),
      () => this.insertListAtCursor("- [ ] ")
    );
    mkTool(
      "table",
      this.plugin.t("toolbar.insertTable"),
      () => this.showTablePicker()
    );
    const submitWrap = toolbar.createDiv({ cls: "muse-submit-wrap" });
    this.editDateTimeEl = submitWrap.createEl("input", {
      cls: "muse-edit-datetime muse-hidden",
      type: "datetime-local",
      attr: { step: "60", title: this.plugin.t("input.editTimeTitle") }
    });
    const cancelBtn = submitWrap.createEl("button", {
      cls: "muse-cancel-btn muse-hidden",
      text: this.plugin.t("input.cancel")
    });
    cancelBtn.addEventListener("click", () => this.exitEditMode());
    this.editBannerEl = cancelBtn;
    const submitBtn = submitWrap.createEl("button", {
      cls: "muse-submit-btn"
    });
    submitBtn.setText(this.plugin.t("input.submit"));
    submitBtn.addEventListener("click", () => this.submitMemo());
  }
  /* ----------------------------- 输入框辅助 ----------------------------- */
  handleEditorKeydown(e) {
    if (this.shouldSendOnKeydown(e)) {
      e.preventDefault();
      e.stopPropagation();
      e.stopImmediatePropagation();
      this.submitMemo();
      return;
    }
    if (!(e.isComposing || e.keyCode === 229)) {
      if (e.key === "Escape" && this.editingMemo) {
        e.preventDefault();
        this.exitEditMode();
      }
    }
  }
  handleEditorChange() {
    if (!this.editingMemo)
      this.saveDraft(this.editor.getValue());
    this.autoResizeInput();
    this.syncInputCardContentState();
  }
  insertAtCursor(text) {
    const s = this.editor.getCaret();
    const n = this.editor.getCaretEnd();
    const active = this.editor.getActiveText();
    if (s !== n) {
      const sel = active.slice(s, n);
      this.editor.replaceRange(s, n, text + sel);
    } else {
      this.editor.replaceRange(s, n, text);
    }
    this.editor.focus();
    this.autoResizeInput();
    if (!this.editingMemo)
      this.saveDraft(this.editor.getValue());
    this.syncInputCardContentState();
  }
  insertListAtCursor(prefix) {
    const s = this.editor.getCaret();
    const n = this.editor.getCaretEnd();
    const active = this.editor.getActiveText();
    if (s !== n) {
      const sel = active.slice(s, n).split("\n").map((l) => `${prefix}${l}`).join("\n");
      const before2 = active.slice(0, s);
      const pad = s === 0 || before2.endsWith("\n") ? sel : `
${sel}`;
      this.editor.replaceRange(s, n, pad);
      this.editor.focus();
      this.autoResizeInput();
      if (!this.editingMemo)
        this.saveDraft(this.editor.getValue());
      return;
    }
    const i = s;
    const before = active.slice(0, i);
    const atLineStart = i === 0 || before.endsWith("\n");
    this.insertAtCursor(atLineStart ? prefix : `
${prefix}`);
  }
  insertOrderedListAtCursor() {
    const s = this.editor.getCaret();
    const n = this.editor.getCaretEnd();
    const active = this.editor.getActiveText();
    if (s !== n) {
      const sel = active.slice(s, n).split("\n").map((l, idx) => `${idx + 1}. ${l}`).join("\n");
      const before2 = active.slice(0, s);
      const pad = s === 0 || before2.endsWith("\n") ? sel : `
${sel}`;
      this.editor.replaceRange(s, n, pad);
      this.editor.focus();
      this.autoResizeInput();
      if (!this.editingMemo)
        this.saveDraft(this.editor.getValue());
      return;
    }
    const i = s;
    const before = active.slice(0, i);
    const atLineStart = i === 0 || before.endsWith("\n");
    const lines = (atLineStart ? before.replace(/\n$/, "") : before).split("\n");
    let next = 1;
    for (let g = lines.length - 1; g >= 0; g--) {
      const m = lines[g].match(/^(\d+)\.\s/);
      if (!m)
        break;
      next = parseInt(m[1], 10) + 1;
      break;
    }
    this.insertAtCursor(atLineStart ? `${next}. ` : `
${next}. `);
  }
  async pickImageFromDisk() {
    const input = document.createElement("input");
    input.type = "file";
    input.accept = "image/*";
    input.addEventListener("change", async () => {
      var _a;
      const file = (_a = input.files) == null ? void 0 : _a[0];
      if (!file)
        return;
      try {
        const path = await this.plugin.store.saveImageAttachment(
          file,
          file.name.split(".").pop()
        );
        this.insertAtCursor(`![[${path}]]`);
      } catch (err) {
        new import_obsidian6.Notice(this.plugin.t("notice.error") + err.message);
      }
    });
    input.click();
  }
  autoResizeInput() {
    var _a;
    (_a = this.editor) == null ? void 0 : _a.autoResize();
  }
  syncInputCardContentState() {
    const card = this.inputEl.closest(".muse-input-card");
    if (!card)
      return;
    card.toggleClass("has-content", this.editor.getValue().length > 0);
  }
  shouldSendOnKeydown(e) {
    if (e.key !== "Enter")
      return false;
    const mod = e.ctrlKey || e.metaKey;
    const shift = e.shiftKey;
    if (this.plugin.settings.sendHotkey === "enter") {
      return !(mod || shift || e.isComposing || e.keyCode === 229);
    }
    return !!(mod && !shift);
  }
  /* ----------------------------- 提交 / 编辑 ----------------------------- */
  async submitMemo() {
    var _a, _b;
    const text = this.editor.getValue().trim();
    if (!text)
      return;
    try {
      if (this.editingMemo) {
        const dtVal = (_b = (_a = this.editDateTimeEl) == null ? void 0 : _a.value) != null ? _b : "";
        const orig = `${this.editingMemo.date}T${this.editingMemo.time}`;
        if (dtVal && dtVal !== orig) {
          const d = new Date(dtVal);
          if (isNaN(d.getTime())) {
            new import_obsidian6.Notice(this.plugin.t("notice.invalidTime"));
            return;
          }
          await this.plugin.store.editMemoDateTime(
            this.editingMemo,
            d,
            text
          );
        } else {
          await this.plugin.store.editMemo(this.editingMemo, text);
        }
        this.exitEditMode();
      } else {
        const withTag = this.appendActiveTagIfMissing(text);
        await this.plugin.store.addMemo(withTag);
      }
      if (this.plugin.settings.clearAfterSave) {
        this.editor.setValue("");
        this.clearDraft();
        this.editor.el.blur();
      }
      this.autoResizeInput();
      this.syncInputCardContentState();
    } catch (err) {
      console.error(err);
      new import_obsidian6.Notice(
        this.plugin.t("notice.saveFailed", { msg: err.message })
      );
    }
  }
  appendActiveTagIfMissing(text) {
    const tag = this.filter.tag;
    if (!tag)
      return text;
    const used = /* @__PURE__ */ new Set();
    const re = /#([A-Za-z0-9_\u4e00-\u9fff/]+)/g;
    let m;
    while ((m = re.exec(text)) !== null)
      used.add(m[1]);
    if (used.has(tag))
      return text;
    for (const u of used)
      if (u.startsWith(tag + "/"))
        return text;
    const pad = text.endsWith("\n") ? "" : "\n";
    return `${text}${pad}#${tag}`;
  }
  draftKey() {
    try {
      return `muse-draft:${this.app.vault.getName()}`;
    } catch (e) {
      return "muse-draft";
    }
  }
  saveDraft(text) {
    try {
      const k = this.draftKey();
      if (text.trim() === "")
        window.localStorage.removeItem(k);
      else if (text.length <= 512 * 1024)
        window.localStorage.setItem(k, text);
    } catch (e) {
    }
  }
  loadDraft() {
    var _a;
    try {
      return (_a = window.localStorage.getItem(this.draftKey())) != null ? _a : "";
    } catch (e) {
      return "";
    }
  }
  clearDraft() {
    try {
      window.localStorage.removeItem(this.draftKey());
    } catch (e) {
    }
  }
  toggleSidebar(open) {
    this.contentEl.toggleClass("muse-sidebar-open", open);
  }
  memoKey(memo) {
    return `${memo.file.path}#${memo.range[0]}`;
  }
  /** 编辑态高亮：仅当前 editingMemo 对应的卡片加 is-editing（列表不会因进入/退出编辑而重绘，
   *  故在此手动切换；memo 为 null 时清除全部）。 */
  setEditingCardHighlight(memo) {
    this.contentEl.findAll(".muse-card").forEach((c) => {
      const el = c;
      const match = memo != null && el.getAttribute("data-memo-key") === this.memoKey(memo);
      el.toggleClass("is-editing", match);
    });
  }
  enterEditMode(memo) {
    if (!this.editingMemo && this.editor.getValue().trim()) {
      this.saveDraft(this.editor.getValue());
    }
    this.editingMemo = memo;
    this.setEditingCardHighlight(memo);
    this.editor.setValue(memo.content);
    if (this.editDateTimeEl)
      this.editDateTimeEl.value = `${memo.date}T${memo.time}`;
    this.editor.focus();
    this.updateEditBanner();
    this.autoResizeInput();
    this.syncInputCardContentState();
  }
  exitEditMode() {
    this.editingMemo = null;
    this.setEditingCardHighlight(null);
    this.editor.setValue(this.loadDraft());
    if (this.editDateTimeEl)
      this.editDateTimeEl.value = "";
    this.updateEditBanner();
    this.autoResizeInput();
    this.syncInputCardContentState();
  }
  updateEditBanner() {
    var _a, _b;
    if (!this.editBannerEl)
      return;
    const card = this.inputEl.closest(".muse-input-card");
    if (this.editingMemo) {
      this.editBannerEl.removeClass("muse-hidden");
      (_a = this.editDateTimeEl) == null ? void 0 : _a.removeClass("muse-hidden");
      card == null ? void 0 : card.addClass("is-editing");
      this.editor.setPlaceholder(
        this.plugin.t("input.editPlaceholder", {
          date: this.editingMemo.date,
          time: this.editingMemo.time
        })
      );
    } else {
      this.editBannerEl.addClass("muse-hidden");
      (_b = this.editDateTimeEl) == null ? void 0 : _b.addClass("muse-hidden");
      card == null ? void 0 : card.removeClass("is-editing");
      if (this.filter.tag)
        this.editor.setPlaceholder(this.plugin.t("input.placeholderWithTag", { tag: this.filter.tag }));
      else
        this.editor.setPlaceholder(this.plugin.t("input.placeholder"));
    }
  }
  /* ----------------------------- 渲染总控 ----------------------------- */
  getInitialPageLimit() {
    return Math.max(10, this.plugin.settings.pageSize || 50);
  }
  renderAll() {
    if (!this.overviewModeOverridden)
      this.overviewMode = this.plugin.settings.defaultOverviewMode || "heatmap";
    this.renderSidebar();
    this.renderList();
    this.updateEditBanner();
  }
  renderSidebar() {
    var _a, _b;
    const stats = this.sidebarEl.querySelector(".muse-stats");
    if (stats)
      stats.empty();
    const overview = this.sidebarEl.querySelector(".muse-overview-wrapper");
    if (overview)
      overview.empty();
    this.sidebarEl.querySelectorAll(
      ".muse-sidebar-section, .muse-nav-item, .muse-daily-goal-row, .muse-tag-node, .muse-tag-cloud"
    ).forEach((el) => el.remove());
    const all = this.plugin.store.getAll();
    const tagSet = /* @__PURE__ */ new Set();
    const daySet = /* @__PURE__ */ new Set();
    let nImg = 0, nLink = 0, nPin = 0, nStar = 0, nTodo = 0, nNoTag = 0;
    const today = dateKey(/* @__PURE__ */ new Date());
    const monthDay = today.slice(5);
    const weekStart = startOfWeek();
    let todayCount = 0;
    for (const v of all) {
      for (const tg of v.tags)
        if (!SYSTEM_TAGS.includes(tg))
          tagSet.add(tg);
      daySet.add(v.date);
      if (v.hasImage)
        nImg++;
      if (v.hasLink)
        nLink++;
      if (v.isPinned)
        nPin++;
      if (v.isStarred)
        nStar++;
      if (v.hasOpenTask)
        nTodo++;
      if (v.tags.filter((t) => !SYSTEM_TAGS.includes(t)).length === 0)
        nNoTag++;
      if (v.date === today)
        todayCount++;
      if (v.datetime.getTime() >= weekStart) {
      }
    }
    if (this.statsEl) {
      this.renderStatItem(this.statsEl, String(all.length), this.plugin.t("stats.memos"));
      this.renderStatItem(this.statsEl, String(tagSet.size), this.plugin.t("stats.tags"));
      this.renderStatItem(this.statsEl, String(daySet.size), this.plugin.t("stats.days"));
    }
    this.renderOverview(this.overviewWrapper, all);
    this.renderDailyGoal(this.sidebarEl, all, today, todayCount);
    this.sidebarEl.createDiv({
      cls: "muse-sidebar-section",
      text: this.plugin.t("sidebar.section.views")
    });
    const views = [
      { key: "all", icon: "layout-grid", text: this.plugin.t("sidebar.all"), count: all.length },
      { key: "pinned", icon: "pin", text: this.plugin.t("sidebar.pinned"), count: nPin },
      { key: "starred", icon: "star", text: this.plugin.t("sidebar.starred"), count: nStar },
      { key: "today", icon: "calendar", text: this.plugin.t("sidebar.today"), count: todayCount },
      { key: "week", icon: "calendar-days", text: this.plugin.t("sidebar.week"), count: this.weekCount(all, weekStart) },
      { key: "todo", icon: "check-square", text: this.plugin.t("sidebar.todo"), count: nTodo },
      { key: "on-this-day", icon: "history", text: this.plugin.t("sidebar.review"), count: this.onThisDayCount(all, monthDay, today) }
    ];
    for (const v of views)
      this.renderNavItem(v.key, v.icon, v.text, v.count);
    this.sidebarEl.createDiv({
      cls: "muse-sidebar-section",
      text: this.plugin.t("sidebar.section.search")
    });
    this.renderNavItem("no-tag", "tag", this.plugin.t("sidebar.noTag"), nNoTag);
    this.renderNavItem("with-image", "image", this.plugin.t("sidebar.withImage"), nImg);
    this.renderNavItem("with-link", "link", this.plugin.t("sidebar.withLink"), nLink);
    const years = /* @__PURE__ */ new Map();
    for (const v of all) {
      const y = v.date.substring(0, 4);
      years.set(y, ((_a = years.get(y)) != null ? _a : 0) + 1);
    }
    if (years.size) {
      this.sidebarEl.createDiv({
        cls: "muse-sidebar-section",
        text: this.plugin.t("sidebar.section.years")
      });
      const sorted = [...years.entries()].sort(
        (a, b) => a[0] < b[0] ? 1 : -1
      );
      for (const [y, c] of sorted) {
        const item = this.sidebarEl.createDiv({
          cls: "muse-nav-item" + (this.filter.year === y ? " active" : "")
        });
        const icon = item.createDiv({ cls: "muse-nav-icon" });
        (0, import_obsidian6.setIcon)(icon, "calendar");
        item.createSpan({ cls: "muse-nav-text", text: y });
        item.createSpan({ cls: "muse-nav-count", text: String(c) });
        item.addEventListener("click", () => {
          this.filter.year = this.filter.year === y ? null : y;
          this.filter.preset = "all";
          this.pageLimit = this.getInitialPageLimit();
          this.renderAll();
        });
      }
    }
    if (this.plugin.settings.sidebarTagsMode !== "off") {
      const tagCounts = /* @__PURE__ */ new Map();
      for (const v of all)
        for (const tg of v.tags)
          if (!SYSTEM_TAGS.includes(tg))
            tagCounts.set(tg, ((_b = tagCounts.get(tg)) != null ? _b : 0) + 1);
      if (tagCounts.size) {
        this.sidebarEl.createDiv({
          cls: "muse-sidebar-section",
          text: `${this.plugin.t("sidebar.section.tags")} (${tagCounts.size})`
        });
        if (this.plugin.settings.sidebarTagsMode === "cloud") {
          this.renderTagCloud(this.sidebarEl, tagCounts);
        } else {
          const tree = this.buildTagTree(tagCounts);
          this.renderTagTree(this.sidebarEl, tree, 0);
        }
      }
    }
  }
  weekCount(all, weekStart) {
    return all.filter((m) => m.datetime.getTime() >= weekStart).length;
  }
  onThisDayCount(all, monthDay, today) {
    return all.filter(
      (m) => m.date.slice(5) === monthDay && m.date !== today
    ).length;
  }
  renderStatItem(host, num, label) {
    const item = host.createDiv({ cls: "muse-stat" });
    item.createDiv({ cls: "muse-stat-num", text: num });
    item.createDiv({ cls: "muse-stat-label", text: label });
  }
  renderNavItem(key, icon, text, count) {
    const active = this.filter.preset === key && !this.filter.tag && !this.filter.year;
    const item = this.sidebarEl.createDiv({
      cls: "muse-nav-item" + (active ? " active" : "")
    });
    const ic = item.createDiv({ cls: "muse-nav-icon" });
    (0, import_obsidian6.setIcon)(ic, icon);
    item.createSpan({ cls: "muse-nav-text", text });
    if (count !== void 0)
      item.createSpan({ cls: "muse-nav-count", text: String(count) });
    item.addEventListener("click", () => {
      this.filter.preset = key;
      this.filter.tag = null;
      this.filter.year = null;
      this.filter.date = null;
      if (key === "random") {
        this.filter.randomSeed = Date.now();
        this.filter.randomCount = 5;
      }
      this.pageLimit = this.getInitialPageLimit();
      this.renderAll();
    });
  }
  /* ----------------------------- 概览（热力图） ----------------------------- */
  renderOverview(host, memos) {
    host.empty();
    const overview = host.createDiv({ cls: "muse-overview" });
    const content = overview.createDiv({ cls: "muse-overview-content" });
    if (this.overviewMode === "calendar")
      this.renderCalendar(content, memos);
    else
      this.renderHeatmap(content, memos);
  }
  renderCalendar(host, memos) {
    var _a;
    const counts = /* @__PURE__ */ new Map();
    for (const m of memos)
      counts.set(m.date, ((_a = counts.get(m.date)) != null ? _a : 0) + 1);
    const todayKey = dateKey(/* @__PURE__ */ new Date());
    const nav = host.createDiv({ cls: "muse-cal-nav" });
    const prev = nav.createDiv({ cls: "muse-cal-nav-btn" });
    (0, import_obsidian6.setIcon)(prev, "chevron-left");
    const title = nav.createDiv({ cls: "muse-cal-title" });
    const next = nav.createDiv({ cls: "muse-cal-nav-btn" });
    (0, import_obsidian6.setIcon)(next, "chevron-right");
    const grid = host.createDiv({ cls: "muse-cal-grid" });
    const dows = ["\u65E5", "\u4E00", "\u4E8C", "\u4E09", "\u56DB", "\u4E94", "\u516D"];
    const draw = () => {
      var _a2;
      const y = this.calViewMonth.getFullYear();
      const mo = this.calViewMonth.getMonth();
      title.setText(`${y}\u5E74${mo + 1}\u6708`);
      grid.empty();
      for (const w of dows)
        grid.createDiv({ cls: "muse-cal-dow", text: w });
      const first = new Date(y, mo, 1);
      const startDow = first.getDay();
      const days = new Date(y, mo + 1, 0).getDate();
      for (let i = 0; i < startDow; i++)
        grid.createDiv({ cls: "muse-cal-cell empty" });
      for (let d = 1; d <= days; d++) {
        const key = dateKey(new Date(y, mo, d));
        const cnt = (_a2 = counts.get(key)) != null ? _a2 : 0;
        const cell = grid.createDiv({
          cls: "muse-cal-cell" + (cnt > 0 ? " has" : "") + (key === todayKey ? " today" : "")
        });
        cell.createSpan({ cls: "muse-cal-day", text: String(d) });
        if (cnt > 0) {
          cell.addClass(`level-${cnt < 2 ? 1 : cnt < 4 ? 2 : cnt < 7 ? 3 : 4}`);
          cell.setAttr("title", `${key}  ${this.plugin.t("list.totalCount", { n: cnt })}`);
          cell.style.cursor = "pointer";
          cell.addEventListener("click", () => {
            this.filter.date = key;
            this.filter.preset = "all";
            this.pageLimit = this.getInitialPageLimit();
            this.renderList();
          });
        }
      }
      const trailing = Math.max(0, 42 - startDow - days);
      for (let i = 0; i < trailing; i++)
        grid.createDiv({ cls: "muse-cal-cell empty" });
    };
    draw();
    prev.addEventListener("click", () => {
      this.calViewMonth = new Date(
        this.calViewMonth.getFullYear(),
        this.calViewMonth.getMonth() - 1,
        1
      );
      draw();
    });
    next.addEventListener("click", () => {
      this.calViewMonth = new Date(
        this.calViewMonth.getFullYear(),
        this.calViewMonth.getMonth() + 1,
        1
      );
      draw();
    });
  }
  renderHeatmap(host, memos) {
    var _a, _b;
    const now = /* @__PURE__ */ new Date();
    now.setHours(0, 0, 0, 0);
    const dow = now.getDay();
    const start = new Date(now);
    start.setDate(now.getDate() - dow);
    const begin = new Date(start);
    begin.setDate(start.getDate() - 13 * 7);
    const counts = /* @__PURE__ */ new Map();
    for (const m of memos)
      counts.set(m.date, ((_a = counts.get(m.date)) != null ? _a : 0) + 1);
    const grid = host.createDiv({ cls: "muse-heatmap" });
    for (let col = 0; col < 14; col++) {
      const c = grid.createDiv({ cls: "muse-heatmap-col" });
      for (let row = 0; row < 7; row++) {
        const d = new Date(begin);
        d.setDate(begin.getDate() + col * 7 + row);
        const key = dateKey(d);
        const cnt = (_b = counts.get(key)) != null ? _b : 0;
        const level = cnt === 0 ? 0 : cnt < 2 ? 1 : cnt < 4 ? 2 : cnt < 7 ? 3 : 4;
        const cell = c.createDiv({
          cls: `muse-heatmap-cell level-${level}`
        });
        if (d > now)
          cell.addClass("future");
        if (cnt > 0) {
          cell.addEventListener(
            "mouseenter",
            () => this.showHeatmapTooltip(cell, key, memos.filter((m) => m.date === key))
          );
          cell.addEventListener("mouseleave", () => this.hideHeatmapTooltip());
          cell.addEventListener("click", () => {
            this.filter.date = key;
            this.filter.preset = "all";
            this.renderList();
          });
          cell.style.cursor = "pointer";
        } else {
          cell.setAttr("title", `${key}  ${this.plugin.t("list.totalCount", { n: cnt })}`);
        }
      }
    }
  }
  /**
   * 主区域热力图 —— 统计头部（固定在滚动容器之外，不随列表滚动消失）。
   * 承载：最近一年贡献 / 最长连续 / 最近连续 / 共 N 条 + 少/多图例 + 筛选操作按钮。
   * 数据来源：`plugin.store.getAll()`（与侧栏一致），不随列表 filter 变化，
   * 提供稳定全局活跃度；「共 N 条」用当前筛选结果数，随筛选实时变化。
   */
  renderMetaHeatmapHead(host, memos, totalLabel, actions) {
    host.empty();
    if (!memos.length) {
      host.addClass("is-empty");
      return;
    }
    host.removeClass("is-empty");
    const now = /* @__PURE__ */ new Date();
    now.setHours(0, 0, 0, 0);
    const cols = 52;
    const dow = now.getDay();
    const lastSunday = new Date(now);
    lastSunday.setDate(now.getDate() - dow);
    const begin = new Date(lastSunday);
    begin.setDate(lastSunday.getDate() - (cols - 1) * 7);
    const dateSet = /* @__PURE__ */ new Set();
    for (const m of memos)
      dateSet.add(m.date);
    let totalYear = 0;
    for (const m of memos) {
      const t = m.datetime.getTime();
      if (t >= begin.getTime() && t <= now.getTime() + 864e5)
        totalYear++;
    }
    const longest = this.calcLongestStreak(dateSet, begin, now);
    const current = this.calcCurrentStreak(dateSet, now);
    const leftWrap = host.createDiv({
      cls: "muse-list-heatmap-stats muse-list-heatmap-stats-right"
    });
    const renderStat = (key, n) => {
      const html = this.plugin.t(key, { n }).replace("<b>", '<b class="muse-list-heatmap-stat-num">');
      const span = leftWrap.createSpan({ cls: "muse-list-heatmap-stat" });
      span.innerHTML = html;
    };
    renderStat("meta.heatmapTotalYear", totalYear);
    renderStat("meta.heatmapLongest", longest);
    renderStat("meta.heatmapCurrent", current);
    if (actions == null ? void 0 : actions.reroll) {
      const reroll = leftWrap.createEl("button", { cls: "muse-meta-btn" });
      (0, import_obsidian6.setIcon)(reroll.createSpan(), "shuffle");
      reroll.createSpan({ text: this.plugin.t("meta.reroll") });
      reroll.addEventListener("click", () => {
        this.filter.randomSeed = Date.now();
        this.renderList();
      });
    }
    if (actions == null ? void 0 : actions.back) {
      const back = leftWrap.createEl("button", { cls: "muse-meta-btn" });
      (0, import_obsidian6.setIcon)(back.createSpan(), "history");
      back.createSpan({ text: this.plugin.t("meta.backToOnThisDay") });
      back.addEventListener("click", () => {
        this.filter.preset = "on-this-day";
        this.renderList();
      });
    }
    const countSpan = host.createSpan({ cls: "muse-list-heatmap-count" });
    countSpan.setText("\u{1F4CA} " + totalLabel);
  }
  /**
   * 主区域热力图 —— 网格（52 列 × 7 行绿系），作为列表滚动容器的首个子元素渲染，
   * 因此向上滚动时网格会随卡片一起滚走（统计头部已固定在上方、不滚动）。
   */
  renderMetaHeatmapGrid(host, memos) {
    var _a, _b;
    if (!memos.length)
      return;
    const now = /* @__PURE__ */ new Date();
    now.setHours(0, 0, 0, 0);
    const cols = 52;
    const dow = now.getDay();
    const lastSunday = new Date(now);
    lastSunday.setDate(now.getDate() - dow);
    const begin = new Date(lastSunday);
    begin.setDate(lastSunday.getDate() - (cols - 1) * 7);
    const counts = /* @__PURE__ */ new Map();
    for (const m of memos)
      counts.set(m.date, ((_a = counts.get(m.date)) != null ? _a : 0) + 1);
    const todayKey = dateKey(now);
    const grid = host.createDiv({ cls: "muse-heatmap muse-list-heatmap" });
    for (let col = 0; col < cols; col++) {
      const c = grid.createDiv({ cls: "muse-heatmap-col" });
      for (let row = 0; row < 7; row++) {
        const d = new Date(begin);
        d.setDate(begin.getDate() + col * 7 + row);
        const key = dateKey(d);
        const cnt = (_b = counts.get(key)) != null ? _b : 0;
        const level = cnt === 0 ? 0 : cnt < 2 ? 1 : cnt < 4 ? 2 : cnt < 7 ? 3 : 4;
        const cell = c.createDiv({
          cls: `muse-heatmap-cell muse-list-heatmap-cell level-${level}`
        });
        if (d > now)
          cell.addClass("future");
        if (key === todayKey)
          cell.addClass("today");
        if (cnt > 0) {
          cell.addEventListener(
            "mouseenter",
            () => this.showHeatmapTooltip(cell, key, memos.filter((m) => m.date === key))
          );
          cell.addEventListener("mouseleave", () => this.hideHeatmapTooltip());
          cell.style.cursor = "pointer";
        } else {
          cell.setAttr("title", `${key}  ${this.plugin.t("list.totalCount", { n: cnt })}`);
        }
        cell.addEventListener("click", () => {
          this.filter.date = key;
          this.filter.preset = "all";
          this.filter.tag = null;
          this.filter.year = null;
          this.filter.keyword = "";
          this.pageLimit = this.getInitialPageLimit();
          this.renderAll();
        });
      }
    }
  }
  /** 在 [begin, end] 窗口内（含两端）求最长连续贡献天数。 */
  calcLongestStreak(dateSet, begin, end) {
    let best = 0, cur = 0;
    const d = new Date(begin);
    while (d.getTime() <= end.getTime() + 864e5) {
      if (dateSet.has(dateKey(d))) {
        cur++;
        if (cur > best)
          best = cur;
      } else {
        cur = 0;
      }
      d.setDate(d.getDate() + 1);
    }
    return best;
  }
  /** 从 today 往前推连续贡献天数（含 today，若今日无贡献则不计入）。 */
  calcCurrentStreak(dateSet, today) {
    let n = 0;
    const d = new Date(today);
    while (dateSet.has(dateKey(d))) {
      n++;
      d.setDate(d.getDate() - 1);
    }
    return n;
  }
  showHeatmapTooltip(anchor, date, memos) {
    this.hideHeatmapTooltip();
    const tip = document.body.createDiv({ cls: "muse-heatmap-tooltip" });
    const head = tip.createDiv({ cls: "muse-heatmap-tooltip-head" });
    head.createSpan({ text: date });
    head.createSpan({
      cls: "muse-heatmap-tooltip-count",
      text: this.plugin.t("list.totalCount", { n: memos.length })
    });
    for (const m of memos.slice(0, 2)) {
      const row = tip.createDiv({ cls: "muse-heatmap-tooltip-row" });
      row.createSpan({ cls: "muse-heatmap-tooltip-time", text: m.time });
      const txt = m.content.replace(/!\[[^\]]*\]\([^)]+\)/g, this.plugin.t("list.imageHolder")).replace(/!\[\[[^\]]+\]\]/g, this.plugin.t("list.imageHolder")).replace(/#[^\s#]+/g, "").replace(/\s+/g, " ").trim().slice(0, 50);
      row.createSpan({
        cls: "muse-heatmap-tooltip-text",
        text: txt || this.plugin.t("list.noText")
      });
    }
    if (memos.length > 2)
      tip.createDiv({
        cls: "muse-heatmap-tooltip-more",
        text: this.plugin.t("list.heatmapMore", { n: memos.length - 2 })
      });
    const r = anchor.getBoundingClientRect();
    tip.style.position = "fixed";
    tip.style.left = `${Math.min(r.right + 8, window.innerWidth - 280)}px`;
    tip.style.top = `${Math.max(8, r.top - 4)}px`;
    tip.style.zIndex = "1000";
    this.heatmapTooltipEl = tip;
  }
  hideHeatmapTooltip() {
    if (this.heatmapTooltipEl) {
      this.heatmapTooltipEl.remove();
      this.heatmapTooltipEl = null;
    }
  }
  renderDailyGoal(host, memos, today, todayCount) {
    const goal = Math.max(1, this.plugin.settings.dailyGoal || 5);
    const pct = Math.min(100, Math.round(todayCount / goal * 100));
    const done = todayCount >= goal;
    const desc = done ? this.plugin.t("list.dailyGoalExceed", {
      goal,
      done: todayCount,
      extra: todayCount - goal
    }) : this.plugin.t("list.dailyGoalDone", { goal, done: todayCount });
    const row = host.createDiv({
      cls: `muse-daily-goal-row${done ? " is-done" : ""}`
    });
    const bar = row.createDiv({
      cls: "muse-daily-goal",
      attr: { "aria-label": desc }
    });
    bar.addEventListener("click", () => {
      this.filter.preset = "today";
      this.filter.tag = null;
      this.filter.date = null;
      this.pageLimit = this.getInitialPageLimit();
      this.renderAll();
    });
    const fill = bar.createDiv({ cls: "muse-daily-goal-bar" });
    fill.createDiv({ cls: "muse-daily-goal-fill" });
    fill.firstChild.style.width = `${pct}%`;
  }
  /* ----------------------------- 标签树 ----------------------------- */
  buildTagTree(counts) {
    const root = {
      name: "",
      full: "",
      count: 0,
      self: 0,
      children: /* @__PURE__ */ new Map()
    };
    for (const [tag, count] of counts) {
      const parts = tag.split("/");
      let node = root;
      let full = "";
      for (const p of parts) {
        full = full ? `${full}/${p}` : p;
        if (!node.children.has(p))
          node.children.set(p, {
            name: p,
            full,
            count: 0,
            self: 0,
            children: /* @__PURE__ */ new Map()
          });
        node = node.children.get(p);
      }
      node.self += count;
    }
    this.sumTag(root);
    return root;
  }
  sumTag(node) {
    let total = node.self;
    for (const c of node.children.values())
      total += this.sumTag(c);
    node.count = total;
    return total;
  }
  renderTagTree(host, node, depth) {
    const children = [...node.children.values()].sort(
      (a, b) => b.count - a.count
    );
    for (const c of children) {
      const wrapper = host.createDiv({ cls: "muse-tag-node" });
      const item = wrapper.createDiv({
        cls: "muse-nav-item muse-tag-item" + (this.filter.tag === c.full ? " active" : "")
      });
      item.style.paddingLeft = `${12 + depth * 14}px`;
      item.createDiv({ cls: "muse-nav-icon" }).setText("#");
      item.createSpan({ cls: "muse-nav-text", text: c.name });
      item.createSpan({ cls: "muse-nav-count", text: String(c.count) });
      item.addEventListener("click", () => {
        this.filter.tag = this.filter.tag === c.full ? null : c.full;
        this.filter.preset = "all";
        this.pageLimit = this.getInitialPageLimit();
        this.renderAll();
      });
      if (c.children.size)
        this.renderTagTree(wrapper, c, depth + 1);
    }
  }
  /** 标签云：按出现频率缩放字号，横向流式排列；点击即筛选（含层级子标签）。 */
  renderTagCloud(host, counts) {
    const entries = [...counts.entries()].sort(
      (a, b) => b[1] - a[1] || a[0].localeCompare(b[0], "zh")
    );
    if (entries.length === 0)
      return;
    const vals = entries.map((e) => e[1]);
    const min = Math.min(...vals);
    const max = Math.max(...vals);
    const minSize = 0.78;
    const maxSize = 1.95;
    const denom = max === min ? 0 : Math.log(max + 1) - Math.log(min + 1) || 1;
    const cloud = host.createDiv({ cls: "muse-tag-cloud" });
    for (const [tag, count] of entries) {
      const size = denom === 0 ? (minSize + maxSize) / 2 : minSize + (maxSize - minSize) * (Math.log(count + 1) - Math.log(min + 1)) / denom;
      const item = cloud.createDiv({
        cls: "muse-tag-cloud-item" + (this.filter.tag === tag ? " active" : "")
      });
      item.style.fontSize = `${size.toFixed(2)}em`;
      item.createSpan({ cls: "muse-tag-cloud-hash", text: "#" });
      item.createSpan({ cls: "muse-tag-cloud-name", text: tag });
      item.setAttribute("title", `${tag} \xB7 ${count}`);
      item.addEventListener("click", () => {
        this.filter.tag = this.filter.tag === tag ? null : tag;
        this.filter.preset = "all";
        this.pageLimit = this.getInitialPageLimit();
        this.renderAll();
      });
    }
  }
  /* ----------------------------- 列表 / 卡片 ----------------------------- */
  getFilteredMemos() {
    var _a, _b, _c;
    const all = this.plugin.store.getAll();
    const kw = this.filter.keyword.toLowerCase();
    let list = all.filter((m) => {
      if (this.filter.year && !m.date.startsWith(this.filter.year))
        return false;
      if (this.filter.date && m.date !== this.filter.date)
        return false;
      if (this.filter.tag && !m.tags.some(
        (t) => t === this.filter.tag || t.startsWith(`${this.filter.tag}/`)
      ))
        return false;
      if (kw) {
        const hay = `${m.content} ${m.tags.join(" ")} ${m.date}`.toLowerCase();
        if (!hay.includes(kw))
          return false;
      }
      return true;
    });
    const today = dateKey(/* @__PURE__ */ new Date());
    if (this.filter.preset === "today")
      list = list.filter((m) => m.date === today);
    else if (this.filter.preset === "week")
      list = list.filter((m) => m.datetime.getTime() >= startOfWeek());
    else if (this.filter.preset === "on-this-day") {
      const md = today.slice(5);
      list = list.filter((m) => m.date.slice(5) === md && m.date !== today);
    } else if (this.filter.preset === "no-tag")
      list = list.filter(
        (m) => m.tags.filter((t) => !SYSTEM_TAGS.includes(t)).length === 0
      );
    else if (this.filter.preset === "with-image")
      list = list.filter((m) => m.hasImage);
    else if (this.filter.preset === "with-link")
      list = list.filter((m) => m.hasLink);
    else if (this.filter.preset === "pinned")
      list = list.filter((m) => m.isPinned);
    else if (this.filter.preset === "starred")
      list = list.filter((m) => m.isStarred);
    else if (this.filter.preset === "todo")
      list = list.filter((m) => m.hasOpenTask);
    else if (this.filter.preset === "random" && list.length) {
      const count = (_a = this.filter.randomCount) != null ? _a : 5;
      if (this.plugin.settings.enableSmartReview) {
        list = this.smartPick(list, (_b = this.filter.randomSeed) != null ? _b : 1, count);
      } else {
        const seed = (_c = this.filter.randomSeed) != null ? _c : 1;
        list = shuffle(list, seed).slice(0, Math.min(count, list.length));
      }
    }
    return list;
  }
  memoId(m) {
    return `${m.file.path}#${m.range[0]}`;
  }
  loadLastSeen() {
    try {
      const raw = window.localStorage.getItem("muse-lastseen");
      return raw ? JSON.parse(raw) : {};
    } catch (e) {
      return {};
    }
  }
  saveLastSeen(map) {
    try {
      window.localStorage.setItem("muse-lastseen", JSON.stringify(map));
    } catch (e) {
    }
  }
  /** 智能回顾：加权挑选 5 条（越久没翻过越优先、与今天标签/情绪呼应加分）。 */
  smartPick(pool, seed, count = 5) {
    const today = dateKey(/* @__PURE__ */ new Date());
    const todayTags = /* @__PURE__ */ new Set();
    const todayMoods = /* @__PURE__ */ new Set();
    for (const m of this.plugin.store.getAll()) {
      if (m.date !== today)
        continue;
      for (const t of m.tags)
        if (!SYSTEM_TAGS.includes(t))
          todayTags.add(t);
      const mood = this.detectMood(m.content);
      if (mood)
        todayMoods.add(mood);
    }
    const now = Date.now();
    const seen = this.loadLastSeen();
    const scored = pool.map((m) => {
      var _a;
      const last = (_a = seen[this.memoId(m)]) != null ? _a : 0;
      const days = last ? (now - last) / 864e5 : 30;
      let score = Math.min(days, 60) / 60;
      for (const t of m.tags)
        if (todayTags.has(t))
          score += 0.5;
      const mood = this.detectMood(m.content);
      if (mood && todayMoods.has(mood))
        score += 0.5;
      return { m, score: score + 0.05 };
    });
    const chosen = [];
    const bag = scored.slice();
    const n = Math.min(count, bag.length);
    let rng = seed >>> 0;
    for (let i = 0; i < n; i++) {
      const total = bag.reduce((s, x) => s + x.score, 0);
      rng = rng * 1103515245 + 12345 & 2147483647;
      let r = rng / 2147483647 * total;
      let idx = 0;
      for (; idx < bag.length - 1; idx++) {
        r -= bag[idx].score;
        if (r <= 0)
          break;
      }
      chosen.push(bag[idx].m);
      bag.splice(idx, 1);
    }
    for (const m of chosen)
      seen[this.memoId(m)] = now;
    this.saveLastSeen(seen);
    return chosen;
  }
  renderList() {
    var _a;
    this.listEl.empty();
    this.childComponent.unload();
    this.childComponent = new import_obsidian6.Component();
    this.childComponent.load();
    this.listEl.toggleClass(
      "is-compact",
      this.plugin.settings.density === "compact"
    );
    const filtered = this.getFilteredMemos();
    this.metaHeatmapHeadEl.removeClass("is-hidden");
    this.renderMetaHeatmapHead(
      this.metaHeatmapHeadEl,
      this.plugin.store.getAll(),
      this.describeFilter(filtered.length),
      {
        reroll: this.filter.preset === "random",
        back: this.filter.preset === "on-this-day"
      }
    );
    if (this.plugin.settings.showHeatmap) {
      this.renderMetaHeatmapGrid(this.listEl, this.plugin.store.getAll());
    }
    if (filtered.length === 0) {
      this.renderEmpty(this.listEl);
      return;
    }
    const slice = filtered.slice(0, this.pageLimit);
    const pinned = slice.filter((m) => m.isPinned);
    const rest = slice.filter((m) => !m.isPinned);
    if (pinned.length) {
      const group = this.listEl.createDiv({
        cls: "muse-day-group muse-pin-group"
      });
      const head = group.createDiv({
        cls: "muse-day-head muse-pin-head"
      });
      const pic = head.createSpan({ cls: "muse-pin-head-icon" });
      (0, import_obsidian6.setIcon)(pic, "pin");
      head.createSpan({
        text: this.plugin.t("list.pinnedHead", { n: pinned.length })
      });
      for (const m of pinned)
        this.renderMemoCard(group, m);
    }
    const byDate = /* @__PURE__ */ new Map();
    for (const m of rest) {
      const arr = (_a = byDate.get(m.date)) != null ? _a : [];
      arr.push(m);
      byDate.set(m.date, arr);
    }
    const today = dateKey(/* @__PURE__ */ new Date());
    const yest = dateKey(new Date(Date.now() - 864e5));
    for (const [date, arr] of byDate) {
      const group = this.listEl.createDiv({ cls: "muse-day-group" });
      group.dataset.date = date;
      const head = group.createDiv({ cls: "muse-day-head" });
      const d = /* @__PURE__ */ new Date(`${date}T00:00:00`);
      let label = `${date}  ${this.plugin.t(`weekday.${d.getDay()}`)}`;
      if (date === today)
        label = `${this.plugin.t("date.today")}  ${this.plugin.t(`weekday.${d.getDay()}`)}`;
      else if (date === yest)
        label = `${this.plugin.t("date.yesterday")}  ${this.plugin.t(`weekday.${d.getDay()}`)}`;
      head.setText(label);
      for (const m of arr)
        this.renderMemoCard(group, m);
    }
    if (this.pageLimit < filtered.length) {
      const more = this.listEl.createDiv({ cls: "muse-load-more" });
      more.setText(
        this.plugin.t("list.loadMore", {
          n: filtered.length - this.pageLimit
        })
      );
      more.addEventListener("click", () => this.loadMore());
    }
  }
  /** 加载下一页：按 pageSize 步长扩大 pageLimit，渲染后保留当前滚动位置。 */
  loadMore() {
    if (this.loadingMore)
      return;
    const filtered = this.getFilteredMemos();
    if (this.pageLimit >= filtered.length)
      return;
    this.loadingMore = true;
    const el = this.listEl;
    const prevScrollTop = el.scrollTop;
    const step = Math.max(10, this.plugin.settings.pageSize || 50);
    this.pageLimit = Math.min(filtered.length, this.pageLimit + step);
    this.renderList();
    requestAnimationFrame(() => {
      el.scrollTop = prevScrollTop;
      this.loadingMore = false;
    });
  }
  renderEmpty(host) {
    const empty = host.createDiv({ cls: "muse-empty" });
    if (this.filter.preset === "on-this-day") {
      empty.createDiv({ cls: "muse-empty-emoji", text: "\u{1F570}\uFE0F" });
      empty.createDiv({ cls: "muse-empty-text", text: this.plugin.t("empty.onThisDay") });
      empty.createDiv({ cls: "muse-empty-sub", text: this.plugin.t("empty.onThisDaySub") });
      const btn = empty.createEl("button", { cls: "muse-empty-btn" });
      (0, import_obsidian6.setIcon)(btn.createSpan(), "shuffle");
      btn.createSpan({ text: this.plugin.t("empty.onThisDayBtn") });
      btn.addEventListener("click", () => {
        this.filter.preset = "random";
        this.filter.randomSeed = Date.now();
        this.filter.randomCount = 1;
        this.renderList();
      });
      return;
    }
    if (this.filter.preset === "todo") {
      empty.createDiv({ cls: "muse-empty-emoji", text: "\u{1F389}" });
      empty.createDiv({ cls: "muse-empty-text", text: this.plugin.t("empty.todo") });
      empty.createDiv({ cls: "muse-empty-sub", text: this.plugin.t("empty.todoSub") });
      return;
    }
    empty.createDiv({ cls: "muse-empty-emoji", text: "\u{1F4ED}" });
    empty.createDiv({ cls: "muse-empty-text", text: this.plugin.t("empty.default") });
    empty.createDiv({ cls: "muse-empty-sub", text: this.plugin.t("empty.defaultSub") });
  }
  /** 基于关键词词典检测情绪维度（返回 CSS 类名后缀，无匹配返回 null）。 */
  detectMood(text) {
    for (const [mood, kws] of Object.entries(_MuseView.MOOD_DICT)) {
      for (const kw of kws) {
        if (text.includes(kw))
          return mood;
      }
    }
    return null;
  }
  renderMemoCard(host, memo) {
    const mood = this.plugin.settings.enableMoodColoring ? this.detectMood(memo.content) : null;
    const card = host.createDiv({
      cls: "muse-card" + (mood ? ` muse-mood-${mood}` : "") + (memo.isPinned ? " is-pinned" : "") + (memo.isStarred ? " is-starred" : "") + (this.editingMemo === memo ? " is-editing" : ""),
      attr: { "data-memo-key": this.memoKey(memo) }
    });
    card.addEventListener("dblclick", (e) => {
      const tgt = e.target;
      if (!tgt.closest(".muse-img-cell") && tgt.tagName !== "A")
        this.enterEditMode(memo);
    });
    const head = card.createDiv({ cls: "muse-card-head" });
    const timeWrap = head.createDiv({ cls: "muse-card-time-wrap" });
    if (memo.isPinned) {
      const pin = timeWrap.createSpan({ cls: "muse-card-pin" });
      (0, import_obsidian6.setIcon)(pin, "pin");
      pin.setAttr("aria-label", this.plugin.t("card.pinnedMark"));
    }
    if (memo.isStarred) {
      const star = timeWrap.createSpan({ cls: "muse-card-star" });
      (0, import_obsidian6.setIcon)(star, "star");
      star.setAttr("aria-label", this.plugin.t("card.starredMark"));
    }
    timeWrap.createSpan({
      cls: "muse-card-time",
      text: `${memo.date} ${memo.time}`
    });
    const actions = head.createDiv({ cls: "muse-card-actions" });
    const quoteBtn = actions.createEl("button", {
      cls: "muse-icon-btn muse-card-quote",
      attr: { "aria-label": this.plugin.t("toolbar.quote") }
    });
    (0, import_obsidian6.setIcon)(quoteBtn, "quote");
    quoteBtn.addEventListener("click", (e) => {
      e.stopPropagation();
      this.quoteMemo(memo);
    });
    const moreBtn = actions.createEl("button", {
      cls: "muse-icon-btn",
      attr: { "aria-label": this.plugin.t("toolbar.more") }
    });
    (0, import_obsidian6.setIcon)(moreBtn, "more-horizontal");
    moreBtn.addEventListener("click", (e) => {
      e.stopPropagation();
      this.showMemoMenu(e, memo);
    });
    const { text: stripped, tags } = stripInlineTags(memo.content);
    const { text: body, images } = extractMemoImages(
      this.app,
      stripped,
      memo.file.path
    );
    if (body.trim()) {
      const bodyEl = card.createDiv({ cls: "muse-card-body" });
      import_obsidian6.MarkdownRenderer.render(
        this.app,
        body,
        bodyEl,
        memo.file.path,
        this.childComponent
      );
      this.bindInternalLinks(bodyEl, memo);
      const limit = this.plugin.settings.collapseLineLimit;
      if (limit > 0)
        this.maybeCollapse(bodyEl, limit);
    }
    if (images.length)
      this.renderImageGrid(card, images);
    const visibleTags = tags.filter((t) => !SYSTEM_TAGS.includes(t));
    if (visibleTags.length) {
      const tagEl = card.createDiv({ cls: "muse-card-tags" });
      for (const tg of visibleTags) {
        const pill = tagEl.createSpan({
          cls: "muse-tag-pill",
          text: `#${tg}`
        });
        pill.addEventListener("click", () => {
          this.filter.tag = tg;
          this.filter.preset = "all";
          this.pageLimit = this.getInitialPageLimit();
          this.renderAll();
        });
      }
    }
  }
  /** 长笔记自动折叠：正文视觉高度超过 collapseLineLimit 行时截断，并加「展开/收起」按钮。
   *  复用 styles.css 的 .is-collapsed（max-height + 渐变遮罩）与 .muse-collapse-toggle 交互。 */
  maybeCollapse(bodyEl, limit) {
    requestAnimationFrame(() => {
      if (!bodyEl.isConnected)
        return;
      if (this.countVisualLines(bodyEl) <= limit)
        return;
      this.applyCollapse(bodyEl, limit);
    });
  }
  /** 对某个正文应用折叠态：设置折叠高度、加遮罩类、插入「展开/收起」按钮。
   *  供初次渲染（maybeCollapse）与窗口尺寸变化时的重新评估复用。 */
  applyCollapse(bodyEl, limit) {
    if (bodyEl.classList.contains("has-toggle"))
      return;
    const cs = getComputedStyle(bodyEl);
    const lh = parseFloat(cs.lineHeight);
    if (!isFinite(lh) || lh <= 0)
      return;
    const maxH = lh * limit;
    bodyEl.style.maxHeight = "";
    bodyEl.style.setProperty("--muse-collapse-max", `${maxH}px`);
    bodyEl.addClass("is-collapsed");
    bodyEl.addClass("has-toggle");
    const btn = bodyEl.createDiv({ cls: "muse-collapse-toggle" });
    const icon = btn.createSpan({ cls: "muse-collapse-icon" });
    (0, import_obsidian6.setIcon)(icon, "chevron-down");
    btn.setAttribute("aria-label", this.plugin.t("card.collapseFull"));
    btn.addEventListener("click", (e) => {
      e.stopPropagation();
      if (bodyEl.classList.contains("is-collapsed")) {
        bodyEl.style.maxHeight = `${maxH}px`;
        bodyEl.removeClass("is-collapsed");
        void bodyEl.offsetHeight;
        bodyEl.style.maxHeight = bodyEl.scrollHeight + "px";
        (0, import_obsidian6.setIcon)(icon, "chevron-up");
        btn.setAttribute("aria-label", this.plugin.t("card.collapseLess"));
        bodyEl.dataset.userExpanded = "1";
        const onEnd = () => {
          bodyEl.removeEventListener("transitionend", onEnd);
          if (!bodyEl.classList.contains("is-collapsed"))
            bodyEl.style.maxHeight = "none";
        };
        bodyEl.addEventListener("transitionend", onEnd);
      } else {
        bodyEl.style.maxHeight = bodyEl.scrollHeight + "px";
        void bodyEl.offsetHeight;
        bodyEl.style.maxHeight = `${maxH}px`;
        bodyEl.addClass("is-collapsed");
        (0, import_obsidian6.setIcon)(icon, "chevron-down");
        btn.setAttribute("aria-label", this.plugin.t("card.collapseFull"));
        delete bodyEl.dataset.userExpanded;
      }
    });
  }
  /** 撤销折叠态：移除遮罩类、渐变、内联高度，并删掉折叠按钮。
   *  用于窗口变宽后内容已能完整显示的情形（无论此前是折叠还是用户手动展开）。 */
  clearCollapse(bodyEl) {
    var _a;
    bodyEl.classList.remove("is-collapsed", "has-toggle");
    bodyEl.style.maxHeight = "";
    bodyEl.style.removeProperty("--muse-collapse-max");
    delete bodyEl.dataset.userExpanded;
    (_a = bodyEl.querySelector(".muse-collapse-toggle")) == null ? void 0 : _a.remove();
  }
  /** 窗口尺寸变化后，重新评估所有卡片正文的折叠状态。
   *  - 内容已能完整显示（视觉行数 <= 限制）：撤掉折叠态与按钮。
   *  - 内容仍超出且非用户手动展开：重新折叠（窗口变窄时）。
   *  已手动展开过的卡片（dataset.userExpanded）保持展开，尊重用户选择。 */
  recomputeCollapse() {
    const limit = this.plugin.settings.collapseLineLimit;
    if (limit <= 0)
      return;
    const bodies = this.containerEl.querySelectorAll(".muse-card-body");
    bodies.forEach((body) => {
      if (!body.isConnected)
        return;
      const lines = this.countVisualLines(body);
      if (lines <= limit) {
        this.clearCollapse(body);
      } else if (body.dataset.userExpanded !== "1" && !body.classList.contains("is-collapsed")) {
        this.applyCollapse(body, limit);
      }
    });
  }
  /** 统计元素内文本的视觉行数：遍历文本节点，用 Range.getClientRects 取得每行矩形，
   *  按行的纵向坐标（四舍五入）去重，得到真实视觉行数。不受 inline 元素拆分
   *  （如 <strong> 包裹）与 markdown 块间距影响，比 scrollHeight/lineHeight 更贴合直觉。 */
  countVisualLines(el) {
    const tops = /* @__PURE__ */ new Set();
    const walk = (node) => {
      var _a;
      if (node.nodeType === Node.TEXT_NODE) {
        const text = (_a = node.textContent) != null ? _a : "";
        if (!text.trim())
          return;
        const range = document.createRange();
        try {
          range.selectNodeContents(node);
        } catch (e) {
          return;
        }
        const rects = range.getClientRects();
        for (let i = 0; i < rects.length; i++) {
          if (rects[i].height > 0)
            tops.add(Math.round(rects[i].top));
        }
      } else if (node.nodeType === Node.ELEMENT_NODE) {
        node.childNodes.forEach(walk);
      }
    };
    el.childNodes.forEach(walk);
    return tops.size;
  }
  renderImageGrid(host, images) {
    if (images.length === 0)
      return;
    const grid = host.createDiv({
      cls: `muse-img-grid muse-img-grid-${Math.min(images.length, 9)}`
    });
    images.slice(0, 9).forEach((img, i) => {
      const cell = grid.createDiv({ cls: "muse-img-cell" });
      const el = cell.createEl("img", {
        cls: "muse-img",
        attr: { src: img.src, alt: img.alt, loading: "lazy" }
      });
      el.addEventListener("click", (e) => {
        e.stopPropagation();
        this.openLightbox(images, i);
      });
      if (i === 8 && images.length > 9) {
        const overlay = cell.createDiv({ cls: "muse-img-overlay" });
        overlay.setText(`+${images.length - 9}`);
        overlay.addEventListener("click", (e) => {
          e.stopPropagation();
          this.openLightbox(images, 8);
        });
      }
    });
  }
  openLightbox(images, index) {
    new LightboxModal(this.app, images, index).open();
  }
  bindInternalLinks(host, memo) {
    host.addEventListener("click", (e) => {
      const tgt = e.target;
      const internal = tgt.closest("a.internal-link");
      if (internal) {
        e.preventDefault();
        e.stopPropagation();
        const href = internal.getAttribute("data-href") || internal.getAttribute("href") || "";
        if (!href)
          return;
        const mod = e.ctrlKey || e.metaKey || e.button === 1;
        this.app.workspace.openLinkText(href, memo.file.path, mod);
        return;
      }
      const tagLink = tgt.closest("a.tag");
      if (tagLink) {
        e.preventDefault();
        e.stopPropagation();
        const href = (tagLink.getAttribute("href") || "").replace(/^#/, "");
        if (!href)
          return;
        this.filter.tag = href;
        this.filter.preset = "all";
        this.pageLimit = this.getInitialPageLimit();
        this.renderAll();
      }
    });
  }
  /* ----------------------------- 卡片菜单 / 操作 ----------------------------- */
  showMemoMenu(e, memo) {
    const menu = new import_obsidian6.Menu();
    menu.addItem(
      (item) => item.setTitle(memo.isPinned ? this.plugin.t("card.unpin") : this.plugin.t("card.pin")).setIcon(memo.isPinned ? "pin-off" : "pin").onClick(async () => {
        await this.plugin.store.togglePinned(memo);
        new import_obsidian6.Notice(memo.isPinned ? this.plugin.t("notice.unpinned") : this.plugin.t("notice.pinned"));
      })
    );
    menu.addItem(
      (item) => item.setTitle(memo.isStarred ? this.plugin.t("card.unstar") : this.plugin.t("card.star")).setIcon(memo.isStarred ? "star-off" : "star").onClick(async () => {
        await this.plugin.store.toggleStarred(memo);
        new import_obsidian6.Notice(memo.isStarred ? this.plugin.t("notice.unstarred") : this.plugin.t("notice.starred"));
      })
    );
    menu.addSeparator();
    menu.addItem(
      (item) => item.setTitle(this.plugin.t("card.edit")).setIcon("pencil").onClick(() => this.enterEditMode(memo))
    );
    menu.addItem(
      (item) => item.setTitle(this.plugin.t("card.openSource")).setIcon("file-text").onClick(() => this.openInFile(memo))
    );
    menu.addItem(
      (item) => item.setTitle(this.plugin.t("card.copySource")).setIcon("copy").onClick(async () => {
        await navigator.clipboard.writeText(memo.content);
        new import_obsidian6.Notice(this.plugin.t("notice.copied"));
      })
    );
    menu.addItem(
      (item) => item.setTitle(this.plugin.t("card.exportImage")).setIcon("image-file").onClick(async () => {
        await openShareImage(this.plugin, memo);
      })
    );
    menu.addSeparator();
    menu.addItem(
      (item) => item.setTitle(this.plugin.t("card.delete")).setIcon("trash").onClick(async () => {
        if (await this.confirmAsync(this.plugin.t("notice.confirmDelete"))) {
          await this.plugin.store.deleteMemo(memo);
        }
      })
    );
    menu.showAtMouseEvent(e);
  }
  async openInFile(memo) {
    const leaf = this.app.workspace.getLeaf("tab");
    await leaf.openFile(memo.file);
  }
  quoteMemo(memo) {
    const quote = `> ${memo.content.replace(/\n/g, "\n> ")}`;
    const cur = this.editor.getValue();
    const prefix = cur ? "\n\n" : "";
    this.editor.setValue(cur + prefix + quote);
    this.editor.focus();
    this.autoResizeInput();
    this.syncInputCardContentState();
  }
  describeFilter(total) {
    var _a;
    const parts = [];
    const names = {
      today: this.plugin.t("sidebar.today"),
      week: this.plugin.t("sidebar.week"),
      "on-this-day": this.plugin.t("list.presetOnThisDay"),
      "no-tag": this.plugin.t("sidebar.noTag"),
      "with-image": this.plugin.t("sidebar.withImage"),
      "with-link": this.plugin.t("sidebar.withLink"),
      pinned: this.plugin.t("list.presetPinned"),
      starred: this.plugin.t("list.presetStarred"),
      todo: this.plugin.t("sidebar.todo")
    };
    if (this.filter.preset !== "all")
      parts.push((_a = names[this.filter.preset]) != null ? _a : this.filter.preset);
    if (this.filter.year)
      parts.push(this.filter.year);
    if (this.filter.date)
      parts.push(`\u{1F4C5} ${this.filter.date}`);
    if (this.filter.tag)
      parts.push(`#${this.filter.tag}`);
    if (this.filter.keyword)
      parts.push(`\u300C${this.filter.keyword}\u300D`);
    return `${parts.length ? parts.join(" \xB7 ") + " \xB7 " : ""}${this.plugin.t("list.totalCount", { n: total })}`;
  }
  /** 表格选择器（简化：插入一个 2x2 表格占位）。 */
  showTablePicker() {
    const rows = ["| \u52171 | \u52172 |", "| --- | --- |", "|  |  |", "|  |  |"].join("\n");
    this.insertAtCursor(`
${rows}
`);
  }
  /** 自建的确认弹窗：直接 createDiv 到 document.body，
   *  无 Obsidian X 关闭按钮，Esc=取消 Enter=确认，点击外部也关闭）。 */
  confirmAsync(message) {
    return new Promise((resolve) => {
      const backdrop = document.body.createDiv({ cls: "muse-modal-backdrop" });
      const modal = backdrop.createDiv({ cls: "muse-modal muse-confirm" });
      modal.createDiv({ cls: "muse-modal-title", text: message });
      const btns = modal.createDiv({ cls: "muse-modal-btns" });
      const cancelBtn = btns.createEl("button", { text: this.plugin.t("input.cancel") });
      const okBtn = btns.createEl("button", {
        text: this.plugin.t("notice.confirmDeleteOk"),
        cls: "mod-warning"
      });
      let settled = false;
      let pendingMouseUp = null;
      const clearPendingMouseUp = () => {
        if (pendingMouseUp) {
          document.removeEventListener("mouseup", pendingMouseUp, true);
          pendingMouseUp = null;
        }
      };
      this.register(() => {
        if (!settled) {
          settled = true;
          backdrop.remove();
          document.removeEventListener("keydown", onKey, true);
          clearPendingMouseUp();
          setTimeout(() => resolve(false), 0);
        }
      });
      const settle = (v) => {
        if (!settled) {
          settled = true;
          backdrop.remove();
          document.removeEventListener("keydown", onKey, true);
          clearPendingMouseUp();
          setTimeout(() => resolve(v), 0);
        }
      };
      const onKey = (ev) => {
        if (ev.key === "Escape") {
          ev.preventDefault();
          settle(false);
        } else if (ev.key === "Enter") {
          ev.preventDefault();
          settle(true);
        }
      };
      cancelBtn.addEventListener("click", () => settle(false));
      okBtn.addEventListener("click", () => settle(true));
      backdrop.addEventListener("mousedown", (downEv) => {
        if (downEv.target !== backdrop)
          return;
        clearPendingMouseUp();
        const onUp = (upEv) => {
          document.removeEventListener("mouseup", onUp, true);
          pendingMouseUp = null;
          if (upEv.target === backdrop)
            settle(false);
        };
        pendingMouseUp = onUp;
        document.addEventListener("mouseup", onUp, true);
      });
      document.addEventListener("keydown", onKey, true);
      setTimeout(() => okBtn.focus(), 20);
    });
  }
};
_MuseView.MOOD_DICT = {
  happy: ["\u5F00\u5FC3", "\u9AD8\u5174", "\u5FEB\u4E50", "\u54C8\u54C8", "\u592A\u597D\u4E86", "\u6B23\u559C", "\u6109\u60A6", "\u723D", "\u8D5E", "\u559C\u6B22", "\u7231\u4E86", "\u5E78\u798F", "\u7F8E\u6ECB\u6ECB"],
  touched: ["\u611F\u52A8", "\u6CEA\u76EE", "\u7834\u9632", "\u6696", "\u6E29\u6696", "\u611F\u4EBA", "\u6CEA", "\u5FC3\u75BC", "\u89E6\u52A8"],
  inspired: ["\u9F13\u52B1", "\u52A0\u6CB9", "\u52AA\u529B", "\u575A\u6301", "\u51B2", "\u71C3", "\u6FC0\u52B1", "\u4FE1\u5FC3", "\u5E0C\u671B", "\u6253\u6C14", "\u594B\u6597"],
  sad: ["\u96BE\u8FC7", "\u4F24\u5FC3", "\u5931\u843D", "\u6CAE\u4E27", "\u90C1\u95F7", "\u4E27", "\u4F4E\u843D", "\u54ED", "\u59D4\u5C48", "\u5B64\u72EC"],
  angry: ["\u70E6", "\u6C14", "\u6012", "\u66B4\u8E81", "\u5D29\u6E83", "\u70E6\u4EBA", "\u8BA8\u538C", "\u65E0\u8BED", "\u8349", "\u70E6\u6B7B\u4E86", "\u6C14\u6B7B"],
  fear: ["\u6015", "\u5BB3\u6015", "\u6050\u60E7", "\u62C5\u5FC3", "\u7126\u8651", "\u614C", "\u7D27\u5F20", "\u6015\u4E86"],
  tired: ["\u7D2F", "\u56F0", "\u75B2\u60EB", "\u5026", "\u4E4F", "\u60F3\u7761", "\u778C\u7761", "\u75B2\u52B3", "\u5026\u4E86"]
};
var MuseView = _MuseView;
function debounce(fn, ms) {
  let timer;
  return () => {
    if (timer)
      window.clearTimeout(timer);
    timer = window.setTimeout(fn, ms);
  };
}
function dateKey(d) {
  const y = d.getFullYear();
  const m = String(d.getMonth() + 1).padStart(2, "0");
  const day = String(d.getDate()).padStart(2, "0");
  return `${y}-${m}-${day}`;
}
function startOfWeek() {
  const now = /* @__PURE__ */ new Date();
  const dow = (now.getDay() + 6) % 7;
  const d = new Date(now);
  d.setDate(now.getDate() - dow);
  d.setHours(0, 0, 0, 0);
  return d.getTime();
}
function shuffle(arr, seed) {
  let s = seed >>> 0;
  const rand = () => {
    s = s + 1831565813 >>> 0;
    let x = s;
    x = Math.imul(x ^ x >>> 15, x | 1);
    x ^= x + Math.imul(x ^ x >>> 7, x | 61);
    return ((x ^ x >>> 14) >>> 0) / 4294967296;
  };
  const out = [...arr];
  for (let i = out.length - 1; i > 0; i--) {
    const j = Math.floor(rand() * (i + 1));
    [out[i], out[j]] = [out[j], out[i]];
  }
  return out;
}
var LightboxModal = class extends import_obsidian6.Modal {
  constructor(app, images, index) {
    super(app);
    this.images = images;
    this.index = index;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.addClass("muse-lightbox");
    const stage = contentEl.createDiv({ cls: "muse-lightbox-stage" });
    this.imgEl = stage.createEl("img", { cls: "muse-lightbox-img" });
    this.counterEl = contentEl.createDiv({ cls: "muse-lightbox-counter" });
    const close = contentEl.createEl("button", {
      cls: "muse-lightbox-close",
      text: "\xD7",
      attr: { "aria-label": "\u5173\u95ED" }
    });
    this.prevBtn = contentEl.createEl("button", {
      cls: "muse-lightbox-nav muse-lightbox-prev",
      text: "\u2039",
      attr: { "aria-label": "\u4E0A\u4E00\u5F20" }
    });
    this.nextBtn = contentEl.createEl("button", {
      cls: "muse-lightbox-nav muse-lightbox-next",
      text: "\u203A",
      attr: { "aria-label": "\u4E0B\u4E00\u5F20" }
    });
    const update = () => {
      this.imgEl.src = this.images[this.index].src;
      this.imgEl.alt = this.images[this.index].alt;
      this.counterEl.setText(`${this.index + 1} / ${this.images.length}`);
      this.prevBtn.style.visibility = this.index > 0 ? "visible" : "hidden";
      this.nextBtn.style.visibility = this.index < this.images.length - 1 ? "visible" : "hidden";
    };
    update();
    const prev = () => {
      if (this.index > 0) {
        this.index--;
        update();
      }
    };
    const next = () => {
      if (this.index < this.images.length - 1) {
        this.index++;
        update();
      }
    };
    close.addEventListener("click", () => this.close());
    this.prevBtn.addEventListener("click", (e) => {
      e.stopPropagation();
      prev();
    });
    this.nextBtn.addEventListener("click", (e) => {
      e.stopPropagation();
      next();
    });
    contentEl.addEventListener("click", (e) => {
      if (e.target === contentEl || e.target === stage)
        this.close();
    });
    this.imgEl.addEventListener("click", (e) => {
      e.stopPropagation();
      next();
    });
    this.keyHandler = (e) => {
      if (e.key === "Escape")
        this.close();
      else if (e.key === "ArrowLeft")
        prev();
      else if (e.key === "ArrowRight")
        next();
    };
    document.addEventListener("keydown", this.keyHandler);
  }
  onClose() {
    document.removeEventListener("keydown", this.keyHandler);
    this.contentEl.empty();
  }
};

// src/main.ts
var MuseViewPlugin = class extends import_obsidian7.Plugin {
  constructor() {
    super(...arguments);
    this.settings = { ...DEFAULT_SETTINGS };
  }
  /** 当前语言的翻译函数。 */
  get t() {
    return createT(resolveLang(this.settings.language, getAppLocale(this.app)));
  }
  async onload() {
    await this.loadSettings();
    this.store = new MemoStore(this.app, this.settings);
    this.registerView(VIEW_TYPE_MUSE, (leaf) => new MuseView(leaf, this));
    this.addRibbonIcon("feather", this.t("ribbon.openMuseView"), () => {
      void this.activateView();
    });
    this.addCommand({
      id: "open-muse",
      name: this.t("command.openMuseView"),
      callback: () => void this.activateView()
    });
    this.addCommand({
      id: "muse-quick-capture",
      name: this.t("command.quickCapture"),
      hotkeys: [{ modifiers: ["Mod", "Shift"], key: "M" }],
      callback: () => void this.quickCapture()
    });
    this.addCommand({
      id: "muse-normalize-all",
      name: this.t("command.normalizeAll"),
      callback: () => void this.normalizeAll()
    });
    this.addSettingTab(new MuseViewSettingTab(this.app, this));
    this.registerEvent(
      this.app.vault.on("modify", (file) => {
        if (file instanceof import_obsidian7.TFile && this.store.isInFolder(file))
          void this.store.reloadFile(file);
      })
    );
    this.registerEvent(
      this.app.vault.on("create", (file) => {
        if (file instanceof import_obsidian7.TFile && this.store.isInFolder(file))
          void this.store.reloadFile(file);
      })
    );
    this.registerEvent(
      this.app.vault.on("delete", (file) => {
        if (file instanceof import_obsidian7.TFile)
          this.store.removeFile(file.path);
      })
    );
    this.registerEvent(
      this.app.vault.on("rename", (file, oldPath) => {
        if (file instanceof import_obsidian7.TFile) {
          this.store.removeFile(oldPath);
          if (this.store.isInFolder(file))
            void this.store.reloadFile(file);
        }
      })
    );
    this.registerEvent(
      this.app.workspace.on("file-open", (file) => {
        if (!file)
          return;
        if (!this.settings.bindFile)
          return;
        if (file.path === this.settings.bindFile) {
          void this.activateView();
          setTimeout(() => {
            var _a;
            const leaves = this.app.workspace.getLeavesOfType("markdown");
            for (const leaf of leaves) {
              const vs = leaf.getViewState();
              if (((_a = vs.state) == null ? void 0 : _a.file) === this.settings.bindFile) {
                leaf.detach();
              }
            }
          }, 0);
        }
      })
    );
  }
  onunload() {
  }
  /** 打开（或聚焦）主视图。focusInput 时顺便把焦点交给输入框。 */
  async activateView(focusInput = false) {
    const { workspace } = this.app;
    let leaf = workspace.getLeavesOfType(VIEW_TYPE_MUSE)[0];
    if (!leaf) {
      leaf = workspace.getLeaf(false);
      await leaf.setViewState({ type: VIEW_TYPE_MUSE, active: true });
    }
    if (leaf)
      workspace.revealLeaf(leaf);
    if (focusInput) {
      const view = this.getView();
      view == null ? void 0 : view.focusInput();
    }
  }
  /** 归一化全部笔记。 */
  async normalizeAll() {
    if (!confirm(this.t("notice.normalizeConfirm")))
      return;
    new import_obsidian7.Notice(this.t("notice.normalizing"));
    try {
      const n = await this.store.normalizeAll();
      new import_obsidian7.Notice(this.t("notice.normalized", { n }));
    } catch (e) {
      console.error(e);
      new import_obsidian7.Notice(this.t("notice.normalizeFailed", { msg: e.message }));
    }
  }
  /** 全局浮层快速记录。 */
  async quickCapture() {
    const existing = document.querySelector(".muse-modal-backdrop");
    if (existing) {
      const lp = existing.querySelector(".muse-lp");
      if (lp)
        lp.focus();
      return;
    }
    const backdrop = document.createElement("div");
    backdrop.addClass("muse-modal-backdrop");
    const modal = backdrop.createDiv({ cls: "muse-modal" });
    modal.createDiv({ cls: "muse-modal-title", text: this.t("quickCapture.title") });
    const sendHint = this.settings.sendHotkey === "enter" ? "Enter \u53D1\u9001" : "Ctrl/Cmd+Enter \u53D1\u9001";
    const editor = new LivePreviewEditor(modal, this.app);
    editor.el.addClass("muse-modal-input");
    editor.setPlaceholder(`\u5FEB\u901F\u8BB0\u5F55 ${sendHint} \xB7 Esc \u5173\u95ED`);
    const btns = modal.createDiv({ cls: "muse-modal-btns" });
    const cancel = btns.createEl("button", { text: this.t("quickCapture.cancel") });
    const send = btns.createEl("button", {
      text: this.t("quickCapture.send"),
      cls: "mod-cta"
    });
    document.body.appendChild(backdrop);
    const cleanup = () => {
      editor.destroy();
      backdrop.remove();
    };
    this.register(() => cleanup());
    setTimeout(() => editor.focus(), 20);
    const doSend = async () => {
      const val = editor.getValue().trim();
      if (!val) {
        cleanup();
        return;
      }
      try {
        await this.store.addMemo(val);
        cleanup();
      } catch (e) {
        new import_obsidian7.Notice(this.t("notice.saveFailed", { msg: e.message }));
      }
    };
    send.addEventListener("click", () => void doSend());
    cancel.addEventListener("click", cleanup);
    backdrop.addEventListener("mousedown", (ev) => {
      if (ev.target === backdrop)
        cleanup();
    });
    editor.setOnKeydown((ev) => {
      if (ev.key === "Escape") {
        ev.preventDefault();
        cleanup();
        return;
      }
      const mod = ev.metaKey || ev.ctrlKey;
      if (this.settings.sendHotkey === "enter") {
        if (ev.key === "Enter" && !ev.shiftKey && !mod) {
          ev.preventDefault();
          void doSend();
        }
      } else if (ev.key === "Enter" && mod) {
        ev.preventDefault();
        void doSend();
      }
    });
  }
  /** 取当前主视图实例（未打开时为 null）。 */
  getView() {
    var _a;
    const leaf = this.app.workspace.getLeavesOfType(VIEW_TYPE_MUSE)[0];
    return (_a = leaf == null ? void 0 : leaf.view) != null ? _a : null;
  }
  async loadSettings() {
    var _a;
    const data = (_a = await this.loadData()) != null ? _a : {};
    if (data.sidebarTagsMode === void 0 && (data.showSidebarTags !== void 0 || data.tagCloud !== void 0)) {
      data.sidebarTagsMode = data.showSidebarTags ? data.tagCloud ? "cloud" : "tree" : "off";
    }
    delete data.showSidebarTags;
    delete data.tagCloud;
    this.settings = { ...DEFAULT_SETTINGS, ...data };
  }
  async saveSettings() {
    await this.saveData(this.settings);
  }
};
